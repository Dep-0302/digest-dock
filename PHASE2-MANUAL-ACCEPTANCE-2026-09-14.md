# Phase 2 验收结果与按需补测（2026-09-15 更新）

## 状态与范围

- 原清单功能基线为 `8a07174`，文档收尾提交为 `a789af7`。2026-09-15 根据用户反馈修正中文轨优先、保存/取消续播及同段重复捕获，最终版本与门禁见收尾记录的当日补记。
- 验收结果按来源保留：用户已通过的项目不重置，Codex 能操作的由 Codex 测；只补测受改动影响的行为。
- 用户已回复“已重载”。Codex 刷新测试页，并在运行实例 `lfcndjcppebmjgalcfkmmgohkelakpfk` 上验证下方修复。对应源码版本为 `19e7c19`。浏览器安全策略不允许工具访问扩展管理页，因此未自行绕过或代为重载。

## 当前结果

| 编号 | 结果与证据 | 后续负责方 |
| --- | --- | --- |
| 中文字幕优先 | **重载后 Chrome 检查通过**：同一视频同时有 `en-US` 与 `zh-Hans` 人工轨；侧栏显示 `原文（zh-hans）` 和真实中文正文。繁体与人工/自动优先的边界沿用本版自动化证据。 | 已完成 |
| 1 | **重载后 Chrome 检查通过**：打开测试想法框时 `paused=true`；Enter 保存后输入框关闭且 `paused=false`；再打开输入不同草稿并 Esc 后同样续播，草稿未保存。 | 已完成 |
| 2 | **重载后 Chrome 检查通过**：同段不同字幕行的两次捕获均找回 `note_18f1b7d0-d7f0-493f-bfc1-239fe8b5ae51`，保留 281 秒原锚点、完整正文和测试想法；当前视频记录数始终为 7，未再新增。 | 已完成 |
| 3 | **YouTube 用户验收通过**（2026-09-15）。 | 已完成；未改动不重测 |
| 4 | **代理检查通过**：真实 Chrome 三视图、上下文和语言切换已验证；另用生产渲染生成的脱敏 DOM 与实际 CSS，在 Chromium 的 320px 宽度下检查，横向溢出 0、越界元素 0，控件无重叠，长文本可换行并正常纵向滚动。 | 已完成 |
| 5 | **YouTube 用户验收通过**（2026-09-15）。 | 已完成；未改动不重测 |
| 6 | **Codex Chrome 检查通过**：当前视频搜索“扭扭捏捏”命中另一视频想法；“翼龙”命中金句；清空恢复当前视频 4 条笔记。 | 已完成 |
| 7 | **Codex Chrome 交互通过**：精确零结果显示按钮，点击“让 AI 找找”后显示 5 条已有候选；原有想法/金句可核对。库逐字节不变及非法 ID 过滤由自动化检查覆盖，不冒充本轮 UI 读数。 | 已完成 |
| 8 | **YouTube 用户验收通过**（2026-09-15）；用户明确未改跟随跳转时沿用此前通过结果。 | 已完成；字幕语言修复只补对应回归 |
| 9 | **Codex Chrome 检查通过**：实际导出截图视频的原文笔记，文件有正确视频 URL、4 个时间码及对应原文，无其他视频内容。 | 已完成 |

下方保留操作说明供失败复现或按需补测，不是要求用户再测一遍。

**测试数据收尾：** 用户明确同意清理后，本轮新增的 04:01、04:39、04:41 三条测试笔记已按 ID 删除；该视频原有 4 条笔记保留。无需用户继续处理测试数据。

## 准备

1. 各选一个已有字幕的 YouTube 和哔哩哔哩视频；记录视频链接和一个便于识别的时间码。
2. 只使用这次新建的临时测试笔记和想法。需要清空或删除时只处理它们，不清空重要的既有笔记。
3. 每项按“操作 → 通过条件”执行；无法执行或没有适用场景，记为“未测”。

## 主项

1. **新金句与第二次 N**
   - 操作：在尚未保存过的新字幕句按一次 `N`，随后在提示仍显示时再按一次 `N`。YouTube 和哔哩哔哩各试一次。
   - 通过：第一次保存一条“金句”且视频继续播放；第二次立即暂停、打开并聚焦“记录想法”输入框。等待至少 15 秒，输入框仍在；Shift+Enter 只换行。原本正在播放时，Enter 成功保存或 Esc 取消均恢复播放；原本暂停仍暂停。取消不保存草稿、金句仍在。
   - 中文输入：实际试一次平时使用的语音输入，识别文字能进入想法并保存。使用拼音输入法时，候选词确认所用的 Enter 不应提前保存，结束选词后的普通 Enter 才保存。

2. **同句重复捕获**
   - 操作：为临时金句保存一段想法，在该完整内容实际覆盖的相邻字幕行再次按 `N`，不必精确回到同一秒。
   - 通过：有明确相同内容与出处证据时笔记数不增加，直接找回原想法。保留一个原回跳点、来源与完整正文；不同内容或不同视频不合并，已有不同想法提示查看而不自动选取。

3. **想法编辑、清空与取消删除**
   - 操作：在笔记页编辑临时想法并保存；再手动清空输入框后保存。重新补写想法，点击“删除想法”并取消；最后再次点击并确认删除。
   - 通过：编辑后立即显示新文字；手动清空保存不弹二次确认。取消删除保留原想法，确认删除才清空。两种清空方式都保留同一条金句、来源和记录日期。

4. **卡片、上下文、语言与窄栏**
   - 操作：在“当前视频”“全部笔记”和搜索结果中查看同一条有想法笔记与一条空想法笔记；切换“原文 / 中文 / 双语”，点击金句展开再收起上下文；将侧栏缩窄。
   - 通过：有想法卡片有明显“想法”区分，空想法显示“金句”；三处显示同一笔记正文与来源，不会换成另一句；上下文默认收起，展开后只见附近内容且无时间码、无需内部滚动；窄栏下搜索和排列控件可见、可点、无横向遮挡。

5. **全部笔记的分组、记忆与排序**
   - 操作：在“全部笔记”分别选择“按日期”和“按视频”，关闭整个侧栏再打开后复查；至少准备两个视频、同日两个不同时间码的临时笔记。已有跨日期记录可直接查看，不必修改日期。
   - 通过：所选排列方式被记住；按日期呈现“日期 → 视频 → 笔记”，按视频呈现“视频 → 日期 → 笔记”。日期与视频组按最近保存时间从新到旧；同一天内按时间码从早到晚；“当前视频”也按时间码从早到晚。

6. **全局搜索与清空恢复**
   - 操作：从“当前视频”和“全部笔记”各试一次，用临时想法里的独特词搜索另一视频的笔记，再搜一个只在金句正文里的词；清空搜索框。
   - 通过：搜索能跨视频找到已有匹配；清空后回到进入搜索前的当前/全部视图、原分组和排序，不残留旧搜索结果。

7. **零结果后的 AI 候选**
   - 操作：用已有想法的另一种说法搜索，让精确搜索无结果；出现“让 AI 找找”后点击一次。此项使用已配置的 AI 服务，暂不想调用可记“未测”。
   - 通过：按钮只在零结果时出现；若返回候选，只展示已有笔记，最多 5 条，原有金句和想法文字保持不变。有合理候选才算验证了候选展示；返回 0 条本身不算故障，但候选展示部分仍记“未测”。服务报错请记录实际提示。

8. **跨视频、双平台回跳与 YouTube 字幕恢复**
   - 操作：从“全部笔记”和搜索结果各试一次回跳，包括当前视频另一时间点、另一 YouTube 视频、YouTube 与哔哩哔哩之间相互回跳。YouTube 回跳后再点击字幕。
   - 通过：打开正确视频并定位到保存的时间码附近（播放开始后的自然前进不算跳错）；YouTube 自动恢复/准备字幕时仍停在笔记页，点击字幕即可看到进度或结果，无需刷新视频页。字幕内容与当前视频相符。

9. **已有内容的导出回归**
   - 操作：对含临时笔记的来源容器点击“导出本视频笔记”，只检查已有内容的导出文件。
   - 通过：按钮名称明确为“导出本视频笔记”，导出文本包含该视频的已有笔记信息。若出现“需补资料/补译”或“完整导出”等路径，停止并记“未测”，不点击任何可能触发补全、付费或第三方调用的操作。

## 条件项（有场景才测）

A. **YouTube 换视频与广告**：正常换到另一个视频，旧字幕应退出并对应新视频；自然遇到广告时，广告的 0 秒不会被当作正片时间，正片恢复后继续跟随。未遇到广告就记“广告未测”。
B. **哔哩哔哩分 P**：在 P1 的临时笔记上打开但不保存想法，切到 P2 后再按 `N`；P1 的编辑框应关闭，P2 保存自己的笔记，不能覆盖 P1。从全部笔记回跳 P1/P2 时应到正确分 P 和时间码。

## 回传格式

用户只需反馈新问题或 Codex 说明无法完成的部分；已报告的 3、5、8 无需重发。必要时按编号描述“失败：实际看到……”，附平台和操作即可，不需要检查内部数据或打开 DevTools。
