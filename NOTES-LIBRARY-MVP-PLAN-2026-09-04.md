# DigestDock 笔记库 MVP：调整优化执行方案

> **历史记录（2026-09-14 归档）：** 下文的状态、版本、门禁数字和执行约束属于文中所标的原阶段，不代表当前分支。保留原文用于追溯；当前阶段 2 的适用修订见 [规格 §10](PHASE2-THOUGHT-LAYER-2026-09-09.md#10-2026-09-14-收尾修订现行判据)，最终状态见 [收尾记录](PHASE2-CLOSEOUT-2026-09-14.md)。

> 文档状态：阶段 0 已完成并验收；阶段 1 待启动
> 起草日期：2026-09-04（第三版）。2026-09-05 更新阶段 0 状态与阶段 1 前置约束
> 基线：`main` @ `ac1c840 Release DigestDock 1.4.8`
> 本版并入 Codex 只读审计（2026-09-04）中经复核属实的发现

本文是调整方向与验收契约，不是实施完成证明。代码修改、自动测试、真实 Chrome 验收、提交、推送与发布必须分别记录。

---

## 1. 产品判断

核心价值在**笔记库**，不在字幕获取。

字幕获取是成本，且是最容易被平台封死的一环；AI 概览正被 YouTube 原生 Ask 与 B站原生总结吸收。本方案把工程重心倒过来，**并且不新增任何取得字幕的能力**。

### 1.1 竞争位置：不是空的，是没人做起来

同时覆盖 YouTube + B站 + 双语 + 时间戳的直接竞品当前存在至少三个：VideoSeek（约 3,000 用户 / 4.7）、AI Watch Later（约 2,000 / 2.4）、Litit（约 800 / 2.0，功能描述与本项目几乎完全重合）。

关键读数不是「位置被占」，而是**进入这个位置的产品都没有获得采用**。Litit 尤其说明问题：功能齐全本身不带来任何增长。

因此本方案不以功能数量竞争。差异化只押在一处：**这个库里存的是使用者自己的想法，不只是视频说了什么。** 上述竞品无一具备这一点。

## 2. 核心链路

用户自述的真实使用回路：

```
看视频 → 某一句触发一个想法 → 记下来
                              ↓
      日后检索到这条想法 → 回到触发点 → 想起当时为什么会这么想
```

**时间码的语义由此确定**：不是「这条想法是关于这一秒的」，而是**「回到这一秒，我能重建当时的状态」**。它是回忆的钥匙，不是内容的分类。

这条链路决定全部取舍。任何让「记下来」变慢、让「回到触发点」变难、或**让钥匙本身失真**的设计，优先砍掉。

## 3. 数据模型：一条笔记，三层

| 层 | 来源 | 规则 | 现状 |
| --- | --- | --- | --- |
| **触发原句** | 字幕逐字 | **永不改写，永不 AI 处理** | 已存为 `rawText`，但**未被优先呈现**（见 3.1） |
| **触发窗口** | 字幕前后片段 | 冻结，随笔记保存 | **正在被丢弃**（见 3.2） |
| **AI 整理** | 可选 | 必须标注来源与生成时间 | 已存为 `text`，但**冒充了「原文」**（见 3.1） |
| **我的想法** | 用户输入 | **不做 AI 润色** | **完全不存在**（见 3.3） |

四行三层：触发原句与触发窗口同属锚点层。三层同属一条记录，不需要新的关系模型。

模型必须是层而非类型：分成「摘录 / 想法」两类笔记，就无法**给一条已有笔记后补想法**——而「重看触发点、想起来了、就在那条上写一句」正是回路里最关键的一步。

### 3.1 「原文」显示的不是原话（最高优先级的语义缺陷）

```
cleanupNoteText()   有 API Key → 送 Provider 改写 → 结果存入 note.text
                    无 API Key → 前后三行拼接后存入 note.text
noteOriginalText()  默认优先返回 note.text（改写版）；rawText 无任何 UI 入口
```

唯一必须逐字保真的字段正在被改写，且用户无法看到真正的原句。对本产品，这不是标注问题，是**回忆的钥匙在存进库之前就已经失真**。

`rawText` 一直在持久化，数据没有丢——只是没有被优先、没有入口。因此修复成本很低。

**规则（不可让步）**：「原文」模式必须返回逐字的 `rawText`。AI 整理是独立的第三层，显式标注，绝不占用「原文」这个名字。想法层同样不做 AI 润色——想法是原话，润色会抹掉回忆线索。

### 3.2 触发窗口正在被无成本地丢弃

`background.js:4640` 附近已收集触发点前后的字幕 `contextLines`，作为输入喂给 `cleanupNoteText()`，随后丢弃。笔记上只保留单行 `rawText`。

这段上下文正是「重建当时状态」的材料，保存时已在内存中，持久化它没有额外获取成本。视频被删、被限制、或只是懒得打开时，冻结的触发窗口是回忆链路的唯一依托。

### 3.3 库里没有一个字是用户写的

`background.js:4690`：`text` 来自 `cleanedText || matchedLine.text`，两者都派生自字幕。全仓 `textarea` / `contenteditable` 仅两处，都是「用户在别处打字时忽略快捷键」的守卫。

**至今没有任何让用户输入自己文字的路径。** 库里 100% 是视频说了什么，0% 是用户想了什么。补上想法层之前，任何检索改进都命中不了真实的检索目标。

### 3.4 静默丢数据缺陷

`background.js:4993 saveNoteToStorage()` 在超过 100 条时 `notes.splice(MAX_SAVED_NOTES)` 删除最旧一条，随后 `return true` 报告保存成功。全仓无提示、无拒绝路径、无日志。

### 3.5 存储结构

全部笔记存在单一 key `ytd_notes` 的一个数组中。每次保存 = 读全数组 → unshift → 写回全数组；每次渲染 = 加载全部再 `groupNotesBySource` 内存分组。没有索引，因此没有检索的着力点。上限 100 是在给这个模型兜底。

上限有两个独立真源：`background.js:36 MAX_SAVED_NOTES` 与 `notes-backup.js:18 MAX_NOTES`，互不引用，改一侧会造成「能存但导不出」。

---

## 4. 阶段划分

四个阶段严格顺序执行。阶段 0 独立发布。

### 阶段 0 — 止血（不改数据模型）— **已完成**

> **交付**：分支 `codex/notes-trust-phase0` @ `07eb21b Harden Phase 0 note trust and reset semantics`（领先 `main` 一个提交，无 merge）
> **门禁**：`npm run check` 754/754，47 个白名单文件通过（2026-09-05 独立复跑确认）
> **真实 Chrome 验收**：`PHASE0-ACCEPTANCE-2026-09-04.md`，六项通过
> **尚未**：push / merge / release
>
> 实际交付范围显著大于本节所列七项：提交为 6,672 增 / 611 删、22 文件，其中生产代码 2,692 增 / 509 删，另行改动了导出任务、翻译任务、Supadata 与 native 冷却、Passive 状态处理、消息监听器与 `options.js`。超出部分有自动化测试覆盖，但**不在本轮真实浏览器验收范围内**。这一事实是下文 §4.1 前置约束的直接来源。
>
> 两处与本节原定内容不同的最终形态，均为有意为之：
> - `noteOriginalText()` 取消了中文源分支，改为无条件 `rawText || text`。与 §3 的「触发原句逐字」规则一致，不再为中文开例外；代价是已有中文笔记的「原文」显示内容与此前不同。
> - `handleResetAllExtensionData()` 从平铺函数改为三层队列加代次计数，源于真实验收中发现的「重置成功但 UI 报失败」竞态。
>
> **遗留待补**：英文 YouTube 正向路径未在真实浏览器验证（取样视频原生 CC 不可用，仅验证了失败关闭）。非中文源正是「原文」缺陷所在的分支，应在阶段 1 启动前用一个确有原生英文 CC 的视频补验一次。

全部为读写路径的小改动，不触碰存储结构。

1. `saveNoteToStorage()` 不再 `splice`。达到上限返回明确失败（`NOTE_STORAGE_FULL`），保留全部已有笔记。
2. **`noteOriginalText()` 改为优先返回逐字 `rawText`**；`text` 在 UI 上明确标注为「AI 整理」。这是 3.1 的最小修复，数据已在，只改读取与呈现。
3. 笔记 Tab 在接近或达到上限时显示容量状态条，含「先导出备份」入口。不用错误红色容器——这是容量事实，不是错误。
4. 两处上限常量合并为单一真源；上限临时提到 500。
5. `handleResetAllExtensionData()` 同步清理 `chrome.storage.session`（当前只 `local.clear()`，Passive bridge 的会话状态会残留）。
6. manifest 声明 `"incognito": "not_allowed"`。当前无此字段，默认 spanning，隐身模式下观看的视频会把笔记写进普通资料库。
7. 修正 `README.zh-CN.md:11` 的「阅读和搜索带时间戳的原始字幕」——当前 `sidepanel.html` 无任何搜索入口。声明与实现必须一致；搜索在阶段 2 实现后再改回。

**验收**：新增测试覆盖「第 N+1 条被拒绝且第 1 条仍在」、「原文模式返回 rawText 而非 text」、「重置后 session 为空」；真实 Chrome 构造满库场景确认无静默删除。

### 4.1 阶段 1 前置约束：迁移提交只做迁移

**这条约束的地位高于阶段 1 的任何功能项。**

阶段 0 的超范围交付结果是好的——发现的竞态是真的，补的测试是真的。但阶段 1 动的是**数据迁移**，同样幅度的扩张在那里的风险性质完全不同：

- 阶段 0 扩错了，最坏结果是回滚一个提交。
- 迁移扩错了，最坏结果是**笔记回不来**。

迁移是本方案唯一不可逆的一步，因此：

1. **迁移提交只包含迁移。** 存储结构改写、三个新字段、备份与回滚路径——只有这些。
2. **迁移期间发现的任何其他问题一律只记录，不在同一提交内修复。** 记到待办里，迁移落地后另开分支处理。竞态、UI 缺陷、文档不一致，全部适用。
3. **迁移提交的 diff 必须能被一次读完。** 如果读不完，说明第 1 条已经被违反了。
4. **迁移的真实浏览器验收必须覆盖迁移本身的全部路径**：空库、1 条、满库、超限库、损坏数据、迁移中断后重启。验收范围与改动范围不匹配时，不得进入下一阶段。

违反其中任何一条，正确的动作是把提交拆开，而不是补测试。

---

### 阶段 1 — 存储模型重置 + 补齐三层

本方案唯一的架构改动。**三个新字段必须在同一次迁移完成**，否则每漏一个就要再迁移一次。

**目标结构**

| Key | 内容 |
| --- | --- |
| `ytd_note_index` | 检索索引：`id`、`mediaKey`、`platform`、`videoTitle`、`channelName`、`timestampSeconds`、`savedAt`、`hasThought`，以及想法与触发原句的规范化检索副本 |
| `ytd_notes_<mediaKey>` | 该视频下的完整记录（想法、触发原句、触发窗口、AI 整理、译文、校验字段） |
| `ytd_notes_schema` | 结构版本号 |

**新增字段**

- `thought`：用户自己写的文字，可空，可后补
- `thoughtAt`：想法写入时间（与 `createdAt` 分开——后补想法的时间不同于锚点建立时间）
- `triggerWindow`：触发点前后字幕，保存时从已有的 `contextLines` 直接落盘，冻结不再更新

**按视频分片而非按笔记分片**：UI 与导出的自然单位都是视频——`groupNotesBySource`、笔记 Tab 渲染、TXT 导出全按来源。按单条分片写入更细，但键数随笔记数线性膨胀，渲染一个视频要发 N 次读取。

**迁移**

1. 检测到旧的 `ytd_notes` 数组时，**先自动生成 JSON 备份并触发下载**，再迁移。
2. 复用 `notes-backup.js` 已有的 `schemaVersion` / `SUPPORTED_SCHEMA_VERSIONS` 机制，不新造。
3. 迁移失败可回滚且不破坏 `ytd_notes`；旧 key 保留一个版本周期后清理。
4. 历史笔记迁移后 `thought` 与 `triggerWindow` 为空，且窗口无法补回。这是可接受的损失，但必须在迁移说明中写明——**也是阶段 1 越早做越好的理由**。
5. 取消固定条数上限，改为容量护栏（`unlimitedStorage` 已持有）。`notes-backup.js` 导入校验同步改为同一护栏。
6. 单条删除与整库重置须一并清理该笔记的孤儿来源资料（`YTD_NOTE_SOURCES`、导出任务残留）。

**验收**：迁移测试覆盖空库 / 1 条 / 100 条 / 超限库 / 损坏数据；迁移后备份导出与导入往返一致；真实 Chrome 确认旧安装升级零丢失。

**不做**：不改已有字段语义；备份文件对外格式保持向后兼容。

### 阶段 2 — 想法捕获 + 检索回跳

护城河在这一阶段。四块，缺一块链路就断。

**2.1 捕获：一个动作，零决策（硬约束）**

```
快捷键 → 输入框浮出（时间码自动取当前播放位置）→ 打字 → Enter → 存完消失
```

中间不得插入：选类型、选标签、选归属、二次确认、保存成功弹窗。

这条约束**优先于功能完整性**。任何一步额外决策都会让功能在真实使用中静默失效，而失效方式是本人不会察觉自己已不再使用它。

同时提供「给已有笔记补写想法」的入口：笔记卡片上一个内联输入，不打开新界面。

**任何会外发到 AI Provider 的保存动作，必须在触发控件处可见地标注。** 想法层默认不外发。

**2.2 检索：想法优先，触发点作上下文**

- 笔记 Tab 顶部单一搜索框，输入即过滤，作用于 `ytd_note_index`
- 检索范围：想法、触发原句、视频标题、频道名
- 过滤维度：**只有**平台（YouTube / B站）与时间范围
- 排序：有想法的笔记优先。摘录会积到几百条、想法几十条，同权排序等于想法被淹没
- 结果卡片形状：**想法在上，触发原句与触发窗口以次级样式附在下方**。搜到的是自己的话，看到的是当时在听什么
- 空结果 / 无索引 / 索引重建中三种状态分别渲染，沿用 DESIGN.md 的区域状态机约定

**2.3 回跳：链路的闭合端**

每条检索结果一键回到该视频该时间点，跨视频、跨平台均可用。这是整条回路的终点，必须无条件可用；视频不可达时降级为展示冻结的触发窗口，并明确说明原因。

**2.4 时间轴入口**

与搜索框并列的第二个入口：按天倒序、跨视频跨平台、只显示带想法的笔记。

搜索需要先记得关键词，时间轴不需要记得任何东西。`savedAt` 已在索引中，成本接近于零。

**验收**：1000 条量级下输入到渲染延迟可接受；捕获从按下快捷键到存完不超过一次交互；跨平台回跳全部可达；概览章节与重点语句的键盘可达性一并修复；已有笔记 Tab 的分组、排序、导出无回归。

**不做**：模糊匹配、向量或 AI 语义检索、保存的搜索条件、想法的 AI 润色。

### 阶段 3 — 出口

**Markdown 到剪贴板**，作用域为单条 / 单视频 / 当前检索结果。

- YAML frontmatter：来源 URL、平台、频道、视频标题、导出时间
- 每条输出想法 + 触发原句 + 可点击时间戳链接，三层关系保持可见，AI 整理明确标注
- 语言模式沿用笔记 Tab 当前设置，不新增语言逻辑

**验收**：粘贴进 Obsidian 后 frontmatter 正确解析、时间戳链接可回跳。

**不做**：OAuth、Notion API、双向同步、自动写文件系统。这些都要引入账号或后端，与「无后端 BYOK」定位直接冲突——而那是当前唯一无人替代的一条。

### 阶段 4 — 上架决策门（门，不是任务）

阶段 1–3 完成、且自用满 30 天后，回答一个问题：

> 我上架，是为了验证有人愿意用吗？

**答「否」→ 不上架，继续作为私有工具。以下全部不做。**

**答「是」→ 才执行下列清单**（并入 Codex 审计的治理项，比原清单完整）：

- 隐私政策公开 URL、Limited Use 声明、公开支持入口
- AI Provider 域名改 `optional_host_permissions`；逐项论证 `tabs` / `scripting` / `unlimitedStorage` 的最小必要性
- 砍掉自动开关标签页的「补充导出」，或为其写明权限论证
- 商店素材：128 图标、5 张 1280×800 截图、440×280 small promo；en / zh 双语 listing
- 商店文案回避「破解 / 无限提取 / 下载任何字幕」类表述；专项核对 YouTube 与 B站条款对固定 IOS/json3 路线的口径
- 更新 README 中「GitHub-only / 无支持渠道」等与上架状态冲突的旧文案
- 修正发布登记册漂移（当前登记 1.4.4，源码 1.4.8），为候选版本建立 `release.yaml`，锁定 commit、文件清单与最终 ZIP SHA
- 打包脚本保留文件时间戳导致 SHA 不可复现，改为可复现构建
- 开发者注册与一次性费用、2FA、Trader 状态、目标分发范围
- **对候选版本做全新安装 / 升级 / 重启 / 无 Key / 错误 Key / 断网 / 无字幕 / YouTube / B站 / 删除 / 重置的真实 Chrome 验收。** v1.4.7 的历史验收不能继承到改过后台与设置路径的新版本。

---

## 5. 明确不做

- **不做标签系统。** 标签要求捕获时做分类决策 + 长期维护分类法，与 2.1 的零决策约束直接冲突。时间 + 视频 + 全文对本库规模已经够；将来若需分类，让它发生在检索侧，不在捕获侧。
- **不做用户验证与分阶段发布的前置设计。** 在想法层存在之前做 5–10 人定向验证，验证的是一个核心尚未建成的产品。
- 不新增 AI provider；不做 ASR / OCR / 音频转录
- 不改动字幕取得链路（Passive → 固定 Active → CC → Supadata 的既定边界不动）
- 不追平台原生摘要能力；不做 AI Chat、思维导图、播客、幻灯片
- 不做订阅、账号、云同步或任何后端
- 不动侧栏既有的字幕 / 概览两个 Tab

## 6. 顺序理由

阶段 0 独立且紧急——它同时在修「正在丢数据」和「回忆钥匙正在失真」两件事，两者都是每天都在发生的损失，且都不需要动数据模型。

阶段 1 必须先于 2：没有 `thought` 字段就没有可检索的想法，没有索引就没有检索的着力点。三个新字段必须同批迁移。

阶段 3 必须后于 2：出口的形状取决于三层关系最终长什么样。

阶段 4 是门不是任务——它的答案取决于阶段 1–3 完成后的真实使用体感，现在回答只是猜测。把治理清单提前执行，等于用确定的成本去换一个尚未成立的前提。

## 7. 如果只做一件事

**阶段 2 的完整回路：捕获 → 检索 → 回跳。**

三块缺一块链路就断：能记不能找等于没记，能找不能回跳等于找到一句无根的话。阶段 0 和 1 存在的全部意义，是让这条回路成立且可信。
