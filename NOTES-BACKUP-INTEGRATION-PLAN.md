# YouTube Digest 笔记备份：主线同步、验收与合并执行方案

> 文档状态：冻结，待用户确认执行
> 冻结日期：2026-08-19
> 功能工作树：`/Users/wangchao/Documents/youtube-digest-notes-backup-import-export`
> 功能分支：`codex/notes-backup-import-export`
> 主线目录：`/Users/wangchao/Documents/youtube-digest`

## 1. 当前有效的项目目的

在不参与主线问题修复、不污染主线、不接触真实笔记与真实 API Key 的前提下，将“笔记 JSON 备份与恢复”功能同步到已稳定的主线版本，在功能工作树完成集成验证和临时 Chrome 验收；只有用户再次确认后，才合并回主线。

## 2. 冻结基线

### 2.1 主线基线

- 用户声明：主线已就绪。
- 2026-08-19 只读回查：工作区干净。
- 分支：`main`
- HEAD：`fd2fd582bf24676ef57dcea96530cb12f28d8f21`
- 提交：`fd2fd58 Release 1.2.1 with translation recovery fixes`
- 版本：`1.2.1`
- 本地状态：`main...origin/main [ahead 5]`

主线领先远端属于当前事实，不构成本方案的推送或发布授权。

### 2.2 功能工作树基线

- 分支：`codex/notes-backup-import-export`
- HEAD：`1a35e10bf4aa416eebff6314c54da8c4bf356c5b`
- 提交：`1a35e10 Recover failed Chinese note translations`
- 版本：`1.1.5`
- 当前功能改动尚未提交。

冻结文件范围：

- `notes-backup.js`
- `background.js`
- `options.html`
- `options.css`
- `options.js`
- `sidepanel.js`
- `README.md`
- `README.zh-CN.md`
- `PRIVACY.md`
- `SECURITY.md`
- `scripts/check-release.sh`
- `tests/notes-backup.test.js`
- `tests/options-language.test.js`
- `tests/release.test.js`
- `tests/translation.test.js`

出现此清单之外的新改动时停止，不自动纳入。

### 2.3 已有开发证据

- 功能工作树自动化测试：77/77 通过。
- 发布检查：23 个白名单文件通过。
- 本地打包成功；最近修正版 ZIP SHA-256：`648ab195877a282a5b2e3d35e74c5fe8a46201a39d67468a60e3fc12750a213e`。
- 合成数据浏览器验证覆盖导出、首次导入、重复导入、损坏文件拒绝和空概览缓存保护。
- Gemini 3.7 Flash (High) 代码与证据审查：核心项通过；保留一项 P2 旧本机重复 ID 导入限制和一项 P3 多窗口删除刷新问题，均不自动扩建。

以上只证明旧基线上的开发验证，不等于与主线 `1.2.1` 集成通过，也不等于用户最终验收。

## 3. 冻结范围

### 3.1 本方案包含

1. 核对主线和功能工作树起点。
2. 冻结并本地提交功能工作树现有改动。
3. 在功能工作树中吸收已批准的主线。
4. 解决功能范围内的文本和逻辑冲突。
5. 运行集成测试、发布检查与打包。
6. 使用临时 Chrome 资料和合成笔记完成真实导出/导入验收。
7. 提交验收证据，等待用户确认。
8. 用户确认后合并回主线并做主线后验检查。

### 3.2 本方案不包含

- 修复、重构或重新评审主线自身问题。
- 自动处理执行时新增的主线提交。
- 扩建 P2/P3 非阻断项。
- 使用真实笔记、真实 API Key 或真实服务商请求。
- 推送 GitHub、创建 PR、发布 Release 或公开版本。
- 删除工作树、删除分支、清理真实 Chrome 数据。
- 对主线执行 `reset`、强制覆盖或改写历史。

## 4. 授权门槛

本文件落盘不启动实施。

用户发送以下明确指令后，才执行阶段 A 至阶段 E：

> 方案确认，按冻结方案执行到合并前验收门。

阶段 E 完成后必须停止。只有用户再次发送：

> 验收通过，确认合并。

才执行阶段 F。

## 5. 执行阶段

## 阶段 A：执行前只读核对

### 动作

1. 读取 `git worktree list --porcelain`。
2. 核对主线分支、HEAD、版本和工作区状态。
3. 核对功能工作树分支、HEAD、版本和改动文件。
4. 保存主线与功能分支的执行前提交号。

### 门槛

- 主线必须干净。
- 主线 HEAD 必须仍为本文件记录的 `fd2fd58...`。
- 功能工作树只能包含冻结清单内的改动和本计划文件。

### 停止条件

- 主线 HEAD 变化：停止，列出变化，请用户重新确认该主线仍可作为集成基线。
- 主线不干净：停止，不处理主线改动。
- 功能范围出现未知文件：停止，不猜测归属。

## 阶段 B：功能工作树检查点

### 动作

在功能工作树运行：

```bash
npm test
npm run check
npm run package
git diff --check
```

检查 ZIP 文件清单、版本和 SHA-256。全部通过后，仅暂存冻结清单内文件和本计划文件，创建功能分支本地提交。

建议提交信息：

```text
Add notes backup and restore
```

### 门槛

- 测试、发布检查、打包和差异检查全部通过。
- 暂存文件与冻结清单一致。
- 不推送远端。

## 阶段 C：主线同步进功能工作树

### 动作

只在功能工作树执行：

```bash
git merge main
```

不得在主线目录解决功能冲突。

重点复核：

- `background.js`：主线恢复/翻译逻辑与笔记写入队列、代际保护。
- `sidepanel.js`：中文优先、缓存恢复、空概览保护与 `notesChanged` 本地刷新。
- `tests/translation.test.js`：主线回归与备份测试合同同时成立。
- README、Privacy、Security：功能说明与主线 `1.2.1` 状态一致。
- `manifest.json`、`package.json`：以批准的主线版本为准，不沿用旧工作树的 `1.1.5` 判断。

### 冲突规则

- 可证明等价的格式或版本冲突可以在功能工作树解决。
- 行为含义、数据格式、重试语义或安全边界不明确时立即停止，展示冲突片段和两种结果，等待用户决定。
- 无文本冲突也必须执行行为测试，不得直接宣称集成完成。

## 阶段 D：集成验证

### 自动化门槛

```bash
npm test
npm run check
npm run package
git diff --check
```

### 必查行为

- 导出仅读取 `ytd_notes`。
- JSON 只含白名单字段，不含 API Key、设置、完整字幕或摘要缓存。
- Unicode、换行、时间戳和中英文笔记往返完整。
- 导入重建安全 YouTube URL，不信任文件中的 URL。
- 损坏 JSON、错误 schema、重复 ID 和容量超限被拒绝，原数据不变。
- 同内容不同 ID 的笔记均保留。
- 旧重复 ID 在导出副本中稳定修复。
- 导入、保存、删除、清空和重置的写入队列不丢数据。
- 导入刷新不触发翻译网络请求。
- 慢保存不会在清空或重置后写回。
- 主线字幕、概览、翻译和 watch-page 恢复测试无回归。

### 产物

- 测试总数与失败数。
- 发布白名单文件数。
- 最终 ZIP 路径、文件清单和 SHA-256。
- 集成后功能分支提交号。

任一检查失败即停止，不进入 Chrome 验收。

## 阶段 E：临时 Chrome 合成数据验收

### 环境边界

- 使用临时 Chrome 资料或隔离的测试扩展实例。
- 只使用合成笔记备份。
- 不填写真实 API Key，不发送 Supadata/DeepSeek 请求。
- 不导入或删除用户真实笔记。

### 验收清单

| ID | 验收项 | 预期结果 |
|---|---|---|
| A01 | 设置页中英文显示 | “笔记备份”、说明和按钮均正确 |
| A02 | 空笔记导出 | 提示没有可导出的笔记，不生成无意义文件 |
| A03 | 导入合成备份 | 显示新增数量，笔记写入本地存储 |
| A04 | 导出已导入笔记 | 下载版本化 JSON，文件名正确 |
| A05 | 文件内容核对 | 无 Key/设置/摘要缓存；文本、中文、时间戳完整 |
| A06 | 清空后恢复 | 导入同一备份后完整恢复 |
| A07 | 重复导入 | 不新增重复笔记，数量不变 |
| A08 | 损坏/错误版本文件 | 明确拒绝，已有笔记不变 |
| A09 | 容量与冲突 | 超过 100 条或相同 ID 冲突时整批拒绝 |
| A10 | 控制台与网络 | 无新错误；导入过程无 AI 请求 |
| A11 | 主线冒烟 | 设置、字幕、概览和 watch-page 基本入口正常 |

### 结果定义

每项只允许：`通过`、`失败`、`未验证`。不得用“应当”“看起来”“预计”代替结果。

### 合并前验收门

完成后输出验收清单、证据和遗留风险，然后停止。没有用户“验收通过，确认合并”的明确指令，不进入阶段 F。

## 阶段 F：用户确认后的主线合并

### 合并前二次核对

1. 主线必须仍干净。
2. 主线 HEAD 必须与阶段 A 记录一致。
3. 若主线在阶段 A 后新增提交，停止；先回到功能工作树重新同步和复测。

### 合并动作

在主线目录将 `codex/notes-backup-import-export` 合并到 `main`。

合并后运行：

```bash
npm test
npm run check
npm run package
git diff --check
```

再做一次短 Chrome 冒烟验证。

### 完成证据

- 合并前主线提交号。
- 功能分支提交号。
- 合并后主线提交号。
- 主线测试、发布检查、打包结果。
- 最终 ZIP 路径与 SHA-256。
- 主线工作区状态。

本阶段不包含推送或发布；如需执行，另行授权。

## 6. 全局停止条件

任一情况出现时立即停止并报告：

- 主线 HEAD 或清洁状态与批准基线不一致。
- 功能范围出现未确认文件。
- 合并冲突含义不明确。
- 自动化测试、发布检查或打包失败。
- Chrome 行为与自动化结果不一致。
- 导出文件包含越界数据。
- 操作需要真实 Key、真实笔记、外部发布或破坏性 Git 命令。

## 7. 回退与保留

- 阶段 F 前的所有变化都停留在功能工作树，主线不受影响。
- 记录所有关键提交号和 ZIP 哈希。
- 不使用 `git reset --hard`、强制检出或历史改写作为默认回退。
- 合并后若需要回退，先展示精确合并提交和影响，再取得单独授权。
- 合并成功后不自动删除功能工作树，保留至用户确认可以清理。

## 8. 准确下一步

当前只完成本计划文件落盘，尚未执行阶段 A。

用户审核无误后，发送：

> 方案确认，按冻结方案执行到合并前验收门。

## 9. 修订记录

| 版本 | 日期 | 内容 | 状态 |
|---|---|---|---|
| v1 | 2026-08-19 | 冻结主线同步、集成验证、临时 Chrome 验收和双重合并授权门 | 待用户确认 |
