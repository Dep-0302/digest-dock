# DigestDock 阶段 2 本地交付记录

> **历史记录（2026-09-14 归档）：** 下文的状态、版本、门禁数字和执行约束属于文中所标的原阶段，不代表当前分支。保留原文用于追溯；当前阶段 2 的适用修订见 [规格 §10](PHASE2-THOUGHT-LAYER-2026-09-09.md#10-2026-09-14-收尾修订现行判据)，最终状态见 [收尾记录](PHASE2-CLOSEOUT-2026-09-14.md)。

> 当前状态：代码实现、本地提交、代码复查及真实 Chrome T1–T10 标准场景验收已完成；T7 在用户修正右侧实例 API 配置后通过。仍停留本地分支，未 push / merge / release；用户个人试用验收与发布是后续独立步骤。
> 基线：`main @ cea382e Release DigestDock 2.0.0`。
> 当前分支：`codex/thought-layer-phase2`，HEAD `5a589d5`。
> 行为真源：`PHASE2-THOUGHT-LAYER-2026-09-09.md` 的 225 行二次修订版；用户随后明确授权 B 站对齐，并采纳原 Claude 会话最终三项收尾方案。

## 1. 已实现

- YouTube 与 Bilibili：第一次 N 存金句、不暂停；10 秒内第二次 N 就地输入、聚焦并暂停；Enter 原文保存、不恢复播放；Esc 丢弃草稿、保留金句。
- toast 过期后下一次 N 新建笔记，不误改上一条；后台重启、分 P 切换、迟到响应均有归属保护。
- 想法写入只改变 `thought` / `thoughtAt` 及既有派生索引；空想法对应 null thoughtAt；不改变 createdAt、日期归属、来源、锚点或冻结窗口。
- 当前视频默认与原列表保留；全部笔记按浏览器本地自然日分组；无置顶、待确认计数或缺失标记。
- 搜索临时全局，清空恢复原筛选；匹配想法、原句、标题、频道；想法优先、组内保存时间倒序。
- 最近与搜索结果都可直接编辑想法；编辑后不匹配当前查询的条目立即移出结果。
- 零结果 AI 兜底仅在点击后请求；只接受本次输入与当前现存笔记 ID 的交集，去重且最多 5 条，展示从本地原笔记取，不改库。
- 应用的单次检索输入预算为 12,000 字符，不宣称是模型上下文极限；完整笔记按最新前缀装入，截断显示最近 N 条。正常小库全部送入，过大的单条不拆写、不自动多轮。
- 搜索和最近的回跳具有精确路由探测、实际秒数复读；立即显示正在确认，失败显示冻结材料并放在被点卡片内，原因不推测；成功或被取代时清掉本次提示，不误清下一次提示。
- 重置笔记数据时，已显示/迟到搜索结果和未完成的回跳反馈都失效，不重新显示已删除的材料。

## 2. 本地提交

| 提交 | 内容 | 边界 |
| --- | --- | --- |
| `7d522df` | 42 项阶段 2 红测与真实脚本测试桩 | 提交前 3 过、39 个 ERR_ASSERTION；含用户要求的 toast 归属与正常 8 条库全量输入补测 |
| `f9571ee` | YouTube 捕获与存储 | T1–T4、T9 写入侧；原 Claude 会话复核通过 |
| `9e0d021` | B 站捕获对齐 | 用户明确追加授权；原 Claude 会话复核通过 |
| `32f571f` | 检索与最近 | T5/T6/T8、T9 界面；首次错用新会话审查，随后已在原会话独立补审，未改历史掩盖失误 |
| `1f80a4b` | AI 候选检索与回跳验证 | T7/T10；原会话独立核对哈希及全量门禁后同意提交 |
| `d1a5229` | 旧概览测试桩补 classList.remove | 一行测试环境适配，独立提交；原断言不变，不改业务行为 |
| `5a589d5` | 三项收尾及重置竞态修正 | 用户采纳最终清单后实施；K3 初轮及增量静态复查均 PASS |

最终相对基线：9 个代码/测试文件，新增 1525 行、删除 40 行。约一半新增内容为测试与测试桩。根目录规格及本说明均未提交。

## 3. 最终本地门禁

| 命令/检查 | 结果 |
| --- | --- |
| `npm test` | 871 / 871 通过，失败/取消/跳过均为 0，退出码 0 |
| `npm run check` | 871 / 871 通过；47 个白名单文件检查通过，退出码 0 |
| 阶段 2 测试 | 62 / 62 通过；原有 809 项保持通过 |
| `git diff --check` | 通过 |

不把两条命令重复执行的测试相加。红测提交及中间实现提交不被描述为逐提交全量绿；这里报告的是最终工作内容的门禁。

测试发展过程：40 项初稿 → 补 A/B 为 42 项 → 后台重启回归 43 项 → B 站共用断言和分 P 专项 54 项 → 播放器可用性/定位复读 56 项 → 搜索编辑与反馈归属 59 项 → 三项重置竞态 62 项。新增行为先出现对应断言失败，再实现变绿，未通过删除/放松断言取得通过。

门禁适配说明：`tests/translation.test.js` 的概览桩原先只有 classList.toggle/contains；新的重置 UI 清理调用浏览器原生 remove 时，该桩报 TypeError。补齐 remove 方法后，原来的“过时概览响应不得回写”断言保持原样并通过。该适配已单独提交为 `d1a5229`。

## 4. 复查证据与沟通纠正

原 Claude 会话为「digestDock Chrome 插件评估」，会话 ID `cse_012ybnpwSW8N8yFhAZVzKj5S`。捕获和 B 站对齐在该会话中复核通过。

沟通失误已如实保留：B 站对齐后，Codex 擅自新建了「DigestDock 代码复核」，用缩减的 diff/规格做静态复核；之后根据那个会话的结论提交了 `32f571f`。用户指出用错会话后，Codex 将 A「检索与最近」及 B「AI 兜底与回跳」补回原会话，原 Claude 独立读 diff、核哈希并运行 865 项测试与 check，确认 A 无需修正、B 无阻断。没有改历史冒充原会话事前已审。

用户又明确必须用 Claude App，Codex 随后关闭自己打开的 Claude 网页入口。后续若再联系 Claude，只能使用 App 中的原会话；不能仅凭 URL 一致擅自改入口。

Claude 额度受限后，用户要求一次收口，转由 Codex 与 K3 完成修改和复查。最终清单只有三项本轮改动：搜索结果可编辑、回跳提示及时且位于卡片、同视频成功不误报的断言。此后未再向 Claude 发起调用。

之前曾尝试上传 116 个文件的仓库审查副本，被自动审批拒绝，原因是超出当前 diff 复核的外发范围。整包未上传；当时改为仅发送已授权的精确 diff 和唯一规格。该错误新会话不作为最终审查依据。

| 执行职责 | 入口 | 实际模型/档位 | 结果 |
| --- | --- | --- | --- |
| 原会话复核及最终改正清单 | cld | 模型/档位未核实；界面配置不当作执行元数据 | A/B 均无阻断，给出三项最终方案；代码门禁由原会话独立复验 |
| 三项收尾静态复查 | call-kimi | response_model=`k3`；请求 `max`，上游未返回档位 | PASS；未执行本地命令 |
| 重置竞态增量静态复查 | call-kimi | response_model=`k3`；请求 `max`，上游未返回档位 | PASS；未执行本地命令 |

K3 回执：两次退出码均为 0，`ok=true`，`finish_reason=stop`。响应 ID 分别为 `chatcmpl-FTxe5G03ae4ZqDtkDwSF80MZ` 与 `chatcmpl-7yqc4QVQVNQdADePN39UjE9D`。本地测试由 Codex 执行，不能说成 K3 自己跑过。

## 5. 真实 Chrome 验收状态（2026-09-10 实测更新）

用户明确提供无键盘/语音争用的操作窗口后，在真实 Chrome 扩展通道与原生侧栏执行。以下“通过”指表内实际样本与步骤，不代表所有网站状态穷举，也不替代用户验收。

| 编号 | 真机结果 | 实际证据 |
| --- | --- | --- |
| T1 | 通过（YouTube / B 站） | 有字幕任务后按 N，出现真实保存 toast；视频 paused=false 且时间继续推进。后台读到新金句 thought=""、thoughtAt=null。广告/字幕未就绪时出现“请先打开侧栏字幕”，未冒充保存成功。 |
| T2 | 通过（YouTube / B 站） | toast 可见时第二次 N，原位 textarea 获得焦点，paused=true；超过十秒后仍存在、仍聚焦。 |
| T3 | 通过（YouTube / B 站） | 分别保存“DDK真机青柠…”与“DDK真机海盐…”；后台确认原文字段和非空 thoughtAt，输入框关闭且暂停。补测通过 Chrome 原生 CDP 组合输入事件：组合期 Enter 未提交，组合结束后 Enter 正常保存；不是声称已测用户语音输入法本身。 |
| T4 | 通过（YouTube / B 站） | 输入取消草稿后 Esc，toast 关闭；两平台新金句均仍在，thought=""、thoughtAt=null，草稿未保存；先前笔记未改。 |
| T5 | 通过 | 在 YouTube、当前视频筛选下搜索“海盐”，搜出 B 站想法；在 B 站、全部笔记一侧搜索“青柠”，搜出 YouTube 想法。真实截图确认想法在上且更醒目，原句与冻结窗口在下。清空搜索分别恢复原来的当前/全部侧。 |
| T6 | 通过 | 搜索仅在触发原句中的“脑海里先模拟”，找到想法文本不含该词的青柠笔记。 |
| T7 | **通过（有效 API 复测）** | 零结果未点击：provider=0、fetch=0、写操作=[]、完整库快照一致。右侧实例 DeepSeek 实际返回 HTTP 200，正常 9 条库全部进入请求，返回 4 个现有 ID；不显示截断提示。容量场景只送最近 8 条、界面明确提示，provider=1、fetch=1、零写入、库不变。受控注入无效 ID、改写对象、重复及超过 5 条的返回，真实 UI 只显示 5 个现有笔记 ID，异常内容未展示，零写入。详见 §9；先前 401 已解除。 |
| T8 | 通过 | 当前视频默认选中且仍按原视频列表显示。全部笔记显示“最近”，以浏览器 America/Los_Angeles 自然日显示 2026-09-10、09-08、09-06，顺序倒排、无空日；后两天使用明确 ID 的历史日期测试夹具。空想法正常呈现“补写想法”，无置顶、待确认计数或笔记缺失标记。 |
| T9 | 通过 | 最近给空想法笔记补写“DDK真机紫薯…”，保存后立即搜到；搜索结果再次编辑并清空，当前结果立即消失。后台确认 thought="" 时 thoughtAt=null、createdAt 不变、无确认/已读/待办字段。最终 9 条测试记录全部满足不变式。 |
| T10 | 通过（标准回跳及故障注入） | 搜索/最近的当前视频跳转分别读到 YouTube 1573.01 秒（目标 1572）与 B 站 42 秒（目标 42），播放器随后就绪。两方向跨平台新标签的真实 getNotePlaybackState 响应分别为 ready=true、1572.251157 秒、routeKey=youtube:8ow6dRDuIEY，以及 ready=true、42 秒、routeKey=bilibili:BV1zBtg6NEjF:p1。真实侧栏注入一次 tabs.create 拒绝后，卡片内可见“没能打开这个视频”和冻结材料；一次失败后恢复原方法，再点可成功回跳。没有声称测试了真实已删除/无权限视频。 |

补测 toast 归属：等待 YouTube toast 自然消失后再按 N，库从 7 条变为 8 条；新 toast 内再次 N、Enter 只写入新 ID，原来 7 条逐字节不变。对应 `note_b937a9b1-2aed-4d82-b377-7e562ad6dc15` 保持空想法，新增 `note_44b40dfa-4e0b-4c40-856f-ef243bc07bd9` 保存“DDK过期归属：只写入这次新建的笔记。”。

### 探针与范围

- 实际包装 background 的 requestAiCompletion，包装函数仍调用原实现；fetch 第二层计数在真实调用中同为 1，排除探针未接上造成假零。
- 整库最终快照覆盖所有 `ytd_note` 前缀键（包含 `ytd_note_index`），固定对象键序；另记录所有 local.set/remove/clear 调用，不只比最终状态。
- 初次临时快照前缀较窄，只含 `ytd_notes`，后来发现缺少单数索引键，已补正并重跑“不点 / 点击 / 含英文夹具不点”三个场景。上表只采用补正后的完整库证据。
- T7 的英文夹具 `note_DDK_QA_english` 在零请求实测后仍 translatedText=""。现有保存金句的 AI 清理也返回认证失败；保留原始材料的既有降级继续工作，本轮没有改翻译、字幕或清理逻辑。
- 真实截图检查了搜索卡片层级与故障反馈位置；没有发现本轮范围内必须改代码的渲染问题。未做移动端、B 站多 P 真机穷举、真实语音输入法测试，也未把这些写成通过。
- 回跳故障通过真实侧栏运行时的一次性故障注入验证，不是修改生产源码。曾在后台环境准备同名 API 注入，确认实际调用属于侧栏后，在触发前恢复后台方法，再对正确侧栏注入。所有临时运行时探针已随停用测试实例结束。

### 浏览器与测试数据收尾

- 旧实例 `jfjiohpdeohjiopbeapdjmaelkiejcen`：来自受保护旧工作树，1.4.8，已恢复开启；没有修改工作树文件或其笔记数据。
- 测试实例 `lfcndjcppebmjgalcfkmmgohkelakpfk`：来源根目录，2.0.0，已恢复停用；没有改凭据。没有迁移两个实例之间的数据。
- 本轮专用及回跳产生的 5 个视频标签（4549、4552、4555、4558、4565，完整 Chrome ID 前缀为 107911）均已关闭；本轮后台与侧栏 DevTools 已关闭。
- 测试实例里保留 **9 条本轮创建的数据**，包括 6 条真实捕获、2 条历史日期夹具、1 条未翻译英文夹具。后台确认全部 ID 属于本轮。保留供 T7 继续验收，未执行整库清除或删除用户数据。
- 9 个 ID：`note_cecc248b-3b0f-44e3-8051-ba57311b4f46`、`note_cdb1d49d-5b5a-4c5a-820e-0ac2239ff70f`、`note_7cd5af7c-9e42-477e-90eb-852f4b848cc9`、`note_923e1f15-3d71-4c87-ad5c-01a6dfeb5f64`、`note_b937a9b1-2aed-4d82-b377-7e562ad6dc15`、`note_44b40dfa-4e0b-4c40-856f-ef243bc07bd9`、`note_DDK_QA_day_2`、`note_DDK_QA_day_4`、`note_DDK_QA_english`。
- 一次原生窗口索引误判打开了 Gemini 入口，随即关闭，没有发送提示。后续窗口切换使用最新完整 AX 状态定位；没有使用 Claude App 或网页，也没有消耗 Claude 额度。

当轮历史阻塞：现有 AI 服务密钥认证失败。此阻塞后来已在 §9 的有效配置复测中解除。

## 6. 本轮发现但没有修改的项目

- 用户指定的 `background.js` 约 5143 行恒假分支。
- `ytd_notes_<mediaKey>` 与 `ytd_notes_schema` 的键名碰撞。
- 两个平台的想法输入状态机存在重复；抽取共享模块会涉及 manifest，不在本轮。
- 搜索没有防抖；本轮保持每次输入一次本地读取。

故意保留，不当缺陷修：清空搜索/保存想法后的 `translateMissing:false`；Esc 后视频保持暂停；AI 输出 JSON 解析失败时降级为零候选。

未做标签、快捷键设置、想法 AI 改写、模糊/向量搜索。未改字幕获取链路、概览、导出内容契约、捕获锚点与冻结窗口。没有 push、merge 或 release；没有宣称用户验收通过。

## 7. 当前交付边界

1. T1–T10 标准场景与本轮要求的自动化门禁均已完成。API 配置问题已解除，见 §9。
2. 保持本地分支 `codex/thought-layer-phase2`，HEAD `5a589d5`。没有 push、merge、release；这些需要后续明确授权。
3. 此次复测未改生产源码或测试源码，因此未重复执行相同门禁；最后已通过的门禁仍是 871/871 与 47 个白名单文件，tracked 工作区干净。
4. 右侧根目录实例是本轮测试版；不要把左侧旧工作树实例或 UI 的同名 2.0.0 当作相同代码/数据。测试笔记仍保留 9 条，清理时只核对上文明确的本轮 ID，不清用户库。

本地日志位于 `/private/tmp/`：`digestdock-phase2-closing-full.log`、`digestdock-phase2-closing-check.log`、`digestdock-phase2-closing-test.log`、`digestdock-phase2-closing-red.log`、`digestdock-phase2-reset-red.log`、`digestdock-phase2-overview-fixture-check.log`、`digestdock-k3-final-review.json`、`digestdock-k3-reset-review.json`。日志未提交；此文件保留核心结果以免临时目录清理后丢失状态。

## 8. API 配置复测补记

用户更新密钥后再次实测。明确在 `lfcndjcppebmjgalcfkmmgohkelakpfk` 根目录实例执行；其实际服务商为 DeepSeek、模型为 `deepseek-v4-flash`。点击零结果中的“让 AI 找找”，服务端返回 HTTP 401，认证错误分类为 true；余额/额度、模型、429 限流分类均为 false。不能认定新凭据已生效，也不能据此推测用户具体哪里填错。

每次独立点击的证据：provider=1、fetch=1、local 写操作=[]、包含索引的库快照 bytesEqual=true。正常大小的测试库共 9 条，provider messages 中 9 个现有 ID 全部出现，补到了正常库不应偷偷截断的真实输入覆盖证据；成功候选展示仍未通过。

此次复测准备时观察到旧实例已开启、根目录实例停用，已切换为旧实例停用、根目录实例开启。未修改任何密钥或源码，未新增测试笔记。准备收尾时 Mac 已锁定，无法关闭测试视频/调试窗口或打开正确设置入口：待用户解锁后完成。尚存测试标签 ID `1079114629`；后台临时探针 `__DDK_T7` 仍可能存活，重新加载根目录实例即可结束。不要把之前一轮“已恢复原状态”的记录当作此刻状态。

## 9. 右侧实例有效 API 复测完成

用户再次在右侧根目录实例保存配置后，已成功复测。worker 身份实际为 `lfcndjcppebmjgalcfkmmgohkelakpfk`，DeepSeek 响应 HTTP 200。没有代填、复制、修改或输出密钥。

- **未点击**：provider=0、fetch=0、写操作=[]、完整库快照一致。
- **真实成功返回**：正常库 9 条的 ID 全部出现于 provider 输入；返回 `note_cecc248b-3b0f-44e3-8051-ba57311b4f46`、`note_DDK_QA_day_2`、`note_DDK_QA_day_4`、`note_DDK_QA_english`，4 个全是既存 ID，UI 显示“4 条结果”。searchedCount=9、totalCount=9、truncated=false，未显示“只检索了最近”。完整库 bytesEqual=true、写操作=[]。此正常库阶段发生两次实际按钮点击，共 2 次 provider 调用；通用 fetch 探针记录 3 次，因此不把通用网络计数冒充仅有 provider 请求的计数。
- **超量输入**：临时把既有、可识别的历史夹具 `note_DDK_QA_day_4` 加长到实际确认的 34000 字符（不改 createdAt）。只送最新 8/9 条，返回 3 个现有 ID；UI 实际显示“3 条结果 · 只检索了最近 8 条”。provider=1、fetch=1、HTTP 200、写操作=[]、bytesEqual=true，没有多轮检索。首次准备夹具时原生键盘输入未产生预期中文长度，现场读回发现 length=0，该次不计作容量验收；改用 ASCII 夹具并确认 length=34000 后重新执行才计通过。
- **异常返回/上限**：在真实 API 成功响应后，一次性注入包含不存在 ID、带改写字段的对象、重复 ID 和超过 5 个有效 ID 的测试返回。这是明确的故障注入，不冒称真实模型自然返回异常内容。真实 UI 显示“5 条结果”，不展示 `MISSING_ID` 或 `AI_REWRITE_MUST_NOT_APPEAR`；后台过滤后的 5 个 ID 全都对应本地笔记。provider=1、fetch=1、HTTP 200、零写操作、bytesEqual=true。
- **恢复**：临时加长的夹具已恢复空想法/null thoughtAt；最终 `originalLibraryRestored=true`，整个库与本轮复测准备前的规范键序快照一致。仍是原来的 9 条测试记录，没有新增笔记。
- **收尾**：已恢复被包装的 provider、fetch、storage 方法与候选处理函数，删除临时探针对象，关闭本轮 worker DevTools；清空测试查询，真实 UI 恢复“当前视频”笔记列表。用户原有视频标签未关闭。本次没有切换扩展启用状态，右侧实例仍用于此次成功验收。

本轮没有产生新的代码提交，也没有向 Claude 或 K3 发起代码复查调用：没有源码改动需要重新复查。此前所有提交哈希、871/871 门禁与范围围栏保持不变。
