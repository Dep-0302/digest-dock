# DigestDock 本次迭代详细执行方案

> 实施状态（2026-08-23）：主体功能、六个官网图标、五个可用 Provider、腾讯安全禁用项、Key Map、按来源笔记、三语言导出与显式有界补译已进入当前未提交工作树。自动测试和发布检查通过后仍需真实 Chrome 扩展验收。腾讯 `hunyuan-translation-lite` 暂不可选择：官方确认专用翻译 API 与通用 OpenAI-compatible Endpoint 分别存在，但尚未确认该翻译模型可使用当前产品的单 Key 兼容调用方式；禁止猜测配置。

状态：待执行，仅完成方案设计
基线：DigestDock `1.4.2`，`main` 提交 `424ce29`
适用范围：侧栏平台差异、笔记标题多语言、按视频来源分类、笔记与字幕导出、设置页信息架构、多 AI Provider 预设与官方品牌图标，以及相关数据迁移、测试与文档
不包含：本文件创建之外的代码修改、真实第三方 API 调用、提交、推送、发布

## 1. 目标

在保留现有 1.4.2 UI、字幕卡片、概览卡片、笔记列表、时间码、Supadata 单次授权和本地数据能力的前提下，完成以下迭代：

1. YouTube 继续使用完整的 `原文 / 中文 / 双语` 模式。
2. B 站字幕与概览不显示无意义的语言胶囊，但笔记仍保留完整语言模式。
3. 笔记语言模式同时控制笔记正文和视频标题。
4. 设置页通过内置 Provider 预设支持多个 AI 服务；用户只选择服务并填写 API Key。
5. 删除用户不需要理解的“字幕服务”一级导航，并在 Provider 预设完成后删除“本地改造”。
6. 保持 Supadata 的 YouTube 专用、逐视频授权和无自动调用边界。
7. “全部笔记”按照视频出处分组，同一视频的笔记放入同一完整容器，并只按时间码升序排列。
8. 增加当前视频与全部笔记的笔记导出，包含标题、频道、网址、视频简介和已保存笔记，不附带整部字幕，并遵循当前语言模式。
9. 现有字幕下载按 `原文 / 中文 / 双语` 输出当前完整字幕。
10. Provider 列表与选中状态显示来自各自官网的准确品牌图标。

## 2. 已确认的产品决策

### 2.1 平台与标签页语言矩阵

| 平台 | 字幕 | 概览 | 笔记 |
|---|---|---|---|
| YouTube | 显示 `原文 / 中文 / 双语` | 显示 `原文 / 中文 / 双语` | 显示 `原文 / 中文 / 双语` |
| B 站 | 隐藏整个语言控件 | 隐藏整个语言控件 | 显示 `原文 / 中文 / 双语` |

补充规则：

- B 站字幕与概览是否隐藏控件，只由 `platform === "bilibili"` 决定，不根据 `zh-CN`、`zh-TW` 等语言标签动态改变布局。
- B 站笔记必须保留语言控件，因为“全部笔记”会聚合 YouTube 与 B 站内容，并可能包含多种原始语言。
- B 站不新增英文字幕轨、多语言字幕轨或 Supadata 路径。
- YouTube 已有三种语言模式保持不变，不做重复重构。

### 2.2 笔记视频标题

| 笔记模式 | 标题显示 |
|---|---|
| 原文 | 原始视频标题 |
| 中文 | 中文视频标题；没有有效译文时回退原始标题 |
| 双语 | 原始标题 + 中文标题；两者相同则只显示一次 |

补充规则：

- 原标题已经是中文时直接复用，不发起翻译。
- 同一媒体身份下的多条笔记共用一份标题译文，不逐条重复请求。
- “当前视频”和“全部笔记”都遵循相同语言语义。
- 标题翻译失败不能阻塞笔记正文、播放、复制、链接或删除操作。

### 2.3 设置页最终导航

设置：

- 服务连接
- 笔记与备份
- 本地数据

其他：

- 隐私说明

删除：

- 左侧“字幕服务”入口；Supadata 卡片仍留在“服务连接”中。
- “本地改造”入口与内容；必须在多 Provider 预设完成后删除。

### 2.4 Provider 设置体验

- 用户只选择 Provider 并填写该 Provider 的 API Key。
- Endpoint、模型名、请求格式、认证方式和 Provider 专用字段由插件预设。
- 不向普通用户提供 Base URL、Endpoint 或模型文本输入框。
- 不同 Provider 的 Key 分别保存；切换后再切回来时仍可继续使用原 Key。
- 选择、保存或切换 Provider 本身不得发起网络请求。
- 不允许在 Provider 失败时静默切换到另一家服务。

### 2.5 Provider 官方图标

- 六个 Provider 在下拉列表和选中后的服务卡中都显示对应图标。
- 图标必须从各自官方网站、官方品牌资源页或官方开发者文档取得，禁止从搜索结果缩略图、非官方图标站或第三方仓库直接复制。
- 下载后作为本地扩展资产打包，禁止运行时远程加载或热链。
- 保留官方形状、比例、留白与颜色，不为了统一风格随意重绘、变色或套用自制字母图标。
- 每个资产记录来源 URL、获取日期、文件格式和允许的使用方式；如果官方没有公开可复用资源，必须先停下确认，不能自行仿制后声称是官方图标。
- 统一视觉尺寸只通过外部容器完成，建议显示框为 `24 × 24px`，内部使用 `object-fit: contain`。

官方来源在实施时重新确认，起始核对域名如下：

| Provider | 官方来源起点 |
|---|---|
| DeepSeek | `deepseek.com`、`platform.deepseek.com` |
| 智谱 GLM | `bigmodel.cn`、智谱官方站点 |
| 阿里云百炼 Qwen | `aliyun.com`、阿里云百炼官方产品与文档页 |
| 腾讯混元 | `cloud.tencent.com/product/hunyuan` 及腾讯云官方文档 |
| SiliconFlow | `siliconflow.cn` 及官方开发者文档 |
| Fireworks | `fireworks.ai` 及官方开发者文档 |

### 2.6 笔记分类与排序

- 分类键使用稳定媒体身份，不使用可能重复或变化的视频标题：YouTube 使用 `videoId/mediaKey`，B 站使用当前分 P 的 `bvid + cid/mediaKey`。
- 同一视频的全部笔记放在一个完整的来源容器中，容器头部显示标题、频道、平台、链接和笔记数量。
- 来源容器内部的笔记只按 `timestampSeconds` 从小到大排列，不按 `createdAt` 或记录先后排列。
- 时间码相同的笔记使用稳定 `id` 作为最终 tie-breaker，保证重复渲染顺序一致。
- “当前视频”只显示一个来源容器；“全部笔记”显示多个来源容器。
- 全部笔记的来源容器默认按标题进行本地化排序，再以 `mediaKey` 作为稳定 tie-breaker；不按最近记录时间排序。

### 2.7 笔记导出

- 笔记区增加明确的“导出笔记”入口；当前视频可直接导出，“全部笔记”可多选一个或多个视频来源，也可导出全部来源。
- 默认导出 UTF-8 TXT；当前视频生成一个来源段，多个视频按来源分隔。
- 每个来源必须包含：标题、频道、网址、视频简介、按时间码排列的已保存笔记；不得附带整部字幕。
- 出现缺口时固定提供“补充导出 / 直接导出 / 导出原文 / 放弃导出”。补充导出只读取用户明确打开的视频页元数据并按确认补译；直接导出使用缺失标记；原文导出零 AI；放弃导出不生成文件。
- 补充引导重新检查前必须读取持久化来源并确认对应缺口已经消失；页面资料仍未就绪时保留在当前引导，给出刷新提示，不得回到相同预检形成循环。
- YouTube 补充资料先读取 MAIN world 播放器的 `videoDetails`，只有标题、频道或简介完整状态不足时才查询内容脚本。播放器与内容脚本返回的 `videoId` 都必须等于目标网址中的 `v`；加载中的新标签使用 `pendingUrl` 绑定目标。错视频返回 `PAGE_CONTEXT_CHANGED` 并等待重试，旧脚本缺少 `videoId` 返回 `PAGE_REFRESH_REQUIRED`，所有失败分支均不得写入来源资料或触发字幕、Supadata、AI。
- 导出语言严格跟随当前笔记模式：
  - 原文：标题、简介和笔记使用原始语言。
  - 中文：使用有效中文版本；原文已经是中文时直接复用。
  - 双语：原文与中文成对输出；相同内容只输出一次。
- 导出不得包含 API Key、内部存储键、调试信息或 Provider 原始响应。
- 所有数据必须来自已加载或已保存的本地资料；缺少必要资料时先显示导出预检结果，不得静默调用 Supadata 或 AI Provider。

### 2.8 字幕下载

- 保留现有字幕下载入口，但导出内容不再固定为 `currentTranscriptText` 原文。
- 原文模式下载完整原字幕；中文模式下载完整中文翻译；双语模式按同一时间码成对输出。
- B 站字幕与概览虽然不显示语言控件，但字幕下载按该平台固定中文内容导出。
- 下载文件继续包含标题、频道、网址、视频简介和字幕正文。
- 文件名增加语言后缀：`-original`、`-zh`、`-bilingual`。
- 中文或双语字幕尚未完整翻译时，必须先提示缺失段数和可能产生的 Provider 调用；未经明确确认不得批量补译。

## 3. 当前主线基线

### 3.1 已存在，无需重复实施

- `sidepanel.html` 已有字幕、概览和笔记三组语言控件。
- `sidepanel.js` 已实现笔记正文的原文、中文和双语渲染。
- `sidepanel.js` 与 `background.js` 已实现按需翻译、失败回退、调用预算和本地持久化。
- `sidepanel.js` 已有字幕 TXT 下载入口，当前会导出标题、频道、网址、视频简介和原字幕。
- `options.html` 已将 DeepSeek 与 Supadata 放入服务卡片区域。
- Supadata 已要求严格的逐视频授权，B 站不会使用 Supadata。
- 设置页固定工作区、内部滚动、导航活动态、底部保存栏、本地数据、笔记备份已经存在。

### 3.2 当前仍需改变

- B 站目前只隐藏部分语言按钮，仍留下单独的“原文”或“中文”胶囊。
- B 站当前还会隐藏笔记模式中的“原文”和“双语”，与聚合笔记需求冲突。
- 笔记卡片的视频标题固定使用 `note.videoTitle`，不随语言模式改变。
- “全部笔记”仍是单条平铺列表，并按存储中的记录顺序显示，尚未按媒体来源分组和时间码排序。
- 当前已具备面向阅读使用的笔记 TXT 导出；设置页 JSON 仍只承担恢复备份格式。
- 字幕下载固定输出原字幕，尚未根据语言模式输出中文或双语字幕。
- Provider 设置没有官方品牌图标资产与来源记录。
- `settings.js` 固定为 DeepSeek；`background.js` 仍包含 DeepSeek 专用请求字段与错误文案。
- 设置页仍有“字幕服务”导航和“本地改造”完整内容。
- `DESIGN.md` 与 `UI-REPLACEMENT-PLAN.md` 仍记录旧导航结构。

## 4. 不得改变的边界

1. 不改变 Supadata 的逐视频授权协议；保存 Key 不代表授权。
2. 不把 B 站接入 Supadata。
3. 不自动使用第三方 AI Provider，不自动测试用户 Key。
4. 不删除或重置已有 Key、笔记、翻译、摘要或备份数据。
5. 不改变 B 站标准 BV 页面与当前分 P 的身份模型。
6. 不放宽 API Key 的日志、截图、备份和源码保护规则。
7. 不恢复用户自定义 Base URL 或任意 OpenAI-compatible Endpoint。
8. 不在本次迭代中提交、推送或发布；这些需要单独授权。
9. 不覆盖并行线程或其他分支的未提交改动；进入实施前必须重新检查工作树。
10. 不在导出过程中静默调用 Supadata 或 AI Provider；任何可能消耗额度的补齐操作必须先明确告知范围并取得当次确认。
11. 不把笔记导出与现有 JSON 备份混为一体：TXT 是阅读交付物，JSON 继续作为严格恢复格式。
12. 不运行时加载 Provider 图标，不增加外部图片域名权限。

## 5. 技术设计

## 5.1 B 站语言控件

### 修改文件

- `sidepanel.js`
- `sidepanel.html`（仅在需要补充语义或测试钩子时）
- `sidepanel.css`（仅在控件隐藏后需要修正顶部间距时）
- `tests/translation.test.js`

### 实施方式

1. 新增单一平台判断，例如 `currentPlatformIsBilibili()`，只读取 `currentMediaRef.platform`。
2. `applyMediaLanguageDefaults()` 只负责设置默认模式，不再通过逐个按钮的 `hidden` 状态模拟单按钮控件：
   - B 站字幕默认 `original`。
   - B 站概览默认 `zh`。
   - B 站笔记默认 `zh`，但保留三按钮供用户切换。
3. `updateHeaderLanguageControlsVisibility()` 按平台和活动标签页决定整个控件容器是否显示：
   - YouTube：按活动标签显示相应控件。
   - B 站字幕、概览：对应容器始终隐藏。
   - B 站笔记：在笔记标签页显示 `notesModeControl`。
4. 移除 B 站逐个隐藏 `.transcript-mode-btn`、`.overview-mode-btn`、`.notes-mode-btn` 的逻辑，避免离开 B 站后按钮状态残留。
5. B 站字幕来源标签不再拼接“原文（zh-cn）”，目标格式为 `B 站视频字幕（zh-cn）`；YouTube 来源标签保持现状。
6. 切换平台、视频和标签页后必须重新计算容器状态，不能依赖上一个视频的 DOM 状态。

### 验收条件

- B 站字幕和概览顶部没有单独的“原文”或“中文”胶囊。
- B 站笔记顶部完整显示三种模式。
- 从 B 站切换到 YouTube 后，YouTube 三种模式全部恢复。
- 从 YouTube 切回 B 站后，不出现旧的活动态或残留按钮。

## 5.2 笔记标题多语言

### 修改文件

- `sidepanel.js`
- `sidepanel.css`
- `background.js`
- `prompts/translation.md` 或新增专用标题翻译段落
- `notes-backup.js`
- `tests/translation.test.js`
- `tests/notes-backup.test.js`

### 数据字段

保留现有 `videoTitle` 作为原始标题，新增可选字段：

```js
{
  videoTitle: "Original video title",
  videoTitleZh: "中文视频标题",
  videoTitleZhValidated: true,
  videoTitleZhValidationVersion: 1
}
```

约束：

- `videoTitleZh` 最大长度与 `videoTitle` 相同，建议继续限制为 500 个字符。
- 没有译文时字段为空，不用原文伪装成已验证译文。
- 原标题确认是中文时，可将显示层直接复用原文；不必强制写入重复字段。
- 标题译文必须绑定稳定媒体身份：YouTube 使用 `videoId/mediaKey`，B 站使用 `bvid + cid/mediaKey`。

### 翻译与去重

1. 在 `ensureNotesChinese()` 中先按媒体身份收集缺失标题译文的唯一视频，而不是按笔记条数处理。
2. 同一批 `translateNotes` 任务中携带去重后的标题列表，或新增一个受同一调用预算约束的 `translateNoteTitles` 子任务。
3. 推荐在现有笔记翻译请求中同时返回：

```json
{
  "notes": [{ "id": "note-id", "textZh": "中文笔记" }],
  "titles": [{ "mediaKey": "stable-key", "titleZh": "中文标题" }]
}
```

4. 后台必须独立验证笔记正文和标题：标题失败时仍可保存有效正文，正文失败时仍可保存有效标题。
5. 使用现有笔记写入队列统一提交，避免翻译、保存、删除和导入同时写 `ytd_notes`。
6. 成功后，将同一 `mediaKey` 下所有现存笔记补齐相同标题译文。
7. 导入旧备份或读取旧笔记时，缺少新字段应自然回退原始标题。

### 渲染

在 `sidepanel.js` 增加：

- `noteOriginalVideoTitle(note)`
- `noteChineseVideoTitle(note)`
- `renderNoteVideoTitle(note, mode)`

显示规则：

- 原文：单行原始标题。
- 中文：单行中文标题；无译文时显示原始标题。
- 双语：上下两行显示，原文为主、中文为辅；内容相同只显示一行。
- 长标题继续截断，必须提供 `title` 或无障碍名称查看完整内容。
- 现有播放、复制正文、复制链接和更多菜单不改变。

### 备份迁移

建议将 `notes-backup.js` 的备份 schema 从 2 升到 3：

- schema 3 导出标题译文及其验证信息。
- schema 1、2 继续可导入，缺失字段默认空。
- 严格校验长度、类型和控制字符。
- 导入时不得信任备份中的 URL；继续由媒体身份重建链接。
- 冲突合并时，已验证标题译文可补齐空值，不覆盖不同的非空已验证译文。

### 验收条件

- “全部笔记”中，同一英文视频的多条笔记只产生一个标题翻译结果。
- 三种模式下正文和标题同步切换。
- 中文原标题不触发 Provider 调用。
- 标题翻译失败时原文仍可见，笔记其他操作可用。
- schema 1、2、3 备份均能按兼容规则处理。

## 5.3 Provider 预设与适配层

### 初始 Provider 清单

| Provider ID | 服务与推荐模型 | 能力说明 |
|---|---|---|
| `deepseek` | DeepSeek V4 Flash | 通用：概览、讲解、字幕翻译、笔记与标题 |
| `zhipu` | 智谱 GLM-4.7-Flash | 通用候选，需验证结构化输出与长上下文 |
| `dashscope` | 阿里云百炼 Qwen3.7-Flash | 通用候选，需验证区域 Endpoint 与限速 |
| `tencent-hymt` | 腾讯混元翻译（原提案 Hy-MT2-Lite；官方调用名 `hunyuan-translation-lite`） | 翻译专用，不应伪装成完整通用 Provider；兼容调用未核实前保持禁用 |
| `siliconflow` | SiliconFlow Qwen3-8B | OpenAI-compatible 候选，需实测中文摘要质量和限流 |
| `fireworks` | Fireworks DeepSeek V4 Flash | 通用候选，需验证模型 ID 与 Serverless 行为 |

模型名、Endpoint、价格和可用区域会变化。实施开始时必须以各家官方文档重新确认；未经确认的值不得写入正式预设或发布文档。

每一项还必须配置本地官方图标资产，例如：

```text
icons/providers/deepseek.svg
icons/providers/zhipu.svg
icons/providers/dashscope-qwen.svg
icons/providers/tencent-hunyuan.svg
icons/providers/siliconflow.svg
icons/providers/fireworks.svg
icons/providers/PROVENANCE.md
```

`PROVENANCE.md` 对每个文件记录官方来源 URL、获取日期、原始文件名、是否做过仅限尺寸/格式的转换，以及品牌使用限制。图标格式优先使用官方 SVG；官方仅提供位图时保留高分辨率透明 PNG，不用 ImageGen 或手工描摹替代。

### Provider 注册表

建议新增 `ai-providers.js`，集中定义：

```js
{
  id,
  displayName,
  model,
  endpoint,
  hostPermission,
  authType,
  capabilities,
  supportsJsonMode,
  supportsTemperature,
  buildRequest,
  parseResponse,
  normalizeError,
  apiKeyHelpUrl,
  iconPath,
  iconAlt,
  brandSourceUrl
}
```

加载顺序：

- `background.js`：先 `importScripts("ai-providers.js")`，再加载使用注册表的设置逻辑。
- `options.html`、`sidepanel.html`：在 `settings.js` 前加载 `ai-providers.js`。
- `scripts/check-release.sh` 与打包清单纳入新文件。
- 发布检查验证六个 `iconPath` 均指向已打包的本地文件，并验证 `PROVENANCE.md` 存在；不得检查或输出任何 Key。

### 请求层重构

把 `background.js` 当前的 `requestAiCompletion()` 拆成：

1. 通用的超时、响应体大小、Abort、状态码和调用预算层。
2. Provider Adapter 的请求构造层。
3. Provider Adapter 的响应与错误归一化层。
4. 产品功能已有的 JSON、字幕、概览、笔记和标题语义校验层。

必须隔离的差异：

- `thinking: { type: "disabled" }` 只发给明确支持该字段的 Provider。
- `response_format` 只发给支持 JSON mode 的 Provider；其余 Provider 依靠严格提示词和现有解析器。
- 认证 Header、模型字段、最大输出字段和 Endpoint 由 Adapter 决定。
- Provider 原始错误统一映射为 `RATE_LIMITED`、`INVALID_KEY`、`PROVIDER_TIMEOUT`、`PROVIDER_UNAVAILABLE`、`OUTPUT_TRUNCATED` 等产品错误。
- 用户可见错误使用当前 Provider 名称，不再硬编码“DeepSeek”。
- Provider 不支持某项能力时，应在 UI 阻止操作并清楚说明，不能自动换用其他 Provider。

### Hy-MT2-Lite 决策门

Hy-MT2-Lite 是翻译专用服务，不能直接替代概览、讲解和笔记润色。执行前必须二选一并由用户确认：

1. 推荐：通用 Provider 下拉只列完整能力服务；另设可选“翻译服务”下拉，Hy-MT2-Lite 仅用于翻译。
2. 单一下拉：允许选择 Hy-MT2-Lite，但明确禁用概览、讲解和笔记润色；不做隐式回退。

在此决策完成前，不把 Hy-MT2-Lite 当作通用 Provider 发布。

## 5.4 设置存储与迁移

### 目标结构

```js
{
  provider: "deepseek",
  aiApiKeys: {
    deepseek: "",
    zhipu: "",
    dashscope: "",
    "tencent-hymt": "",
    siliconflow: "",
    fireworks: ""
  },
  supadataApiKey: ""
}
```

规则：

- Endpoint 和模型不写入用户设置，始终从注册表派生。
- 将旧的 `aiApiKey` 安全迁移为 `aiApiKeys.deepseek`，不得清空现有 DeepSeek Key。
- 继续忽略已经废弃的自定义 `aiBaseUrl`、`aiModel` 和 `provider: "custom"`。
- 迁移必须幂等；多次打开设置页不会重复改写或丢 Key。
- 不在日志、测试快照、备份文件或错误详情中输出 Key。
- `checkConfig` 返回当前 Provider、是否配置 Key、能力信息和 Supadata 状态，不返回密钥正文。

### 修改文件

- 新增 `ai-providers.js`
- `settings.js`
- `options.js`
- `background.js`
- `sidepanel.js`
- `sidepanel.html`
- `options.html`
- `manifest.json`
- `scripts/check-release.sh`
- `scripts/package-extension.sh`
- `tests/settings.test.js`
- 新增 `tests/ai-providers.test.js`

## 5.5 设置页 UI

### 服务连接布局

AI 服务卡目标结构：

1. 带官方图标的 `AI 服务商` 下拉框。
2. 当前推荐模型只读说明。
3. 能力标签，如“概览 / 讲解 / 翻译 / 笔记”。
4. 当前 Provider 的 API Key 密码输入框与显示/隐藏按钮。
5. Provider 官方创建 Key 链接。
6. 配置状态与隐私提示。

交互要求：

- 切换 Provider 时只更换当前输入框绑定和说明，不发请求。
- 输入 Key 后切换到其他 Provider，再切回来时输入仍在表单状态中。
- 保存时一次性写入完整 Key Map。
- 未保存提示、键盘操作、焦点、窄屏布局和底部保存栏继续工作。
- 下拉必须有可访问标签；能力限制既有文字也有语义状态，不能只靠颜色。
- 如果要求下拉选项内也显示图标，不使用无法可靠渲染图片的原生 `<select>`；实现符合 ARIA combobox/listbox 规范的自定义控件，同时保留键盘方向键、Enter、Escape、焦点返回和屏幕阅读器名称。
- 选中后，服务卡头像立即切换为该 Provider 的官方本地图标；图标加载失败时显示中性占位并报告资源错误，不能显示错误 Provider 的图标。

### Supadata

- 删除左侧 `href="#section-transcript"` 的“字幕服务”导航项。
- Supadata 卡片保留在 `section-services` 的服务网格中。
- 标题或副标题可改为“YouTube 备用字幕服务”，但保留 Supadata 品牌和官方链接。
- 不修改逐视频授权、限流、失败提示与 B 站隔离逻辑。

### 删除本地改造

Provider 预设通过设置、后台和测试后，再删除：

- `options.html` 的 `section-remix` 与左侧入口。
- `options.js` 的 `localRemix`、提示词、复制状态和剪贴板逻辑。
- `options.css` 的 customization/remix 样式。
- 对应 `tests/options-language.test.js`、`tests/release.test.js` 和 `tests/settings-nav.test.js` 旧断言。
- README 中让用户交给编程 Agent 修改 Provider 的说明。

保留“本地数据”及其危险操作区域。

## 5.6 Provider 官方图标资产

### 获取流程

1. 逐一进入 Provider 官方站点、开发者文档或品牌资源页。
2. 确认页面域名、品牌名称与目标服务一致，避免混用母公司、模型系列和云产品图标。
3. 下载官方提供的 SVG 或透明 PNG，不从网页截图裁切，不从第三方图标库复制。
4. 在 `icons/providers/PROVENANCE.md` 登记来源与使用约束。
5. 只做必要的格式安全检查与画布标准化，不改变 Logo 形状、配色和比例。
6. 使用 SVG 时移除脚本、外链、事件处理器、远程字体和不必要元数据；保留合法的矢量路径和官方色值。
7. 为浅色、深色或单色版本按官方规则选择最适合当前浅色设置页的版本，不自行反色。

### UI 规格

- 下拉项图标容器：`24 × 24px`；服务卡选中图标可使用 `32 × 32px`。
- 图标与文字间距保持 8–10px，六个选项对齐同一视觉轴。
- `alt`/无障碍名称使用 Provider 品牌名，模型名作为旁边文本，不塞入图片替代文本。
- 高对比度和 Windows 强制颜色模式下，品牌文字仍必须可识别，不能只依赖图标。

### 验收条件

- 六个 Provider 选项和选中状态图标一一对应。
- 断网状态下图标仍正常显示。
- 打包文件中不存在远程图片 URL。
- 图标来源记录可从文件追溯到官方页面。

## 5.7 笔记按来源分组

### 修改文件

- `sidepanel.js`
- `sidepanel.html`
- `sidepanel.css`
- `tests/translation.test.js` 或新增 `tests/notes-presentation.test.js`

### 数据处理

新增纯函数：

- `noteMediaGroupKey(note)`：返回稳定 `mediaKey`，并对旧 YouTube 笔记回退 `videoId`。
- `groupNotesBySource(notes)`：按稳定来源分组。
- `sortNotesByTimecode(notes)`：按 `timestampSeconds` 升序，`id` 作为 tie-breaker。
- `sortNoteGroups(groups)`：按当前语言模式下可见标题执行 `localeCompare("zh-CN")`，再按 `mediaKey` 稳定排序。

禁止使用 `createdAt` 决定来源内顺序。`createdAt` 只保留为数据元信息和冲突处理依据。

### UI 结构

每个来源容器建议结构：

```text
来源容器
├─ 标题（遵循原文/中文/双语）
├─ 频道 · 平台 · 笔记数量
├─ 打开视频 / 导出此视频笔记
└─ 笔记列表
   ├─ 00:04  笔记正文与操作
   ├─ 00:47  笔记正文与操作
   └─ 02:34  笔记正文与操作
```

- 来源容器使用一个完整边框与适度底色，不再让每条笔记各自形成厚重悬浮卡。
- 时间码继续承担跳转功能。
- 删除菜单、播放、复制正文和复制链接继续作用于单条笔记。
- 切换语言模式只更新标题与正文，不重新排序时间码，不重新请求已经存在的译文。

### 验收条件

- 同一视频的笔记无论保存先后，都只出现一个来源容器。
- 先保存 02:34、再保存 00:04、再保存 00:47，最终显示顺序必须是 00:04、00:47、02:34。
- 同名但不同 `mediaKey` 的视频不能错误合并。
- B 站不同分 P 使用不同媒体身份，不能错误合并。

## 5.8 导出资料数据层

当前 `digest_<mediaKey>` 缓存保存字幕、标题和频道，但 30 天过期且最多保留 20 个视频；`currentVideoDescription` 目前没有随缓存完整保存。来源资料层仍保留字幕供独立字幕 TXT 使用；笔记 TXT 只依赖标题、频道、网址、简介和已保存笔记。

### 推荐方案：笔记来源资料库

新增独立、按媒体去重的来源资料记录，不把完整字幕重复写入每条笔记：

```js
{
  schemaVersion: 1,
  mediaKey,
  platform,
  canonicalUrl,
  titleOriginal,
  titleZh,
  channelName,
  descriptionOriginal,
  descriptionZh,
  sourceLanguage,
  transcriptOriginal: [{ start, text }],
  transcriptZh: [{ start, text }],
  transcriptTranslationComplete: false,
  updatedAt
}
```

存储键建议与普通摘要缓存隔离，例如 `ytd_note_sources` 或 IndexedDB object store `noteSources`。实施前必须评估 Chrome 当前本地存储配额：

- 如果 100 条笔记可能覆盖大量长视频，优先使用 IndexedDB 按 `mediaKey` 存储来源资料，避免挤占 `chrome.storage.local` 的设置、笔记和摘要空间。
- 不建议为此直接申请宽泛的 `unlimitedStorage`；如确有必要，必须单独说明权限变化并取得用户确认。
- 每份资料只保存当前功能所需文本，不保存 Cookie、页面 HTML、API 响应或临时签名字幕 URL。
- 删除某视频最后一条笔记时，可以保留来源资料到用户执行清理；是否自动回收由存储策略测试决定。

### 写入时机

1. 首次为视频保存笔记且当前字幕已可用时，创建或更新来源资料。
2. 后续笔记只引用 `mediaKey`，不重复存字幕和简介。
3. 标题、简介或字幕翻译成功后更新同一来源资料。
4. 老笔记迁移时，优先从仍存在的 `digest_<mediaKey>` 缓存回填。
5. 老笔记资料缺失且缓存也不存在时，导出预检列出缺失视频；不得为 YouTube 自动调用 Supadata，也不得在后台打开页面抓取。

### 导出预检

导出前构造只读报告：

- 目标范围中的视频数量和笔记数量。
- 每个视频是否具备标题、频道、URL 和简介。
- 当前笔记语言模式是否具备中文标题、简介和已保存笔记。
- 笔记导出需要补译的标题数、简介块和笔记数；完整字幕只在字幕 TXT 预检中计算。
- 资料完全缺失的视频列表。

规则：

- 原文导出不检查整部字幕；网址或其他元数据缺失时进入四动作预检，直接导出必须写入缺失标记。
- 中文/双语导出存在缺失翻译时，不自动调用 Provider；先显示范围与预计调用批次，由用户明确选择“补充导出 / 直接导出 / 导出原文 / 放弃导出”。
- “全部笔记”批量补译必须有总量上限、进度、取消和可恢复状态，且沿用 Provider 调用预算；不允许一次点击无限循环处理全部库。
- 无法补齐的项目不能静默省略；应明确标为缺失并由用户选择取消或导出带缺失标记的文件。

## 5.9 笔记导出

### 入口

- 笔记页顶部增加“导出笔记”按钮。
- 当前视频状态显示 `导出当前视频`；全部笔记状态显示 `选择视频导出…` 与 `导出全部视频`。
- 每个来源容器也可提供“导出此视频”快捷入口，但不得替代顶部两个命令。

### 输出格式

默认生成 `.txt` 纯文本：

```text
标题：视频标题
频道：频道名
网址：https://...
平台：YouTube / B 站
语言：原文 / 中文 / 双语

视频简介：
简介正文

笔记：

[00:04]
笔记正文
```

全部笔记导出：

- 一个 TXT 文件，首行为 `DigestDock 全部笔记`。
- 每个视频一个清晰分隔段，顺序与 UI 来源分组一致。
- 每段包含完整元数据、简介和时间码排序后的已保存笔记，不附带整部字幕。
- 文件名包含导出范围、语言模式和日期，但不包含非法路径字符。

### 语言组装

新增通用组装函数，UI 和导出共用同一语言判定：

- `localizedTitle(source, mode)`
- `localizedDescription(source, mode)`
- `localizedTranscript(source, mode)`
- `localizedNote(note, mode)`

双语输出应清楚标记“原文”和“中文”，按同一内容单元相邻排列；内容相同只输出一次。禁止 UI 一套语言逻辑、导出再实现另一套互相漂移的规则。

### 下载实现

- 复用现有 `Blob + URL.createObjectURL + <a download>` 模式，不申请 `downloads` 权限。
- MIME 使用 `text/plain;charset=utf-8`。
- 下载后立即 `URL.revokeObjectURL()`。
- 添加导出进度与成功/失败状态，不因浏览器阻止下载而声称已完成。

## 5.10 字幕下载按语言模式

### 修改现有逻辑

将 `exportTranscript()` 改为：

1. 读取当前 `currentTranscriptMode`；B 站固定使用中文来源语义。
2. 使用完整分段数据而不是仅使用 `currentTranscriptText`。
3. 原文按时间码输出原始段落。
4. 中文按时间码输出全部有效中文段落。
5. 双语在每个时间码下先原文、后中文；相同内容去重。
6. 标题和视频简介也使用同一语言模式。
7. 缺少完整翻译时进入导出预检，不生成误称“完整字幕”的不完整文件。

### 文件格式

- 继续使用 UTF-8 `.txt`；字幕与笔记通过文件名中的 `transcript` / `notes` 区分。
- 文件头保留标题、频道、网址、视频简介、语言模式和导出时间。
- 文件名：`<safe-title>-transcript-<original|zh|bilingual>.txt`。

### 验收条件

- 三种模式下载内容与侧栏当前语言语义一致。
- 中文/双语导出不会只包含已经滚动到的懒加载翻译片段。
- 用户取消补译后不产生文件、不发 Provider 请求。
- B 站固定中文下载不显示冗余的“原文/中文”模式控件，但文件仍包含正确中文内容。

## 6. 分阶段执行顺序

### Phase 0：冻结基线与冲突检查

1. 确认当前分支、提交和工作树。
2. 如有并行线程未提交改动，停止修改重叠文件并先协调。
3. 记录当前测试基线，但不把旧版本测试结果当作新迭代通过证据。
4. 创建独立实现分支或工作树；不得在有他人未提交改动的工作树直接实施。

完成条件：目标分支、允许修改文件和保留改动均已明确。

### Phase 1：B 站语言控件

先完成平台矩阵与来源标签调整，并补齐纯 DOM/逻辑测试。该阶段不改 Provider 和笔记数据结构。

完成条件：YouTube 三控件不回归，B 站字幕/概览无控件，B 站笔记三控件完整。

### Phase 2：笔记标题多语言与来源分组

1. 增加标题渲染 helper。
2. 增加标题翻译与去重协议。
3. 使用现有写入队列持久化。
4. 升级备份 schema 并保留旧版本兼容。
5. 按 `mediaKey` 建立来源容器，容器内部只按时间码排序。
6. 补齐当前视频、全部笔记和跨平台测试。

完成条件：标题与正文同步切换；同一视频标题只翻译一次；同源笔记只出现一个容器并按时间码排序；失败安全回退。

### Phase 3：导出资料层、笔记导出与字幕下载

1. 保存按媒体去重的标题、频道、网址、简介和完整字幕来源资料。
2. 对现有笔记执行无网络的缓存回填迁移。
3. 实现导出预检、缺失提示、调用范围确认和取消。
4. 实现当前视频/所选视频/全部笔记 TXT 导出。
5. 改造字幕下载，使其遵循原文/中文/双语模式。

完成条件：导出包含要求字段；全部笔记按来源分节；任何缺失资料或补译调用都不会静默发生。

### Phase 4：Provider 注册表与后台 Adapter

1. 建立 Provider 注册表。
2. 完成设置迁移。
3. 拆分通用请求层与 Provider 差异。
4. 用 Mock 测试覆盖全部 Adapter。
5. 更新最小 host permissions。

完成条件：离线测试能够证明每个 Provider 生成正确请求、解析正确响应、隔离专用字段且无自动回退。

### Phase 5：Provider 设置 UI 与官方图标

从官网获取并登记六个品牌图标，实现带图标的下拉、Key Map、能力说明、状态和中英文文案；保留 Supadata 卡片。

完成条件：用户仅需选择服务并填写 Key；切换不丢值；保存不发网络请求；图标与 Provider 一一对应且离线可用。

### Phase 6：删除冗余入口

1. 删除“字幕服务”导航。
2. 删除“本地改造”入口、内容、脚本、样式和旧测试。
3. 重新校准设置页滚动锚点、导航活动态和底部空间。

完成条件：最终导航与第 2.3 节一致，设置页没有死锚点或孤立文案。

### Phase 7：文档与验收

更新文档、执行自动检查和真实浏览器复核。真实 Provider 连通测试必须另行取得用户授权和可用 Key，不得从“执行方案”自动推导授权。

完成条件：第 8 节所有不需要真实付费调用的门均通过；需真实调用的项目明确标为“未验证”或在单独授权后完成。

## 7. 文件级修改清单

| 文件 | 主要修改 |
|---|---|
| `sidepanel.js` | 平台控件矩阵、标题多语言、来源分组、笔记/字幕导出、导出预检、Provider 名称与能力提示 |
| `sidepanel.html` | 保留三组语言控件；增加笔记导出入口；调整脚本加载顺序与必要语义 |
| `sidepanel.css` | 双语标题、来源容器、导出菜单/状态、截断、隐藏控件后的顶部布局 |
| `background.js` | Provider Adapter、标题/简介/字幕补译协议、来源资料持久化、通用错误归一化 |
| `ai-providers.js` | 新增 Provider 注册表、请求/响应差异与能力定义 |
| `icons/providers/*` | 六个官网品牌图标与来源记录 |
| `settings.js` | Key Map、旧设置迁移、注册表派生设置 |
| `options.html` | Provider 下拉、动态 Key 卡、删除字幕服务导航与本地改造 |
| `options.js` | Provider 表单状态、i18n、保存与状态展示；删除复制提示词逻辑 |
| `options.css` | 下拉、能力标签、动态服务卡；删除 remix 样式 |
| `notes-backup.js` | 标题译文字段、schema 3、旧备份兼容；与阅读型 TXT 导出保持职责分离 |
| 新增 `note-export.js` | 来源分组、语言组装、笔记/字幕 TXT 生成与文件名清理的纯函数 |
| 新增 `note-sources.js` | 按媒体去重的来源资料 schema、存储、回填与导出预检 |
| `manifest.json` | 经核实的最小 Provider host permissions；版本号由发布阶段决定 |
| `prompts/translation.md` | 标题翻译的结构化输出约束 |
| `scripts/check-release.sh` | 新文件、官方图标来源记录、权限、文档和禁止旧入口检查 |
| `scripts/package-extension.sh` | 打包新增运行时文件 |
| `DESIGN.md` | 平台语言矩阵、来源分组、标题和导出语义、Provider 图标、最终设置导航 |
| `UI-REPLACEMENT-PLAN.md` | 删除旧导航与本地改造阶段，补充分组、导出与本轮验收 |
| `README.md` / `README.zh-CN.md` | Provider 设置、官方图标来源、Supadata 边界、分类与导出 |
| `PRIVACY.md` / `SECURITY.md` | 多 Provider 数据流、Key 存储、导出资料、域名与风险 |

## 8. 测试与验收

### 8.1 自动测试

#### `tests/translation.test.js`

- YouTube 三个标签页显示对应语言控件。
- B 站字幕、概览隐藏容器，笔记显示三按钮。
- 平台切换不残留 hidden/active 状态。
- B 站来源标签不再出现冗余模式词。
- 标题三模式渲染、中文复用、相同译文去重。
- 同一媒体多条笔记只生成一个标题翻译项。
- 标题成功、正文失败；正文成功、标题失败；两者都失败的独立回退。
- B 站笔记模式保留三按钮，字幕和概览不显示语言控件。

#### 新增 `tests/notes-presentation.test.js`

- 按 `mediaKey` 分组，同名不同视频不合并。
- B 站不同分 P 不合并。
- 来源内笔记只按 `timestampSeconds` 升序，保存时间不影响顺序。
- 相同时间码使用 `id` 保持稳定顺序。
- 标题、频道、平台、链接和笔记数量正确显示。

#### 新增 `tests/note-export.test.js`

- 当前视频与全部笔记导出结构正确。
- 每个来源包含标题、频道、网址、视频简介和已保存笔记，不附带整部字幕。
- 原文、中文、双语组装与 UI 语言 helper 一致。
- 双语相同内容去重，不同内容相邻输出并带清楚标签。
- 笔记按时间码排序，来源不按 `createdAt` 排序。
- 缺失原字幕时阻止完整导出；缺失翻译时返回预检结果，不发网络请求。
- TXT 使用 UTF-8、安全文件名和正确 MIME；Object URL 被释放。

#### 新增 `tests/transcript-export.test.js`

- 原文、中文、双语字幕按时间码完整输出。
- 中文/双语不能只导出已滚动到的懒加载片段。
- 标题、频道、网址和视频简介遵循当前模式。
- B 站固定中文导出正确。
- 用户取消补译时零 Provider 请求、零下载。

#### 新增 `tests/note-sources.test.js`

- 同一媒体只保存一份来源资料。
- 老笔记可从现存 digest 缓存无网络回填。
- 资料缺失时预检列出明确媒体，不自动访问 Supadata、B 站或 YouTube。
- 存储配额、单份字幕大小和清理策略有边界。

#### `tests/notes-backup.test.js`

- schema 3 标题译文往返。
- schema 1、2 兼容导入。
- 非法标题字段、超长字段和控制字符拒绝。
- 冲突合并不覆盖不同的已验证标题译文。

#### `tests/settings.test.js`

- 旧 `aiApiKey` 迁移至 `aiApiKeys.deepseek`。
- 每个 Provider Key 独立保存。
- Endpoint 和模型由注册表派生，用户输入无法覆盖。
- 未知 Provider 安全回到支持状态，但不得误用其他 Provider 的 Key。

#### `tests/ai-providers.test.js`

- 每个 Provider 请求 URL、Header、模型和字段正确。
- DeepSeek 专用字段不会泄露到其他 Provider。
- JSON mode 与 temperature 按能力发送。
- 响应与错误正确归一化。
- Provider 失败不会自动调用另一家。
- 六个 `iconPath` 对应存在的本地资产，品牌来源记录完整。
- 图标路径不包含 `http:`、`https:`、`data:` 或远程重定向。

#### 设置与发布测试

- `tests/options-language.test.js`：中英文动态文案与可访问标签。
- Provider combobox/listbox：键盘、焦点、Escape、Enter、活动项和图标替代文本。
- `tests/settings-nav.test.js`：最终四个导航目标、滚动活动态与底部边界。
- `tests/release.test.js`：不存在字幕服务导航、本地改造、自定义 Endpoint；新增运行文件和权限完整。
- 现有 B 站、YouTube、备份、页面按钮和 Supadata 测试全部继续通过。

### 8.2 命令门

```bash
npm test
npm run check
npm run package
git diff --check
node --check sidepanel.js
node --check options.js
node --check background.js
node --check settings.js
node --check ai-providers.js
node --check note-export.js
node --check note-sources.js
```

任何一项失败都不能表述为“完成”。

### 8.3 浏览器复核

必须重新加载解压扩展后检查：

1. YouTube 英文字幕视频：字幕、概览、笔记三种模式。
2. YouTube 中文原文视频：不产生不必要翻译调用。
3. B 站中文字幕视频：字幕与概览无语言控件，笔记有三种模式。
4. B 站与 YouTube 的“全部笔记”：标题与正文同步切换语言。
5. 全部笔记：同源内容在一个完整容器内，乱序保存后仍按时间码排列。
6. 笔记导出：当前视频、所选视频和全部笔记分别检查原文、中文、双语 TXT。
7. 字幕下载：三种模式文件内容、文件名和完整性。
8. 设置页桌面与窄屏：Provider 下拉、六个官网图标、Key 切换、固定保存栏和导航活动态。
9. 断网复核：Provider 图标仍显示，设置页不请求远程图片。
10. Supadata：没有当次授权时零请求；授权后仅当前 YouTube 视频请求。

真实 Provider 连通测试：

- 默认不执行。
- 每家 Provider 的真实调用需要用户提供或确认使用对应 Key，并明确同意本次可能计费的测试。
- 每家只运行事先约定的最小 canary；失败不自动重试或换 Provider。
- 记录请求是否成功、实际模型、能力、限流与响应格式，但不记录 Key 或完整私密内容。

## 9. 风险与控制

| 风险 | 控制措施 |
|---|---|
| Provider 宣称 OpenAI-compatible，但字段不完全兼容 | Adapter 独立构造；逐项测试，不只替换 URL/模型 |
| 模型名、Endpoint、价格漂移 | 实施与发布时查官方文档；价格只写带日期的说明 |
| Host permissions 扩大 | 只添加官方 API 精确域名，不使用宽泛通配符 |
| Provider 图标来源不准确或品牌使用不当 | 只取官网资产，登记来源与日期；无公开资产时停止确认，不自行仿制 |
| 自定义图标下拉损害无障碍 | 按 ARIA combobox/listbox 实现并做完整键盘与屏幕阅读器测试 |
| 切换 Provider 导致 Key 丢失或串用 | Key Map + 迁移测试 + UI 状态测试 |
| 标题翻译放大调用量 | 按 mediaKey 去重，并并入现有用户触发的有界任务 |
| 全部笔记导出放大翻译调用量 | 先做只读预检，明确缺失数量；补译需当次确认、总量上限、进度和取消 |
| 全量字幕与简介挤占 Chrome 本地配额 | 按媒体去重；优先评估 IndexedDB；不把字幕重复写入每条笔记 |
| 旧笔记没有来源资料导致导出不完整 | 从现存 digest 缓存无网络回填；仍缺失时明确阻止或标记，绝不静默请求第三方 |
| 分类误用标题导致不同视频合并 | 只以稳定 mediaKey 分组，标题仅用于显示和组排序 |
| 字幕下载只导出部分懒加载译文 | 导出前检查全量段落完整性，缺失时进入确认与补译流程 |
| 备份兼容破坏 | schema 3 向后兼容 1/2，保留严格校验 |
| B 站隐藏状态污染 YouTube | 隐藏整个容器并在每次平台/标签切换重算 |
| 翻译专用 Provider 被用于概览 | capability gate；无隐式回退 |
| 并行线程覆盖改动 | Phase 0 重查工作树，重叠文件先协调 |
| 自动测试通过但真实扩展未验证 | 单独记录浏览器复核和真实 Provider 未验证项 |

## 10. 完成定义

只有同时满足以下条件，才可把本次迭代标为本地完成：

1. 第 2 节全部产品决策已实现。
2. 旧“字幕服务”导航与“本地改造”内容已删除，Supadata 功能未回归。
3. Provider 预设、设置迁移、请求适配和能力限制均有自动测试。
4. 六个 Provider 的下拉项和选中状态使用可追溯的官网本地图标，离线可用。
5. 笔记正文和视频标题在三种模式下行为一致，备份兼容通过。
6. 全部笔记按媒体来源分组，来源内只按时间码排序。
7. 当前视频/全部笔记导出均包含标题、频道、网址、视频简介和已保存笔记，不附带整部字幕，并遵循语言模式。
8. 字幕下载按原文、中文、双语输出完整内容，不导出部分懒加载结果。
9. 第 8.2 节所有命令通过。
10. YouTube、B 站、导出文件和设置页真实浏览器复核完成。
11. 文档与代码一致，无过期 Provider、导航、导出或隐私描述。

“本地完成”不等于已提交、已推送、已发布或用户验收。版本号、提交、推送、发布和真实付费 Provider 测试均需单独授权与证据。

## 11. 推荐交付顺序

1. 先交付 B 站控件矩阵的小型变更，降低与 Provider 重构的耦合。
2. 再交付笔记标题翻译、来源分组与备份 schema，锁定数据兼容和展示结构。
3. 建立按媒体去重的来源资料层，再实现当前视频/全部笔记导出和三模式字幕下载。
4. 再实施 Provider 注册表、后台 Adapter 和设置迁移。
5. 从各官网获取并登记品牌资产；Provider 通过离线测试后实现带图标下拉并删除冗余入口。
6. 最后统一更新设计、隐私、安全、README 和发布检查，并进行浏览器及导出文件验收。

该顺序允许每一阶段独立审查和回退，避免把 UI、来源资料存储、导出、第三方 API、品牌资产与发布改动堆进一次不可审查的大型提交。
