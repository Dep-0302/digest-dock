{
  "name": "youtube-digest",
  "version": "1.1.5",
  "private": true,
  "description": "Turn YouTube and Bilibili videos into learning resources with transcripts, AI overviews, and timestamped notes.",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "bash scripts/check-release.sh",
    "package": "bash scripts/package-extension.sh"
  }
}
