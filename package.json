{
  "name": "digest-dock",
  "version": "1.4.8",
  "private": true,
  "description": "Turn supported media into learning resources with transcripts, AI overviews, and timestamped notes.",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "bash scripts/check-release.sh",
    "package": "bash scripts/package-extension.sh"
  }
}
