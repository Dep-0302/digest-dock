# DigestDock 阶段 2：想法层实施清单（交 Claude 复核）

> **历史记录（2026-09-14 归档）：** 下文的状态、版本、门禁数字和执行约束属于文中所标的原阶段，不代表当前分支。保留原文用于追溯；当前阶段 2 的适用修订见 [规格 §10](PHASE2-THOUGHT-LAYER-2026-09-09.md#10-2026-09-14-收尾修订现行判据)，最终状态见 [收尾记录](PHASE2-CLOSEOUT-2026-09-14.md)。

> 执行进展更新：红测已获用户复核并补 A、B，提交为 `7d522df`；捕获与存储已通过原 Claude 桌面会话独立复核，提交为 `f9571ee`；定向 15 项通过。当前待用户决定是否额外增加 B 站捕获对齐。以下保留执行清单的阶段定义；当前证据见 [捕获与存储复核说明](PHASE2-CAPTURE-STORAGE-REVIEW-2026-09-10.md)。

> 日期：2026-09-10
> 状态：清单已获批准；已按二次修订补定更新。阶段 A 核对与红测已完成，红测待用户复核；尚未实现功能或提交。
> 唯一行为事实来源：[PHASE2-THOUGHT-LAYER-2026-09-09.md](PHASE2-THOUGHT-LAYER-2026-09-09.md)，标题区标注 2026-09-10 二次修订，本次读取共 225 行。
> 本文说明 Codex 准备怎么做，不替代规格、不新增产品需求。与规格冲突时以修订规格为准。

## 1. 请 Claude 重点复核什么

1. **目标与范围**：以下每项是否确实对应 T1–T10，有没有偷偷恢复确认状态、扩大改动或改变既定交互。
2. **正确性与证据**：测试是否验证真实行为，尤其 T7 是否覆盖 provider 调用和存储不变，T10 是否检查实际播放位置。
3. **失败与恢复**：编辑保存、异步搜索、跨视频回跳失败时，是否会丢想法、覆盖旧字段或错误宣称成功。

请将反馈区分为：必须先补定的规格问题、必须调整的实施问题、可留后续的非本轮建议。本文复核不替代各阶段提交前的实际 diff 复核。

## 2. 当前事实与授权边界

- 已完整读取修订规格；此前关于确认字段、AI 读写边界的两个问题已解决。
- 阶段 A 已从 `main @ cea382e Release DigestDock 2.0.0` 创建 `codex/thought-layer-phase2`；尚无新提交。
- 本文创建前，工作区有 5 份未跟踪 Markdown 文档，没有已跟踪代码改动。
- 当前新增自动化测试供用户复核；不开始功能实现，不自行调用 Claude，不提交红测。
- 已从核对后的 `main` 创建本地分支 `codex/thought-layer-phase2`；后续继续使用该分支，若基线或用户改动变化，先核对原因，不覆盖已有工作。
- 不 push、不 merge、不 release、不改版本号、不为本轮制作发布包。
- 不修改、删除或清理 `worktrees/notes-trust-phase0/`，不进入该工作树实施。
- 根目录未跟踪规格、旧交接文档以及本文都不纳入代码提交；逐个路径暂存，不使用全量暂存。
- 规格未覆盖且会影响用户行为或数据含义的情况，停止相关实现并反馈，不自行补定。

### 2.1 旧交接文档的冲突处理

`PHASE2-CODEX-HANDOFF-2026-09-10.md` 仍有旧规则：确认标记、未确认置顶、绝不让 AI 读取想法等。它不是本轮行为真源，不据此实现，也不顺手改写它。

本轮执行以下修订：

- 不存在确认、已读、未读、待办状态；不新增相关字段、置顶区或计数。
- 空 `thought` 就是正常金句；历史笔记不标“缺失”。
- 任意笔记可在「最近」就地编辑想法；编辑笔记对象只改变 `thought`、`thoughtAt`。
- AI 可读取想法以检索；不可改写想法或任何已存笔记字段。
- AI 返回值只作为现有笔记 ID 的候选；展示内容始终从本地原笔记取出。

## 3. 冻结的实现范围

| 模块 | 必须完成的行为 | 验收对应 |
| --- | --- | --- |
| 捕获 | 第一次 N 保存金句、不暂停；toast 可见期间第二次 N 就地输入、聚焦并暂停；Enter 保存、Esc 退出 | T1–T4 |
| 本地检索 | 一个搜索框；输入后全局搜索；覆盖想法、原句、标题、频道；想法优先并按保存时间排序 | T5、T6 |
| AI 兜底 | 只在零结果时提供按钮；点击前无 provider 请求；点击后只展示有效现有 ID 对应笔记、库不变 | T7 |
| 最近与编辑 | 全部笔记且空搜索显示按本地现实日期分组的最近笔记；当前视频列表保持不变；无置顶或确认计数；任意最近条目可补写、修改或清空想法 | T8、T9 |
| 回跳 | 两个视图中的笔记均能回到对应视频对应秒；不可达时展示冻结触发窗口和实际原因 | T10，规格 §6 |

### 3.1 明确不做

- 不增加类型字段、分类选择、标签、金句/想法筛选按钮。
- 不增加快捷键设置，不向 manifest 添加 `commands`，保留裸 N。
- 不改变第一次 N 的轻量保存交互，不改为第一次 N 就弹输入框。
- 不做想法润色、翻译、纠错、摘要或其他 AI 改写。
- 不做本地模糊匹配、向量检索、保存的搜索条件。
- 不修改捕获锚点 `-3` 秒、`triggerWindow -8/+12`、原有 AI 输入窗口“前4+TARGET+后2”。
- 不改变字幕获取链路、概览、任何现有导出行为。
- 不修用户指定的两项遗留问题：`background.js` 约 5143 行恒假分支、`ytd_notes_<mediaKey>` 与 `ytd_notes_schema` 键名碰撞；确保本轮不加重。
- 不进行全文件格式化、无关重构、依赖升级或迁移流程重写。
- 新代码、注释和 UI 统一称「最近」或现实日期分组，避免与视频内时间码混淆。

## 4. 执行顺序与分阶段提交

### 阶段 A：实施前核对

1. 再读当前规格，确认修订内容未变化；核对 Git 状态、基线、分支和已有用户改动。
2. 创建本地实施分支，保持旧工作树与未跟踪规格原样。
3. 聚焦读取捕获、笔记读写、索引、笔记列表、provider 调用与回跳代码及相应测试。
4. 核对本机 Chrome 中实际加载的扩展 ID、来源目录、版本与可用测试页面，避免验收到旧工作树。
5. 记录基线门禁结果和已有失败；这一步只建立对照，不将原有失败归因给本轮。

### 阶段 B：先写 T1–T10 自动化测试，先红

1. 在现有 Node 测试体系内增加行为测试；尽量复用当前测试沙箱、存储桩和 DOM 桩，不安装新测试框架。
2. 将下文十项和必要的边界断言全部写出，再进入功能实现。
3. 在未实现的代码上运行，记录哪些通过、哪些因缺少阶段 2 行为失败。
4. 保留测试命令、失败原因和数量；已有正确行为如第一次 N 不暂停可先绿，不制造人为失败来凑十条全红。
5. 红测必须因缺少目标行为而失败，不能仅因测试自身语法错误、错误夹具或加载失败。
6. 将测试 diff、T 编号映射、红测结果先交用户复核；由用户安排 Claude 查看。收到通过反馈前不提交，之后记录红测提交哈希。

### 阶段 C：实现并使测试变绿

按“捕获与存储 → 本地检索与最近 → AI 兜底与回跳”的顺序接入，同一阶段内只运行必要的相关测试定位问题。

完成后运行全部门禁，再交 Claude 复核实现 diff。反馈属于本轮的则修复并重跑受影响测试；超出本轮的只记录或反馈。

实现必须拆成三个提交，每份提交前单独复核：

1. 捕获与存储：T1–T4、T9 写入侧。
2. 检索与「最近」：T5、T6、T8；T9 的最近内编辑入口与该视图一并接入，存储契约仍归第一份。
3. AI 兜底与回跳：T7、T10。

红测单独在前，计划合计四个提交。验收阶段若必须再修复，独立说明该修复与原三个实现提交的关系，不混入未复核内容。

### 阶段 D：完整门禁与真实 Chrome 验收

1. 实现完成后，`npm test` 与 `npm run check` 全部通过，再开始本轮正式 Chrome 验收。
2. 在真实 Chrome 重新加载对应本地分支的扩展，并确认页面 content script 已更新。
3. 按下文方法逐条执行 T1–T10；浏览器里的失败需要最小修复、复核 diff、对应复测并重新通过完整门禁。
4. 若产生验收修复，单独提交并记录哈希；不能把未经复测的新 diff 混入已通过验收的版本。
5. 最终核对验收过的代码与最终提交一致，停在本地分支。

门禁说明：当前 `npm run check` 会检查发布文件白名单、资源引用、JS 语法、配置/凭据等，并再次执行测试；它不等于发布。交付时分别报告两次命令的实际结果，不把重复执行的测试相加成“覆盖数量”。

## 5. 各模块准备怎样实现

### 5.1 两次 N 与 toast（T1–T4）

- 继续使用现有 `saveCurrentNote()` 保存第一条笔记，保留现有快捷键位置、输入场景避让和字幕保存路径。
- toast 持有本次成功保存笔记的 ID 与来源标识；第二次 N 编辑同一条笔记，不再新存一条。
- 将普通 toast 停留时长从 5 秒调整为 10 秒。
- 第二次 N 时清理自动消失计时器，在原 toast 容器内放置想法输入控件并自动聚焦，暂停对应视频。
- 输入模式不继续自动消失，普通键入 N 不再触发新的保存。
- Enter 将用户输入原文传给专用想法更新路径；保存成功才关闭输入框，不自动恢复播放。
- Esc 不提交输入、不删除已保存笔记，关闭输入框；不引入二次确认。
- 中文输入法候选确认期间的 Enter 不能误提交；需要用组合输入事件状态验证。
- 处理异步保存与输入关闭的先后关系，避免旧响应操作新 toast；失败不得伪报保存成功或吞掉尚未保存的输入。
- 空想法与 null thoughtAt 同进同退；清空退回金句，不二次确认，不改 createdAt。

### 5.2 想法读写与索引（T3、T9）

- 直接复用 `thought`、`thoughtAt`，不引入类型字段或状态字段。
- 后台根据笔记 ID 定位现有记录，沿用已有串行写入与数据重置保护机制。
- 对笔记对象只更新 `thought`、`thoughtAt`；字幕正文、原句、触发窗口、来源、锚点、创建时间等逐项保留。
- 同步更新已有索引的 `hasThought`、`searchText` 等派生内容，使新想法立即可被搜索到；不改变索引结构。
- 已有索引 `savedAt` 来自笔记 `createdAt`，编辑想法不把保存时间改成编辑时间。
- 不让存储标准化或旧对象回写丢掉其他字段；并发更新、删除或数据重置后，不得由迟到的编辑复活旧记录。
- 不改导出/备份格式；若现有存储辅助方法会损失想法字段，先核对调用路径，不能顺带重写备份系统。

### 5.3 全局精确检索（T5、T6）

- 笔记页增加一个搜索框；默认当前视频及其原列表保持不变。全部笔记且空搜索时显示「最近」；搜索临时全局，清空回到搜索前那一侧。
- 输入搜索词后直接放开“当前视频”范围，检索完整笔记库；即使此前停在当前视频也不能漏掉其他来源。
- 复用已有索引/文本规范化方式，搜索范围只包含想法、触发原句、视频标题、频道名，不添加模糊匹配。
- 有想法的结果排在前，各组按 `savedAt` 倒序；不引入状态优先级。
- 卡片上方展示用户想法，下方用次级样式展示触发原句和冻结窗口，保留来源与回跳入口。
- 渲染用户文本时按文本处理，避免被解释成 HTML；不向 AI 请求“改善”卡片内容。
- 连续输入、切换筛选、清空搜索时，旧异步结果不能覆盖新查询或「最近」。

### 5.4 零结果 AI 兜底（T7）

- 精确搜索零结果才出现“让 AI 找找”；正常结果和空搜索时不显示此按钮。
- 本地搜索、零结果渲染、清空查询不调用 provider；只有该按钮明确点击才启动检索请求。
- 输入只组装必要的现有笔记 ID、想法、原句、标题和频道，不附带凭据或无关缓存。
- 复用既有 provider 配置与调用层，不新增 provider、设置页选项或默认自动检索路径。
- 请求 AI 只返回候选笔记 ID；对响应做结构解析、现有 ID 校验、去重，丢弃未知 ID 和附带生成内容。
- 展示时重新取得本地笔记原内容，最多 5 条，不展示 AI 生成的答案、理由、改写文本，不把响应写回笔记库。
- 大库按 savedAt 倒序取装得下的笔记，并显示「只检索了最近 N 条」；不静默截断、不自动多轮请求。
- 防止迟到响应覆盖新查询；响应期间被删除的笔记不能作为当前候选出现。
- 配置不足或 provider 失败不得伪造候选或自动循环重试；沿用可复用的现有错误提示方式。
- 已观察到 `loadNotes()` 默认可能调用 `ensureNotesChinese()`。搜索路径必须避免借由加载笔记暗中触发翻译，不能只拦截新检索接口后宣称“零请求”。只做满足 T7 的局部隔离，不改其他功能的翻译行为。

### 5.5 「最近」与随时编辑（T8、T9）

- 全部笔记且空搜索时显示「最近」，按浏览器本地时区自然日分组倒序，跳过没有笔记的日期；当前视频一侧保持原列表。
- 不渲染置顶区、确认按钮、未读或待确认数量，不写入任何同类状态。
- 空 `thought` 正常显示为金句，迁移来的笔记即使缺少该字段也不能显示异常或“缺失”。
- 「最近」里任意条目可就地编辑想法，不依赖 10 秒 toast，也不受状态限制。
- 编辑保存后刷新本地展示与索引；保持原保存日期，不因修改错别字改变日期归属。
- 覆盖已有想法的修改，而不只是首次补写；失败时不把未保存输入当作已经落库。

### 5.6 回跳及不可达降级（T10）

- 复用现有 `playNote()`、来源标识和带时间参数的 URL，不重写媒体路由或字幕获取链路。
- 两个视图都以实际笔记对象调用回跳，不仅处理当前视频。
- 验证当前视频定位、跨 YouTube 视频、跨 YouTube/Bilibili；Bilibili 有分 P 时保留已有定位信息。
- 验收目标为笔记存储的 `timestampSeconds`，不再次加减捕获偏移。
- 对可明确观测的打开失败、无效来源或视频不可用状态，展示冻结的 `triggerWindow` 和可确认原因。
- 新标签页创建成功不等于视频可播放，也不等于已到对应秒；真实浏览器检查实际页面与播放位置。
- 不额外启动字幕抓取或 AI 请求来“补救”不可达视频；无法确认原因时不编造已删除或无权限等结论。

## 6. 自动化验收矩阵：先写出的具体断言

| 编号 | 自动化设置与操作 | 必须断言 |
| --- | --- | --- |
| T1 | 用真实 content 脚本事件处理器模拟播放中按 N，后台存储夹具返回成功 | 只新增一条笔记；toast 出现；视频无暂停调用且继续播放；thought 为空；10 秒前不因旧 5 秒计时关闭 |
| T2 | 在 T1 的同一个 toast 可见期间再次按 N，然后推进计时器超过 10 秒 | 未新增第二条笔记；原容器变成输入框；activeElement 为输入控件；视频暂停；输入框仍存在 |
| T3 | 输入中文/多行文本并按 Enter，走后台想法更新及真实存储桩 | thought 等于用户输入；thoughtAt 落库；只变动允许字段与派生索引；成功后关闭；无 play 调用；输入法组合期间 Enter 不误保存 |
| T4 | 第二次 N 后输入内容，再按 Esc | 输入框关闭；第一条笔记仍在且 thought 仍空；不存在更新想法或删除笔记请求 |
| T5 | 当前视频与全部范围各运行一次，搜索只存在于另一视频想法的唯一词 | 两种范围都搜到；全局范围被放开；想法在原句/窗口上方；有想法优先且各组 savedAt 倒序；标题/频道搜索也各有覆盖 |
| T6 | 想法含同音错字，搜索只存在于原字幕的词 | 仍命中对应笔记；匹配不是依赖想法、AI 或模糊算法 |
| T7 | 零结果后先不点击，随后点击；provider 返回有效 ID、重复 ID、无效 ID、附带文字等夹具 | 不点击时 provider 层调用为 0；点击后候选仅为有效现有 ID；AI 文本不展示；请求前后笔记分片、索引及 schema 的序列化内容逐字节一致；对笔记存储的 set/remove/clear 调用为 0 |
| T8 | 装入跨多个日期、有空档日、含历史金句和想法的笔记，切到全部笔记且空搜索；另验证当前视频一侧 | 标题为最近；按浏览器本地时区自然日倒序；没有空日期组、置顶区、待确认计数或缺失提示；当前视频默认与原列表不变；清空搜索恢复搜索前一侧 |
| T9 | 在最近给空想法补写，再修改一条已有想法，保存后立即搜索新词 | thought/thoughtAt 正确；createdAt、锚点和其他原字段不变；不新增任何额外状态字段；索引同步；新内容可查，旧内容不残留于该字段索引 |
| T10 | 分别从搜索结果和最近点击当前/跨视频/跨平台笔记，并注入可明确观测的打开失败 | 回跳参数对应笔记来源及 timestampSeconds；无二次偏移；失败有冻结窗口与原因；不改变存储内容或调用字幕/AI 补抓 |

### 6.1 T7 证据不能降格的细节

- 不能只断言“UI 没变化”“没调用新搜索函数”或只检查某一个 endpoint。
- 在后台统一 provider 调用层或其底层请求边界挂 spy，覆盖现有笔记翻译等旁路；测试驱动实际页面搜索流程。
- 库不变断言范围为笔记分片、索引与 schema。请求发起前已完成正常初始化/迁移，不把初始化混入只读检索证据。
- 使用固定键序的序列化快照进行逐字节比较；另检查存储写操作，防止先改后还原而最终快照碰巧相同。
- 响应中合法 ID 对应的可见内容必须来自原笔记；有效 ID 旁夹带的 AI 改写也不能展示。
- 非法响应、没有有效候选、请求失败、查询已变化、候选已删除要有对应断言；不为失败路径额外新增产品能力。

## 7. 真实 Chrome：逐条重走的方法与证据

使用现有 Chrome 控制通道，核对实际加载目录；不脚本启动系统 Chrome、不安装替代浏览器、不修改系统安全设置。用可识别的专用测试笔记，避免覆盖用户已有内容；跨日期夹具如需写入真实扩展存储，先明确测试对象与可恢复方式，不整库替换。

| 编号 | 真机动作 | 记录的实际证据 |
| --- | --- | --- |
| T1 | 在可播放视频按一次 N | 播放位置继续前进、toast 出现、存储新增笔记 thought 为空；观察超过 5 秒仍存在并在约 10 秒退出 |
| T2 | toast 可见时再次 N，等待超过 10 秒 | 同一 toast 输入框、光标聚焦、视频暂停、输入框不消失；笔记总数不增加 |
| T3 | 中文输入后 Enter | 实际存储 thought/thoughtAt、输入关闭、视频仍暂停；中文组合输入确认不误提交 |
| T4 | 另建一条测试金句，第二次 N 后 Esc | 关闭输入；笔记仍在；输入内容没有写入 thought |
| T5 | 当前视频范围搜另一视频想法唯一词，再从全部范围重复 | 两次都命中；来源正确；想法在上、原句/窗口在下；多条夹具排序符合规则 |
| T6 | 搜原字幕独有词 | 想法无该词的条目仍命中 |
| T7 | 打开零结果，不点击观察请求，再点击 | service worker/provider 请求记录证明先无请求、点击才有；候选 ID 对应现有笔记；请求前后笔记库快照一致；非法响应过滤由自动化补证 |
| T8 | 切到全部笔记且空搜索；用跨日笔记检查；分别从当前/全部进入搜索再清空 | 本地自然日分组倒序且跳过空日；无置顶、计数、缺失提示；空想法正常显示；默认与当前视频原列表不变，筛选恢复正确 |
| T9 | 在最近补写一条想法，再修改已有想法 | 落库字段与索引更新；没有确认字段；立即搜索新词可命中；原日期不变 |
| T10 | 两个视图分别完成当前视频、跨视频、跨平台回跳，并验证不可达降级 | 目标视频身份与实际播放秒数；记录读取播放位置的时刻，区分正常播放推进；失败时实际窗口内容与原因 |

真机验收不以模拟 DOM 测试替代，不以打开链接替代实际定位。若登录态、provider 配置、视频可用性、浏览器工具能力使某条无法完成，该条写“未验证 + 具体阻塞”，不写通过。

## 8. 预计文件落点与改动控制

| 文件/区域 | 拟改内容 | 对应验收 |
| --- | --- | --- |
| `content.js` | toast 状态与计时器、第二次 N、想法输入事件 | T1–T4 |
| `background.js` | 想法更新消息与串行写入、索引同步、只读候选检索 | T3、T7、T9 |
| `sidepanel.js` | 搜索/最近状态、排序渲染、编辑、AI 按钮、回跳降级的必要接入 | T5–T10 |
| `sidepanel.html` | 搜索框及必要状态容器 | T5、T7、T8 |
| `sidepanel.css` | 想法主次层级、日期标题、编辑框及必要状态样式 | T5、T8、T9 |
| `tests/` 相关测试 | T1–T10 的事件、存储、provider、排序与导航断言 | T1–T10 |

- 优先在以上位置最小实现。它们是预计落点，不是必须全部改的清单，也不是扩大改动的授权。
- 不预先新增运行时模块、prompt 文件、依赖、manifest 项或构建流程；若实际必须新增，先说明用途及对应 T 编号。
- 保留现有导出入口及其行为；列表改为日期分组不能暗中改变导出范围、内容或顺序契约。
- 原有测试若断言了本轮明确替换的列表布局，只更新相关预期并标明 T8；不删除测试或放松断言来绕过回归。
- 每次复核列出变更文件数、增删行数和“文件/关键改动 → T 编号”映射；不能映射的改动撤出本轮。
- 门禁若确实要求改规格外文件，先区分原有缺陷与本轮引入问题；必要的门禁调整单独复核、单独提交并交付说明，不能借此扩大功能。

## 9. 五项边界已经补定，不再开放

1. 空 `thought` 当且仅当 `thoughtAt` 为 null；允许清空，不二次确认，不改 createdAt 或日期归属。
2. 当前视频默认与列表不变；全部笔记空搜索显示「最近」；搜索临时全局，清空恢复搜索前一侧；按浏览器本地时区自然日分组。
3. AI 候选最多 5 条；容量不足时按 savedAt 倒序选取装得下的部分，明示「只检索了最近 N 条」，不自动多轮。
4. 不可达原因仅在能确证时说明，否则显示「没能打开这个视频」及冻结窗口；分别验证标签打开、视频可播放、实际定位。
5. 规格已把 AI 禁止范围统一为改写，允许检索读取；无需额外解释旧称。

## 10. 最终交付清单

- 本地分支名、起始基线、各阶段提交哈希与用途。
- Claude 各阶段 diff 复核结果、必须修复项及处理情况；没有实际复核证据不能标“已复核”。
- 红测命令、真实失败数量与原因；最终 `npm test` 和 `npm run check` 的通过/失败/跳过数字及退出状态；check 的白名单数量按实际输出记录。
- T1–T10 自动化对应测试位置与结果。
- T1–T10 真实 Chrome 逐条结果，实际扩展目录/版本、测试页面与关键证据；通过、失败、未验证分开写。
- 最终 diff 文件数、增删行数与验收映射；如有规格外门禁提交，独立说明。
- 本轮发现但没有修改的问题，以及未完成验收的具体原因。
- 明确停在本地：未 push、未 merge、未 release。测试通过和真机操作证据均不替代用户最终验收。

## 11. 当前状态（阶段 A 与红测复核入口）

已完成：完整重读 225 行二次修订规格；从指定基线建立本地分支；基线两项门禁通过；只读核对 Chrome 加载来源；编写并运行 T1–T10 红测。

尚未完成：用户/Claude 对红测 diff 的复核、红测提交、三个实现提交、实现后的完整门禁与真实 Chrome T1–T10 验收。

下一步：先把红测交用户复核，收到通过反馈后再提交和实施。当前不调用 Claude、不提交、不 push / merge / release。

实际数字、文件范围和 Chrome 核对结果见 [PHASE2-RED-TEST-REVIEW-2026-09-10.md](PHASE2-RED-TEST-REVIEW-2026-09-10.md)。
