# 阶段 2：阶段 A 结果与红测待复核

> **历史记录（2026-09-14 归档）：** 下文的状态、版本、门禁数字和执行约束属于文中所标的原阶段，不代表当前分支。保留原文用于追溯；当前阶段 2 的适用修订见 [规格 §10](PHASE2-THOUGHT-LAYER-2026-09-09.md#10-2026-09-14-收尾修订现行判据)，最终状态见 [收尾记录](PHASE2-CLOSEOUT-2026-09-14.md)。

> 后续更新：用户已复核通过，按要求补 A、B 后，42 项（3 过、39 个 ERR_ASSERTION）已提交为 `7d522df`。以下保留初轮 40 项复核记录。当前停点见 [捕获与存储复核说明](PHASE2-CAPTURE-STORAGE-REVIEW-2026-09-10.md)。

> 当前停点：红测已写完、已运行，等待用户复核。尚未提交红测、尚未实现任何功能。
> 行为真源：`PHASE2-THOUGHT-LAYER-2026-09-09.md`，2026-09-10 二次修订，225 行。

## 1. 阶段 A 已完成什么

- 已完整重读二次修订规格，将五项补定视为已定规则，不再列为开放项。
- 核对基线 `main @ cea382e Release DigestDock 2.0.0`，建立本地分支 `codex/thought-layer-phase2`。当前 HEAD 仍为 `cea382e`。
- 建分支时普通沙箱不能写 `.git/index.lock`；按用户已授权的分支创建动作申请执行权限后成功。没有提交、推送、合并或发布。
- 基线 `npm test`：809 项通过，失败/取消/跳过均为 0，退出码 0。
- 基线 `npm run check`：809 项通过，47 个发布白名单文件检查通过，退出码 0。
- 已聚焦检查捕获、索引/存储、侧栏列表、统一 provider 调用和回跳路径；没有修改运行时代码。
- 已更新实施清单的筛选恢复、本地时区、清空不变式、大库截断说明和四次提交要求。

## 2. Chrome 只读核对结果

| 安装项 | 状态与显示版本 | 实际加载来源 |
| --- | --- | --- |
| `jfjiohpdeohjiopbeapdjmaelkiejcen` | 已启用，1.4.8 | `~/文稿/061-DigestDock/worktrees/notes-trust-phase0` |
| `lfcndjcppebmjgalcfkmmgohkelakpfk` | 已停用，1.4.8 | `~/文稿/061-DigestDock` |

Chrome 浏览器通道可列出标签，但不能接管 `chrome://extensions/`；通过 Chrome 原生界面读取详情完成核对。现有标签中有 YouTube 和 Bilibili 视频；本轮尚未播放或操作它们进行 T1–T10 验收。

没有重新加载、启用、停用、移除任何扩展，没有修改扩展存储，没有进入或改动旧工作树。根目录文件版本为 2.0.0，并不代表 Chrome 已加载该版本。正式真机验收前必须明确对准本地实施分支；受保护旧工作树不能作为本轮验收结果。

## 3. 本次请复核的代码范围

| 文件 | 行数 | 内容 |
| --- | --- | --- |
| `tests/thought-layer.test.js` | 299 | T1–T10 及其边界断言，共 40 项可执行检查 |
| `tests/helpers/thought-layer-harness.js` | 252 | 离线 DOM/计时/Chrome API/存储/HTTP 桩；原样加载 content、sidepanel、background 生产脚本 |

共 2 个新增测试文件、551 行。现有已跟踪文件没有 diff，没有已暂存文件。根目录规格、旧交接、实施清单与本说明均不进入代码提交。

测试桩没有实现想法编辑、搜索、最近排序或 AI 候选选择；那些行为必须由后续生产代码提供。没有新增依赖、修改 package/manifest、改动字幕/概览/导出模块。

内部测试约定：搜索入口采用 `input[type="search"]`；新视图卡片提供 `data-note-id`；日期组提供 `data-date`；想法和触发材料分别用 `.note-thought` / `.note-trigger`；编辑/保存分别用 `.note-edit-thought` / `.note-save-thought`；后台想法更新消息暂定 `updateNoteThought`。这些是可调整的实现连接点，不是额外产品设计。

## 4. 运行结果：当前红是预期，不是交付通过

| 运行 | 通过 | 失败 | 取消/跳过 | 退出码 |
| --- | --- | --- | --- | --- |
| 基线 `npm test` | 809 | 0 | 0 / 0 | 0 |
| 基线 `npm run check` | 809 | 0 | 0 / 0 | 0 |
| `node --test tests/thought-layer.test.js` | 3 | 37 | 0 / 0 | 1 |
| 加红测后的 `npm test` | 812 | 37 | 0 / 0 | 1 |
| 加红测后的 `npm run check` | 812 | 37 | 0 / 0 | 1 |

全量 849 项 = 原有 809 项 + 新增 40 项；原有测试保持通过。加红测后 check 在测试门禁停止，没有完成后续全部检查，不能写“check 通过”。

新增通过的三项：

1. 测试桩正向对照：现有当前视频渲染可执行；默认翻译路径实际进入统一 provider 层并到达离线 HTTP 桩。
2. T1：第一次 N 已能保存一条空想法笔记，不暂停，保留原捕获偏移与字幕锚点。
3. T8 保护项：当前视频默认、来源分组与视频内时间顺序保持原状。

37 项失败均是断言失败，没有语法错误、ReferenceError、TypeError、未处理 Promise 拒绝、取消或跳过。失败主要位于：toast 仍是 5 秒；第二次 N 没有输入框；没有搜索入口；全部笔记没有日期分组；想法更新消息尚未实现；不可达回跳尚无冻结窗口降级提示。

部分用例会先被缺少搜索框/输入框等前置行为拦住，因此该用例后面的断言已写出但尚未跑到；这里不把“写有断言”称为“目标行为已验证”。后续逐步实现后必须让每个断言实际执行并通过。

## 5. T1–T10 对应的检查内容

| 编号 | 本次已经写出的断言 |
| --- | --- |
| T1 | 一次 N 只存一条、空 thought/null thoughtAt、视频不暂停、捕获 -3 秒和原字幕锚点不变；toast 应保持到 10 秒 |
| T2 | 原 toast 就地变输入框并聚焦、视频暂停、没有第二条笔记、超过 10 秒仍不关闭 |
| T3 | Enter 原文落库及 thoughtAt、关闭且不恢复播放；中文组合输入期 Enter 不提交；写入失败保留草稿 |
| T4 | Esc 不写草稿、不删原笔记、笔记库不变 |
| T5 | 从当前/全部两侧都能搜到其他视频的想法；清空恢复原侧；想法在触发原句/窗口上；想法优先、组内 savedAt 倒序；标题/频道可搜；迟到查询不覆盖新查询 |
| T6 | 只在原字幕出现的词可以命中含同音错字的想法；不引入模糊匹配 |
| T7 | 零结果按钮；不点击时 provider/HTTP 均为 0；有效 ID 去重、上限 5、只展示本地内容；库快照字节一致且无写操作；大库最新优先并明示 N；无效回复丢弃；失败不自动重试；迟到回复/已删除候选不展示 |
| T8 | 全部笔记本地自然日倒序、跳过空日、历史空想法正常显示、无置顶/确认计数/缺失提示；当前默认与列表不变；洛杉矶和上海两个时区的跨午夜边界 |
| T9 | 补写、修改、清空；空 thought 与 null thoughtAt 同步；原字段与创建日期不变；索引即时更新、可搜新词；不新增任何字段；并发保留、删除后不复活、过期数据重置标识不允许写入 |
| T10 | 搜索/最近两个入口 × 当前视频/跨 YouTube/Bilibili，共六种路由；对应来源和秒数；无法打开时显示冻结材料与保守原因 |

T10 自动化只证明消息、导航意图与界面状态。真正打开视频、能播放、定位到对应秒仍需后续真实 Chrome 分别核验，本次没有声称完成。

## 6. T7 的关键证据设计

- sidepanel 的运行时消息进入真实 background 消息处理器；不把新搜索处理器或现有 `translateNotes` 替换成固定成功结果。
- 同时记录 `requestAiCompletion` 统一入口和底层 HTTP 调用。正向对照确实捕获到原自动翻译路径的请求，排除探针没有接上的假零结果。
- 只在离线桩提供测试配置和 provider 响应；没有读取真实 API Key、没有向外部 provider 发请求或花费额度。
- 零请求检查从已经稳定完成的初始笔记加载后开始，随后驱动真实搜索输入事件；搜索路径的全局加载若触发 `ensureNotesChinese()`，会被记录并失败。
- 库快照覆盖笔记分片、索引、schema 与存在的旧笔记键，递归固定键序后比较 UTF-8 字节；另检查 set/remove/clear，防止先写后还原。
- 候选展示必须从本地现有 ID 取内容，夹带 AI 改写对象或不存在 ID 都不能进入展示。
- 正常加载笔记的翻译正向对照保留，防止为了 T7 把整个功能的翻译一刀切关闭。

## 7. 复现命令与本地日志

```sh
node --test tests/thought-layer.test.js
npm test
npm run check
```

执行目录：`/Users/wangchao/Documents/061-DigestDock`。

- 基线测试：`/private/tmp/digestdock-phase2-baseline-test.log`
- 基线 check：`/private/tmp/digestdock-phase2-baseline-check.log`
- 专项红测：`/private/tmp/digestdock-phase2-red.log`
- 含红测的全量测试：`/private/tmp/digestdock-phase2-with-red-test.log`
- 含红测的 check：`/private/tmp/digestdock-phase2-with-red-check.log`

日志为本机临时证据，不提交；本文已记录核心数字便于转交。

## 8. 复核后才执行的提交顺序

1. 红测：用户先复核，收到通过反馈后提交。
2. 捕获与存储：T1–T4、T9 写入侧。
3. 检索与「最近」：T5、T6、T8；T9 最近内编辑入口随视图接入，存储契约由上一份负责。
4. AI 兜底与回跳：T7、T10。

三个实现提交各自独立对到验收条目，提交前分别复核。不 push、不 merge、不 release。

当前等待的是用户对红测的复核；不是请求扩大范围，也没有把五项已补定事项重新变为开放问题。
