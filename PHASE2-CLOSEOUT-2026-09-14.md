# 阶段 2 想法层：最终收尾记录

> **2026-09-15 接续：** 下方 §1–5 是 `a789af7` 收尾时的记录；§6 是修复及重载前状态。§7 记录重载后验收与测试数据清理，§8 记录用户授权后的主线合并及 GitHub 3.0.0 正式发布。

> 日期：2026-09-14（America/Los_Angeles）。
> 状态：实现与自动化门禁已有通过证据；本轮整理判据和历史记录，用户人工验收待完成。
> 功能基线：`codex/notes-presentation @ 8a07174`，相对 `main @ cea382e` 是 **12 条新增提交**，不是 13 条。原检查时已跟踪文件无改动，另有 10 个未跟踪 Markdown 文件。
> 本轮变更：文档整理与一处代码注释，不改变功能、不重排既有提交。此记录随本轮本地提交归档，不能把它当作合并或发布记录。

## 1. 最终判断

保留当前功能和提交顺序。已有证据没有给出必须拆分或回退上述功能的理由；自动化通过也不等于所有网站状态或用户体验都已验收。

采纳的修整：

- 将原规格与后续要求的差异明确写入 [规格 §10](PHASE2-THOUGHT-LAYER-2026-09-09.md#10-2026-09-14-收尾修订现行判据)。保留 §1–9 原文，避免把后来调整伪装成最初要求。
- 将阶段 2 原规格、上位方案与相关阶段复核/交付记录纳入本地版本管理。旧委托要求未跟踪规格不要提交，因此历史上未纳入 Git 不能归因于执行方忘记 `add`；本轮按用户授权调整归档方式。
- 将 `background.js` 的字幕清理注释从 “saved thought” 改为 “cleaned quote”，避免与用户 `thought` 字段混淆；执行逻辑不变。
- 另列 [人工验收清单](PHASE2-MANUAL-ACCEPTANCE-2026-09-14.md)，结果初始均为“未验证”。

不采纳的定性：

- 不能把后续五条提交全部视为原阶段 2 范围失控；其中混合了缺陷修复与后续交互调整。
- 记住“按日期 / 按视频”是排列偏好，不等于保存搜索条件。
- 旧规格已允许清空想法；新增的是独立删除入口和它的临时确认框，不是“已确认/待确认”持久字段。
- “944 全绿”只能证明已编码场景通过，不能证明完全无回归；“错的全是规格、执行没有问题”也超出本轮证据。

## 2. 后续提交与现行要求

`5a589d5` 之前的七条提交及当时浏览器检查保留在 [9 月 10 日交付记录](PHASE2-DELIVERY-2026-09-10.md)。之后的五条如下：

| 提交 | 最终行为 | 分类与验收落点 |
| --- | --- | --- |
| `a2e3d7d` | 换视频时释放旧字幕状态；广告时不以广告时钟推进正片字幕 | 字幕跟随修复；有广告时的条件检查 |
| `4d38366` | 各视图共用一致卡片；日期/视频两种排列并记住选择 | 卡片修复与后续排列交互；人工检查呈现、排序、重开记忆 |
| `b1d4899` | 搜索覆盖保存内容；上下文折叠；独立删除确认；工具栏和导出标签调整 | 搜索一致性与后续交互；人工检查搜索、删除取消、窄栏与导出范围 |
| `ab2745c` | 同句重复捕获复用原笔记，直接打开既有想法，保留历史内容 | 捕获去重与找回交互；人工检查重复记录数量与旧文本 |
| `8a07174` | 笔记回跳后准备缓存/免费原生字幕，保留笔记页 | 回跳后的关联恢复；人工检查定位、字幕及当前页保持 |

本轮只作这些已经存在的行为与判据的对齐。字幕范围例外限于关联跟随和恢复，不是新增来源、改抓取架构或自动付费的授权。更具体的规则和未变硬边界见规格 §10。

## 3. 验证状态

| 证据 | 当前可确认的结论 |
| --- | --- |
| 本轮修整前 `8a07174` 的 `npm test` | 944 / 944 通过，失败/取消/跳过均为 0，退出码 0 |
| 本轮修整前 `8a07174` 的 `npm run check` | 944 / 944 通过；47 个白名单文件检查通过，退出码 0 |
| 本轮文档与注释修整后 `npm test` | 944 / 944 通过，失败/取消/跳过均为 0，退出码 0 |
| 本轮文档与注释修整后 `npm run check` | 944 / 944 通过；47 个白名单文件检查通过，退出码 0 |
| 本轮静态核对 | 文档本地链接目标全部存在；8 份历史正文逐字保留；`background.js` 确认只有注释替换；差异空白检查通过 |
| 历史真实 Chrome 记录 | 只适用于原记录标明的版本、实例和样本，保留原结果；不据此宣布最新功能已完整验收 |
| 本轮实际 Chrome 操作 | 未执行；人工检查前需确认加载来源并重新加载扩展和测试页面 |
| 用户个人验收 | 未验证，按人工清单逐项记录 |
| 同步/发布 | 本轮未执行 push、merge、release |

`npm run check` 会再次运行自动化测试，不能把两次 944 相加成覆盖数量。本轮不增加镜像实现的测试；文档链接、历史正文保留和注释差异另作静态检查。

## 4. 文档入口与归档边界

| 文档 | 使用方式 |
| --- | --- |
| [DESIGN.md](DESIGN.md) 的“笔记”部分 | 当前产品呈现与交互真源 |
| [原规格与 §10 修订](PHASE2-THOUGHT-LAYER-2026-09-09.md) | 原 T1–T10 + 后续行为的明确修订；其他硬边界保留 |
| [人工验收清单](PHASE2-MANUAL-ACCEPTANCE-2026-09-14.md) | 用户操作、通过标准、失败现象与未测项 |
| [笔记库 MVP 上位方案](NOTES-LIBRARY-MVP-PLAN-2026-09-04.md) | 阶段 2 的历史需求来源；保留当时判断与日期，不当成当前全项目状态 |
| [原委托](PHASE2-CODEX-HANDOFF-2026-09-10.md)、[实施清单](PHASE2-IMPLEMENTATION-REVIEW-2026-09-10.md) | 历史执行边界，不自动触发旧模型路由或提交流程 |
| [红测](PHASE2-RED-TEST-REVIEW-2026-09-10.md)、[捕获与存储](PHASE2-CAPTURE-STORAGE-REVIEW-2026-09-10.md)、[B 站对齐](PHASE2-BILIBILI-CAPTURE-REVIEW-2026-09-10.md)、[原交付](PHASE2-DELIVERY-2026-09-10.md) | 历史证据：原始通过/失败数字和对应版本保持原样 |

本次归档原有文档共 8 份（7 份阶段 2 文档及其上位方案），另新增本记录和人工清单。原有 `NOTE-CLEANUP-QUALITY-2026-09-08.md` 与 `PHASE1-MIGRATION-SPEC-2026-09-05 (1).md` 属于其他阶段，保持原状。Git 跟踪可保留版本，但在没有额外同步的情况下不等于已有异地备份；这两份新记录不加入扩展发布包白名单。

## 5. 下一步

从 [人工验收清单](PHASE2-MANUAL-ACCEPTANCE-2026-09-14.md) 开始。用户可按编号回复“过 / 失败现象 / 未测”；只有获得实际结果的项目才能改为通过。若有失败，只处理对应行为并复测受影响项。用户验收和后续发布分别决定。

## 6. 2026-09-15 用户反馈修复与代理验收

### 本次修复

- **中文轨优先：** 先读取当前页面轨证据，再允许 Passive 成功；存在中文目标时英文结果不能提前返回。简体、繁体、粤语等中文语族等价，人工/自动类型分开。侧栏同视频内存和持久缓存采用相同判据，晚到的中文信息可替换旧英文，同类有效中文不会重复加载。
- **保存与取消续播：** 输入时仍暂停，成功保存或 Esc 取消后恢复进入前的播放状态；原本暂停和保存失败时不启动播放。检查视频/分 P、播放器和广告归属，防止迟到回应误播。
- **完整内容的重复捕获：** 原上下文提取与清理规则保持不变。已存完整正文必须在新旧冻结字幕中唯一对应同一连续段落，并覆盖两个捕获点，才跨字幕行复用；只忽略自然语言排版差异，不丢弃数字、正负号和百分比。保留原单点时间码、回跳锚点和想法，写队列内再次判定并发；不自动整理或删除历史重复项。

已有通过结果按用户要求保留。清单 4、6、7、9 的操作由 Codex 负责，不能再次整体交回用户。

### 本次验证

| 检查 | 结果与边界 |
| --- | --- |
| `npm test` | **967 / 967 通过**，失败/取消/跳过均为 0，退出码 0 |
| `npm run check` | **967 / 967 通过，47 个白名单文件检查通过**，退出码 0 |
| 差异检查 | `git diff --check` 通过 |
| 同段捕获定向 | 30 / 30 通过，含真实结构的中文长句、标点整理、超过原 `after2` 的尾部字幕、数字/符号、首尾边界、同词异位、冲突与并发；同段重复不新增 provider 调用 |
| 保存/取消定向 | 想法层 71 / 71 通过，含成功后续播、原本暂停、失败保留及导航不误播 |
| 中文轨定向 | 字幕流/API/缓存 113 / 113，通过简繁、人工优先、无中文时英文可用、晚到信息及同视频稳定性；全套门禁同时覆盖既有跟随/跳转测试 |
| 用户验收 | 用户报告 YouTube **3、5、8 通过**。跟随/跳转的既有通过结果继续保留；本轮未修改 `playNote`、`seekTo` 或广告跟随处理器 |
| Chrome 6 | 从截图视频的当前列表，搜索另一视频想法“扭扭捏捏”和原句“翼龙”；清空后回到原来的 4 条当前视频笔记，通过 |
| Chrome 7 | 查询“在脑中预演危险以决定行动”先为零结果，点击一次“让 AI 找找”后返回 5 条已有候选。此次真实 UI 调用成功；服务未向测试入口暴露实际模型/档位，不能用本地配置冒充上游回执。字节级不变与非法 ID 处理另有自动化证据 |
| Chrome 9 | 实际生成 `陈一发儿：我是怎么没的？-notes-original-2026-09-15.txt`（2100 字节），含正确 `XVOhygutivE` URL、00:23/00:40/00:43/00:44 四条原文，无其他视频内容，通过。未执行补资料/补译路径 |
| Chrome 4 | 三视图正文、上下文展开和三种语言已操作；当前宽度下目视检查可用，更窄宽度的最终截图被窗口切换中断，由 Codex 完成剩余检查 |
| 新修复的 Chrome 复测 | 待用户重载扩展一次后由 Codex 继续；当前运行实例 ID 已观察，加载目录未通过扩展管理页实时核实。工具对此管理页返回明确安全策略阻止，未尝试绕过 |

### 验收边界与下一步

浏览器中 4、6、7、9 的已验证交互属于未改动的卡片/检索/导出功能，可以保留；不能据此宣称新加的中文轨、续播或跨行判重代码已在 Chrome 加载并通过。用户只需协助重载当前项目目录的扩展，之后的新代码复测仍由 Codex 执行。未改动且已经通过的项目不要求用户重测。

## 7. 用户重载后的 Chrome 复测

用户回复“已重载”后，Codex 刷新保留的测试页并打开侧栏。源码基线 `19e7c19`，运行实例为 `lfcndjcppebmjgalcfkmmgohkelakpfk`，测试视频为 `https://www.youtube.com/watch?v=XVOhygutivE`。

### 已确认的结果

- **中文优先通过。** 页面实际轨列表包含 `en-US/manual` 与 `zh-Hans/manual`；侧栏显示 `原文（zh-hans）`，实际截图可见中文段落。未把本次简体样本冒称繁体也做了相同视频实测；繁体边界已有本版自动化覆盖。
- **保存续播通过。** 页面实际捕获入口打开想法框时播放器暂停。用真实 textarea 保存 `DDK复测：保存后继续播放。` 后，输入框关闭，`paused=false`。读回写入的是本轮新记录 `note_18f1b7d0-d7f0-493f-bfc1-239fe8b5ae51`，原锚点 281 秒保留。
- **同段重复通过。** 完整正文为“2014年的时候，在设计院里没项目，我闲着无聊就在斗鱼开了直播。”；原始字幕点为 281.67、283.25、284.83 秒。在 287.2、289.2 秒受控定位后，通过页面实际金句按钮进入捕获（沿用 3 秒反应补偿），后两行均打开相同笔记的想法框。记录数保持 7，未额外创建笔记，时间码仍为 04:41。
- **取消续播通过。** 找回上述想法后输入另一段草稿，使用真实 textarea 的 Escape 取消。输入框关闭、`paused=false`；读回原测试想法逐字相同，未存入草稿。
- **窄栏排版通过。** Chrome 原生分隔线的工具拖动未改变宽度，因此没有把该尝试当作通过。另由生产 `renderNotes` 输出脱敏 DOM，配合同版 `sidepanel.css`、`ui-themes.css` 和字体，在隐藏 in-app Chromium 中实际渲染 320px 宽度：body 的 clientWidth/scrollWidth 均为 320，横向溢出 0、可见越界元素 0。标题、筛选、导出、搜索和日期/视频排列控件不重叠；长中英文本、连续英文长词与展开上下文可换行。内容区 `overflow-y:auto`，640px 容器的 scrollHeight 为 1798，可纵向滚动。这里验证的是生产布局，交互结果采用前述真实扩展检查；未声称原生侧栏已被拖到 320px。

为了准确区分触发点，定位与只读结果核对通过测试页的 CDP 完成；实际输入与 Enter/Escape 通过页面控件执行。没有替换生产捕获、保存、复用或播放器处理函数。早期按键接口存在超时/未聚焦，未将那些返回当作功能失败或通过；最终以存储读回、输入框状态和播放器状态为准。

### 测试数据与剩余检查

初始当前视频 4 条笔记，测试期间新增 3 条；重试前均先核对实际结果，后续改用已创建记录完成测试。原 4 条笔记未编辑。新增记录为：

| 时间码 | ID | 内容识别 |
| --- | --- | --- |
| 04:01 | `note_23228d30-93a9-4e4f-976b-c974278b732e` | 25 岁之后走一步看一步 |
| 04:39 | `note_26e2ddd6-6f4e-46a6-b8a4-5696d6bbd40e` | 主播十周年与延迟退休 |
| 04:41 | `note_18f1b7d0-d7f0-493f-bfc1-239fe8b5ae51` | 2014 年开直播；有 DDK 复测标记 |

只对这 3 个新 ID 请求清理确认。扩展删除没有回收站，未在确认前删除任何记录。窄宽度检查的临时服务与标签均已关闭，复核静态页保留在 `/private/tmp/ddk-narrow-qa.k5mSae/index.html`；它是测试证据，不是交付给用户运行的产品。此轮没有生产源码变更，已通过的 967 项测试不因只写验收记录重复执行。

本轮必要复测已完成。用户已通过的项目保持通过，其余由 Codex 补齐；无需再让用户重复整张清单。

### 测试数据已清理

用户随后明确回复“清理”。删除前重新核对以上 3 个 ID 的视频、时间、原句和测试想法，全部与待清理对象一致；通过扩展既有 `deleteNote` 接口逐一删除，使用当前数据版本与运行实例校验。删除后 3 个 ID 均不存在，`XVOhygutivE` 恢复为原有 4 条笔记（00:23、00:40、00:43、00:44），已有想法保留。

清理期间另一个视频 `iLlrIi9-NfQ` 新增了笔记，因此整库数量从 29 变成 27，静态整库相等检查未成立；不能据此宣称整库逐字节不变。实际删除请求只包含上表 3 个测试 ID，未删除或回写其他视频的新记录。测试数据清理完成，不影响已通过的验收结论。

## 8. 主线合并与 GitHub 3.0.0 正式发布

用户明确要求“合并主线 并发布 GitHub，新的大版本号”后，将版本从 2.0.0 升至 **3.0.0**。版本声明、设置页版本、对应测试桩同步更新；中英文 README 增补想法层操作说明，隐私说明补充可选 AI 检索的数据边界。没有新增安装权限或修改备份格式。

- **本地主线：** `main` 已快进合入 `codex/notes-presentation`，3.0.0 版本提交为 `0a76a663b126268482e64e91793290b047b0ab95`。本记录的后续保存仅增加发布回执，不改变产品文件。
- **GitHub 公开主线：** `Dep-0302/digest-dock` 的 `main`、`codex/public-release-3.0.0`、`v3.0.0` 解引用均核对为 `2325823518f5a7bd6004eff40a376a4114e52ddf`。
- **发布边界：** 沿用独立公开发布树，公开提交直接继承 `eeb34b8`；内部阶段文档与实验历史保留在本地主线。47 个产品安装文件与本地主线逐字节一致。
- **正式 Release：** [DigestDock 3.0.0](https://github.com/Dep-0302/digest-dock/releases/tag/v3.0.0)，非草稿、非预发布，已被 GitHub 标为 Latest；发布于 2026-09-15 12:17:46 UTC。

| 验证 | 结果 |
| --- | --- |
| 本地完整代码门禁 | 967 / 967 通过，47 文件检查通过 |
| 公开代码门禁 | 920 / 920 通过，47 文件检查通过；公开树沿用既有规则，不包含内部 Panel 实验测试 |
| 发布分支 CI | [34967635850](https://github.com/Dep-0302/digest-dock/actions/runs/34967635850)，success |
| GitHub main CI | [34967786453](https://github.com/Dep-0302/digest-dock/actions/runs/34967786453)，success |
| v3.0.0 标签 CI | [34967881320](https://github.com/Dep-0302/digest-dock/actions/runs/34967881320)，success |
| 线上安装包回读 | 下载回读与公开构建 ZIP 完全一致；47 文件，ZIP 完整性通过，manifest 版本 3.0.0 |

安装包：[`digest-dock-v3.0.0.zip`](https://github.com/Dep-0302/digest-dock/releases/download/v3.0.0/digest-dock-v3.0.0.zip)，451642 字节。

SHA-256：`d6e6aeb1152bad54a05f062a3b3ad6a24067e26689c1a1279260b04958dc0051`。

本次发布为 GitHub 仓库与 Release，不是 Chrome Web Store 上架。功能验收继续采用 §7 的已完成结果；当前目录已是 3.0.0，Chrome 重新加载扩展后读取新版版本声明。
