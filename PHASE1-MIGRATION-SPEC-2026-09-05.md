# 阶段 1 迁移规格：存储模型重置 + 补齐三层

> 文档状态：阶段 1 已验收
> 起草：2026-09-05
> 基线：`codex/notes-trust-phase0` @ `07eb21b`（阶段 0 已验收，未 push / 未 merge）
> 上位约束：`NOTES-LIBRARY-MVP-PLAN-2026-09-04.md` §4.1「迁移提交只做迁移」
> 方向依据：同文档 §2 核心链路、§3 三层模型

本文是**实现规格**，不是方向讨论。所有设计判断已在此定死，实现方不需要再做取舍；遇到本文没写到的情况，停下来问，不要自行决定。

---

## 1. 职责划分

### 1.1 Claude（规格与边界）

- 写本规格，并对其中每一个设计判断负责
- 提交前按 §4.1 复核 diff：范围、字段完整性、回滚路径、验收覆盖
- 不写实现代码，不执行迁移，不做真机验收（无真实浏览器与本地 git 写权限）

### 1.2 Codex（实现与验收）

- 按本规格实现，**不新增本规格未列出的字段、键、函数或行为**
- 补自动化测试至本规格 §2 全覆盖
- 在真实 Chrome 执行 §2 验收矩阵，逐条记录结果
- 提交（不 push、不 merge、不 release）

### 1.3 交接点

| 时点 | 谁 | 产出 |
| --- | --- | --- |
| 开工前 | Codex | 英文正向路径补验（阶段 0 遗留，见 §7.1） |
| 实现中发现规格未覆盖的情况 | Codex | 默认：停下来问。**一次性模式下见 §1.5** |
| 实现中发现无关缺陷 | Codex | **只记录到 §7.2 待办，不在本提交内修** |
| 提交前 | Claude | diff 复核，出具通过 / 拆分意见 |
| 提交后 | Codex | 更新本文档验收结果 |

### 1.5 一次性模式（额度受限时启用）

无法做多轮往返时，「停下来问」会浪费整轮额度。此时改为：

1. **取最保守的解释**——保留更多数据、改动更少代码、更接近现状的那一个
2. 把该判断写进 §7.4，格式：`情况 / 我选了什么 / 保守在哪 / 另一个选项是什么`
3. 继续执行，不停

§7.4 是复核时的第一优先级。这条**不放宽**「不新增规格未列的行为」——遇到规格没写的**功能**，一律不做，记进 §7.2。它只适用于规格没写清的**实现细节**。

**分阶段提交**，不要攒成一个：

| 提交 | 内容 |
| --- | --- |
| C1 | §2 六条验收矩阵的自动化测试（此时应为红） |
| C2 | 访问层 + 迁移实现（测试转绿 + 门禁通过） |
| C3 | 真机验收结果写回 §7.3 |

额度不足时在提交边界停，把状态写进 §7.5，不要停在半截实现上。

### 1.4 唯一的仲裁规则

本规格与实现冲突时，以本规格为准；本规格与 `NOTES-LIBRARY-MVP-PLAN-2026-09-04.md` §4.1 冲突时，以 §4.1 为准。

---

## 2. 验收矩阵（先于代码成立）

**这张表定义本次提交的范围。不在表上的行为，不属于本次提交。**

实现前先把这张表变成自动化测试；真机验收再走一遍同样的六行。

| # | 场景 | 前置状态 | 预期结果 |
| --- | --- | --- | --- |
| M1 | 空库迁移 | 无 `ytd_notes` | 建立空索引与 schema 标记；不触发备份下载；侧栏正常显示空态 |
| M2 | 单条迁移 | `ytd_notes` 1 条 | 备份先下载；1 条完整出现在新结构；21 个字段逐一相等；侧栏与 TXT 导出内容不变 |
| M3 | 500 条多媒体库迁移 | `ytd_notes` 500 条、跨 3 个以上 mediaKey | 备份先下载；500 条全部迁移；分片数 = mediaKey 去重数；不出现固定条数满额状态；在 32 MiB 容量护栏内可继续保存 |
| M4 | 历史 500 条以上库迁移 | `ytd_notes` 620 条 | **全部迁移，一条不丢**；不因条数截断；迁移后不显示固定条数上限；只要结果仍在 32 MiB 容量护栏内，第 621 条可以保存；超过容量护栏时原子拒绝且旧笔记不变 |
| M5 | 损坏数据 | `ytd_notes` 含非数组 / 缺 id / 缺 mediaKey 与 videoId / 字段类型错误的条目 | 迁移整体失败并回滚；`ytd_notes` 原样保留；侧栏显示明确失败原因与「导出备份」入口；不产生半迁移状态 |
| M6 | 迁移中断后重启 | M3 的库，在写入分片过程中强制终止 service worker | 重启后检测到未完成状态；从头重做或安全续做；**任何时刻 `ytd_notes` 与新结构至少有一份是完整的**；最终结果与 M3 相同 |

补充断言，适用于 M2–M4：

- 迁移前后各做一次笔记备份导出，两份 JSON 规范化后**逐字节相同**
- 迁移后导入一份迁移前导出的备份，结果与迁移后状态一致（幂等）

---

## 3. 目标结构

### 3.1 键

| 键 | 类型 | 内容 |
| --- | --- | --- |
| `ytd_notes_schema` | number | 存储结构版本号，本次为 `1` |
| `ytd_note_index` | array | 全部笔记的检索索引，见 §3.2 |
| `ytd_notes_<mediaKey>` | array | 该媒体下的完整笔记记录，按时间码升序 |
| `ytd_notes` | array | **保留不动**，见 §4.4 |

`<mediaKey>` 直接取记录的 `mediaKey`；`mediaKey` 为空时取 `videoId`；两者皆空的记录判为损坏数据（走 M5）。

### 3.2 索引条目

每条笔记在 `ytd_note_index` 中恰好一项，字段固定为以下 9 个，不多不少：

```js
{
  id,                  // 笔记 id，与分片内记录一一对应
  mediaKey,            // 分片键，用于定位所在分片
  platform,            // "youtube" | "bilibili"
  videoTitle,          // 供检索与结果分组显示
  channelName,
  timestampSeconds,
  savedAt,             // = 记录的 createdAt；时间轴入口依赖此字段
  hasThought,          // boolean，本阶段全部为 false
  searchText,          // 规范化检索副本，见下
}
```

`searchText` = `[thought, rawText, videoTitle, channelName]` 去空后以单空格连接，再经 `String.normalize("NFKC").trim().replace(/\s+/g, " ")`。本阶段 `thought` 恒为空，故实际等于后三项。

索引只用于检索与定位，**不是真源**。任何正文渲染必须回分片读取。

### 3.3 记录字段

分片内的记录保留当前全部 21 个字段，逐字段原样搬运，不做规范化、不做裁剪、不做补默认值：

`id`、`videoId`、`mediaKey`、`platform`、`canonicalUrl`、`bvid`、`cid`、`page`、`videoTitle`、`channelName`、`timestamp`、`timestampSeconds`、`timestampedUrl`、`text`、`translatedText`、`translatedValidated`、`translatedValidationVersion`、`translatedUnchanged`、`rawText`、`sourceLanguage`、`textLanguage`、`createdAt`

新增三个字段，**仅对本次提交之后新保存的笔记写入**，历史笔记留空：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `thought` | string | 用户自己写的文字。本阶段**不提供任何写入入口**，只建字段。默认 `""` |
| `thoughtAt` | number \| null | 想法写入时间。与 `createdAt` 分开——后补想法的时间不同于锚点建立时间。默认 `null` |
| `triggerWindow` | array | 触发点前后字幕，见 §3.4。默认 `[]` |

历史笔记的 `triggerWindow` 无法补回（当时未持久化），这是已接受的损失，必须在迁移说明中写明。

### 3.4 triggerWindow

`background.js` 在保存笔记时已经收集 `contextLines`（索引范围 `matchedIndex - 8` 到 `matchedIndex + 12`，共 21 行）喂给 `cleanupNoteText()`，随后丢弃。

本次在**同一个既有循环内**追加一次采集，得到：

```js
triggerWindow: [{ t: transcript[j].start, text: transcript[j].text }, ...]
```

硬性要求：

- 不改变 `contextLines` 本身，不改变喂给 `cleanupNoteText()` 的内容，不改变任何 AI 请求
- 不新增遍历、不新增网络请求、不改变窗口范围
- 整体序列化后超过 20 KB 时，从两端等量裁剪至 20 KB 以内，保证 `matchedIndex` 那一行始终保留

---

## 4. 迁移过程

### 4.1 触发时机

单一入口 `ensureNotesMigrated()`：

- 幂等、single-flight（并发调用共享同一个 Promise）
- §5 列出的每一个读写点在访问笔记前 `await` 它
- 不挂在 `onInstalled`——侧栏可能先于它发起读取

### 4.2 步骤

1. 读 `ytd_notes_schema`。已是 `1` 则立即返回。
2. 读 `ytd_notes`。不存在或为空数组 → 写入空索引与 `ytd_notes_schema = 1`，结束（对应 M1，不下载备份）。
3. **先下载一份备份**：复用 `YTD_NOTES_BACKUP.createBackup()` 与现有下载路径，文件名沿用 `notesBackupFilename()`。备份失败则整个迁移中止并保持原状。
4. 校验全部记录。任一条损坏 → 中止、回滚、报错（对应 M5）。
5. 写入全部 `ytd_notes_<mediaKey>` 分片。
6. 写入 `ytd_note_index`。
7. 写入 `ytd_notes_schema = 1`。

顺序不可调换：分片先于索引，索引先于版本号。任何一步失败，已写入的新键全部删除，`ytd_notes` 保持原样。

迁移完成后不再设置固定笔记条数上限。新保存与导入合并统一使用 `notes-backup.js` 的 32 MiB 可恢复备份字节上限作为容量护栏；任何会超过护栏的写入在改变存储前整体拒绝，已有笔记不删除。

### 4.3 中断安全

`ytd_notes_schema` 是唯一的完成标记。它写入之前，`ytd_notes` 是唯一真源；写入之后，新结构是唯一真源。中途重启永远落在这两个状态之一（对应 M6）。

### 4.4 旧键处理

`ytd_notes` **本次不删除**。保留一个版本周期，作为回滚依据。删除动作单独排期，不属于本提交。

### 4.5 备份文件格式不变

`notes-backup.js` 的 `SCHEMA_VERSION` 与备份文件结构**本次完全不动**。导出仍产出扁平数组，导入仍接受扁平数组，只是写入时落到新结构。

理由：备份文件是这次不可逆操作的唯一逃生通道，它必须在迁移前后都可用。迁移前导出的备份，迁移后必须能导入。

---

## 5. 访问层

当前 `ytd_notes` 有 15 个直接读写点：`background.js` 14 处、`sidepanel.js` 1 处。

全部改为经由一组访问函数，直接的 `chrome.storage.local.get("ytd_notes")` 在生产代码中归零：

```
readAllNotes()              → 全部笔记（合并分片，供导出与备份）
readNotesByMedia(mediaKey)  → 单个分片
readNoteIndex()             → 索引
appendNote(note)            → 写入分片 + 索引；取消固定条数上限，写入前执行 32 MiB 可恢复备份容量护栏
replaceNotes(notes)         → 整体替换（导入、清空用）
deleteNote(id)              → 删除记录 + 索引项 + 空分片
```

**这一层是本提交允许新增的唯一抽象。** 除迁移与这六个函数外，不新增其他架构。

---

## 6. 范围围栏

本提交**不做**：

- 不提供想法输入入口（字段建好，UI 属于阶段 2）
- 不做检索、不做搜索框、不做时间轴入口
- 不改笔记 Tab 的任何现有交互与视觉
- 不改导出、翻译、字幕获取、概览的任何行为
- 不改备份文件格式
- 不删 `ytd_notes`
- 不修迁移期间发现的无关缺陷（记入 §7.2）
- 不做性能优化，除非 M3 验收实测不达标

判据：如果一处改动无法对应到 §2 的某一行验收，它不属于本提交。

---

## 7. 附录

### 7.1 开工前置（阶段 0 遗留）

英文 YouTube 正向路径未在真实浏览器验证：取样视频原生 CC 不可用，只验证了失败关闭。非中文源正是「原文」缺陷所在的分支。

用一个确有原生英文 CC 的视频，在配置 AI Key 的情况下保存一条笔记，确认「原文」显示逐字字幕而非 AI 整理版。通过后再开工。

**执行结果（2026-09-05）：通过。**

- 测试扩展：`jfjiohpdeohjiopbeapdjmaelkiejcen`，来源 `worktrees/notes-trust-phase0`；日常版本在测试期间停用。
- 视频：BBC World Service `X1L1Hd3xfrU`。侧栏明确显示「来自视频字幕 · 原文（en）」并读出完整英文字幕。
- 在 03:51 保存笔记后，「原文」模式显示逐字 `rawText`：`in being able to think critically about the things that they're reading,`。导出的 schema v3 JSON 同样记录 `sourceLanguage: "en"`、`timestampSeconds: 231`，且 `rawText` 与上下文型 `text` 明确不同。
- 首次复用的日常 DeepSeek Key 被 Provider 判定无效，按阻塞规则未进入 C1；用户随后在测试扩展中设置并验证了有效 Key，原失败证据保留在本节恢复记录中，不把失败回退冒充成功。
- 有效 Key 下在 03:54 保存第二条残句笔记：`rawText` 为 `because that's fundamental to a democracy is our ability to analyse`；DeepSeek 成功生成独立完整的 `text`：`We need to support individuals in being able to think critically about the things that they're reading, because that's fundamental to a democracy: our ability to analyze and think deeply about the information that we're consuming.`，两者明确不同；同次 Provider 路径也成功生成中文译文。
- 切换笔记页到「原文」后，03:54 卡片仍只显示上述逐字 `rawText`，没有显示 AI 整理版；再次导出的 schema v3 JSON 同时保留两者，`sourceLanguage: "en"`、`timestampSeconds: 234` 正确。
- 结论：真实英文 CC、有效 AI Key、AI 整理成功、原文逐字保真四项均有真实 Chrome 证据，本项通过，可以开始 C1。

### 7.2 迁移期间发现的其他问题（只记录，不修）

<!-- Codex 在此追加，每条一行：现象 / 位置 / 复现条件 -->

- API Key 的「显示」开关保持开启时，Chrome 辅助功能树会向本机自动化暴露输入框明文 / `options.html#aiApiKey` / §7.1 真机复验切换到设置页时触发；已立即重新隐藏，Phase 1 不改该交互，测试结束后应在 Provider 平台轮换本次 Key。
- 阶段 1 验收后，用户在英文 YouTube、中文字幕 YouTube 与中文字幕 B 站真实使用中发现“按下保存时听到的内容”和最终笔记正文可能不一致 / 页面入口、`background.js` 选行与 `cleanupNoteText()`、YouTube 保存提示 / 量测确认三个即时保存入口都已固定回退 3 秒，原始 `rawText` 锚点只会落在点击之前；主要偏移来自 AI 清理窗口原为前 2 / 后 4、模型可改选邻近观点，以及英文 YouTube 提示显示 `text` 而“原文”卡片显示 `rawText`。后续纠正保持 `-3 秒` 与 `triggerWindow -8/+12` 不变，把 AI 主窗口改为前 4 / 后 2，增加 TARGET 保真失败关闭，并让英文提示明确显示“字幕原话”；最终体感等待用户复验。

### 7.4 一次性模式下的自主判断（复核第一优先级）

<!-- 格式：情况 / 选了什么 / 保守在哪 / 另一个选项 -->

- §1.4 指定以 `NOTES-LIBRARY-MVP-PLAN-2026-09-04.md` §4.1 仲裁，而该节明确要求阶段 1 取消固定条数上限；本规格原 M3、M4 与访问层说明误沿用了阶段 0 的 500 条合同 / 选择：移除 `MAX_NOTES`、新保存与导入合并的条数拒绝及相关 UI 文案，保持备份 schema、字段、文件名与现有 32 MiB 字节护栏不变；迁移、新保存、导入和笔记内容回写只在最终可恢复备份超过该护栏时原子拒绝 / 保守在复用已有护栏、不新增存储键、权限、协议字段或估算逻辑，同时继续保证失败不改旧数据 / 另一选项是按 `chrome.storage.local.getBytesInUse()` 新造实际占用护栏，但规格没有定义计入哪些键、阈值或提示方式，会扩大本提交行为。
- 用户只能按最终体感判断锚点，无法提供精确按键秒数；实测又证明原始 cue 已在点击前，不能凭感觉继续增加时间补偿 / 选择：保留页面与侧栏既有 `-3 秒`，保留 `triggerWindow -8/+12` 与 20 KiB 合同；只把 AI 主清理窗口由前 2 / 后 4 调整为前 4 / 后 2，强化提示词，并对丢失 TARGET、否定、数字、方向或改写成第三人称总结的结果失败关闭到逐字 TARGET；英文 YouTube 保存提示同步显示 `rawText` 并标注“字幕原话” / 保守在不移动时间锚点、不改存储字段、不增加网络请求，先解决已证实的正文漂移与展示错位 / 另一选项是加大或动态调整反应补偿，但当前没有足够用户时点证据，可能把正确锚点推得更早。
- §3.3 称“21 个字段”，显式清单实际有 22 个，现存记录还可能带 `videoTitleZh`、`videoTitleZhValidated`、`videoTitleZhValidationVersion`、`videoTitleZhSourceHash` / 选择：历史分片记录原样复制全部既有 own fields，不对白名单外既有字段做裁剪；索引仍严格只有九字段，新保存仍只新增规格列出的 `thought`、`thoughtAt`、`triggerWindow` / 保守在优先保留用户数据且不创造新语义 / 另一选项是只复制显式清单，会删除真实存在的标题译文字段。
- 迁移要求 service worker 在首个分片写入前沿用现有 Blob-anchor 下载，但 worker 无 DOM、manifest 无 `downloads` 权限且发布门禁禁止该 API / 选择：不新增 `downloads` 权限；复用已打开的扩展页面执行现有 Blob-anchor 下载并在收到成功确认后继续，无可响应扩展页面时迁移失败关闭、旧 `ytd_notes` 保持原样 / 保守在不扩大权限、不引入新下载架构且不冒险先写后备份 / 另一选项是新增 `downloads` 权限或离屏页面，均超出规格围栏。
- `ytd_notes_schema = 1` 但索引或分片损坏时规格未定义恢复 / 选择：明确失败，不从旧键自动猜测修复，也不静默重建 / 保守在避免两套真源混用 / 另一选项是自动回退或修复，会新增规格未列行为。
- 分片要求按时间码升序，但同时间码顺序和 `readAllNotes()` 的扁平顺序未定义 / 选择：索引保持旧扁平数组顺序以重建现有导出顺序；分片按 `timestampSeconds` 稳定升序，同值保留输入先后；新笔记索引仍置前 / 保守在迁移前后 UI 与备份顺序不变 / 另一选项是按分片键拼接，会破坏逐字节等价合同。
- 合法旧备份允许 YouTube `mediaKey = "schema"`，但按 §3.1 直接拼接后会与完成标记 `ytd_notes_schema` 确定性碰撞 / 选择：把该保留值判为损坏数据，迁移、导入及新写入全部失败关闭，旧真源不动 / 保守在绝不让分片覆盖版本号或让版本号覆盖分片 / 另一选项是编码分片键，会违反“直接取 mediaKey”并改变规格定义的键结构。
- 迁移完成后若全量重置插在等待迁移的笔记操作之前，旧操作继续读取会面对已经清空的新结构 / 选择：各有锁操作在进入写队列后复核捕获的 generation/fence，跨过重置的一律取消，不在锁内启动第二次迁移 / 保守在避免死锁和旧任务复活 / 另一选项是让旧操作在重置后自动重试，会扩大既有重置语义。
- `triggerWindow` 只剩 matched 行时该单行自身仍超过 20 KiB，规格的“整体不超过 20 KB”和“matched 行始终保留”无法同时靠移除行满足 / 选择：保留 matched 行及其 `t`，仅把该行 `text` 截到序列化后不超过 20 KiB；通常的多行窗口仍严格从两端交替裁剪 / 保守在同时保住触发锚点和硬容量上限 / 另一选项是超限保存或丢掉 matched 行，分别违反两个硬要求。
- 旧数据已成功下载迁移备份、但随后因字段或保留键冲突失败时，反复加载侧栏会不会再次下载未定义 / 选择：本次 service worker 生命周期内保留同一 rejected Promise，只允许“尚未下载成功”这一瞬时失败重新尝试；worker 重启后仍可从头重做 / 保守在避免一次确定性损坏触发无限重复下载，同时保留无页面接收备份时的恢复能力 / 另一选项是所有失败都立即清空 single-flight，会对同一损坏库反复下载。
- M5 要求显示「导出备份」入口，但 §4.5 又禁止改变备份格式，而缺 id、缺媒体身份等损坏数据无法通过现有 schema 生成合法备份 / 选择：显示并保留现有导出入口与明确失败原因，不新增“原始损坏 JSON”格式，也不绕过既有备份校验 / 保守在不伪造可恢复备份、不改变唯一逃生文件合同 / 另一选项是新增 raw dump，会直接扩大格式与行为范围。
- §4.4 的“`ytd_notes` 保留不动”和 §6 的“不删 `ytd_notes`”与用户在新结构里逐条删除或清空后的物理擦除语义有张力 / 选择：本阶段严格保持旧键不动，逐条删除与清空只改变 schema=1 的唯一真源；全量重置仍按既有语义清除全部 local storage / 保守在遵守本阶段明确回滚合同 / 另一选项是同步修改或删除旧键，违反本提交围栏，留待旧键清理的单独排期。
- 无 `downloads` 权限的 Blob-anchor 页面只能确认 `click()` 已派发，无法从 Web API 证明文件最终落盘；同一时刻多个 sidepanel 页面也可能同时收到定向广播 / 选择：后台要求页面回传与请求完全相同的非空文件名后才写新结构，C3 再核对下载目录中的实际文件、字节与可解析性；本提交不新增权限或页面寻址架构 / 保守在收紧可验证回执且不扩大权限 / 另一选项是 `chrome.downloads` 或离屏下载器，超出围栏。

### 7.5 中断时的状态

<!-- 额度不足停在哪个提交、下一步是什么 -->

- 2026-09-05：停在步骤 0（§7.1），尚未进入 C1；分支仍为 `codex/notes-trust-phase0 @ 07eb21b`，没有 Phase 1 提交、没有 push / merge / release。阻塞原因是唯一已配置的 DeepSeek Key 被 Provider 判定无效。测试扩展已重新加载并保留上述一条英文测试笔记，日常 DigestDock 已停用以维持单扩展隔离。恢复入口：用户在测试扩展或日常配置中更新一个有效 Provider Key 后，重新使用 `X1L1Hd3xfrU` 跑 §7.1；只有 Provider 整理成功且「原文」仍逐字显示 `rawText`，才开始 C1 红测提交。
- 2026-09-05：用户更新并验证有效 Key 后已从同一现场恢复；§7.1 复验通过，解除步骤 0 阻塞，开始 C1。此前没有产生半截实现或伪造提交。
- 2026-09-06：已完成并提交 C1 `f7b062a` 与 C2 `ecd07d0`；修后 `npm test` 为 763 / 763，`npm run check` 通过 47 个白名单文件。Claude 生产 diff 复核首轮提出阻断项，按证据修正兼容过滤、确定性失败重复下载、下载文件名回执及 Unicode 截断，并把与 §4.4 / §4.5 / §6 冲突的建议写入 §7.4；处置复核结论为“可以提交 C2”。尚未进入 C3 真机矩阵：准备重新加载测试扩展时发现 Mac 已锁屏，原生 Chrome 控制明确要求用户手动解锁；扩展页面又受浏览器 URL 安全策略禁止自动控制，不能绕过。当前停在干净的 C2 提交边界，没有伪造任何 M1–M6 真机结果，没有 push / merge / release。恢复入口：用户解锁 Mac 后重新加载 `jfjiohpdeohjiopbeapdjmaelkiejcen`，执行 §2 六行矩阵，写回 §7.3 并提交 C3。
- 2026-09-07：用户解锁 Mac 并要求继续后，从 C2 干净边界恢复；重新核对测试扩展 ID、加载路径与版本，完成 §2 的 M1–M6 真机矩阵。M6 在首个分片已落盘、迁移 Promise 仍悬空时调用 `chrome.runtime.reload()`，新 worker 中测试 helper 为 `undefined`，确认不是沿用旧执行上下文；重启后迁移完整收敛。测试期间原有 DeepSeek 设置被保留且密钥始终保持遮蔽。阶段 1 已完成，等待 C3 文档提交；仍未 push / merge / release。

### 7.3 验收结果

| # | 结果 | 日期 | 备注 |
| --- | --- | --- | --- |
| M1 | 通过 | 2026-09-07 | 清空 local 后触发首次读取：`schema=1`、空索引、0 分片、无旧键；Downloads 未新增备份；侧栏“全部笔记”显示全库空态。 |
| M2 | 通过 | 2026-09-07 | 1 条迁移为 1 索引/1 分片；显式 22 字段及 1 个额外 own field 逐值一致、历史记录无 Phase 1 三字段；规范化备份相同且重复导入 `changed=false`。真实备份 `636 B` 可解析；侧栏原文显示 `RAW:`，原文 TXT `263 B` 含 `RAW:`、不含 `CLEANED:`。 |
| M3 | 迁移通过；新容量合同自动化通过、真机待用户复验 | 2026-09-07 | 500 条跨 4 个 mediaKey 的原迁移证据保持有效；修正版自动化确认不返回或显示固定条数上限，第 501 条在 32 MiB 内保存成功且原 500 条不丢。原 `500 / 500` 满额断言已撤销。 |
| M4 | 迁移通过；新容量合同自动化通过、真机待用户复验 | 2026-09-07 | 620 条迁移一条不丢的原证据保持有效；修正版自动化确认第 621 条在 32 MiB 内保存、导入与导出成功，超过字节护栏时原子拒绝。原 `620 / 500` 与 `NOTE_STORAGE_FULL` 断言已撤销。 |
| M5 | 通过 | 2026-09-07 | 非数组、缺 mediaKey+videoId、timestampSeconds 类型错误、缺 id 四类均整体失败；旧键原样保留，0 个新结构键。重建侧栏后显示具体 id 失败原因及“导出备份”入口。 |
| M6 | 通过 | 2026-09-07 | 500 条/4 mediaKey：首分片 125 条写入后强制 reload；中断态为旧 500 条完整、1 分片、无索引/无 schema。新 worker 从头重做，45 ms 后得到索引 500、分片 4、字段/备份一致；中断前后两份真实 500 条备份均为 `241,757 B`。 |

原固定条数合同下的自动门禁：`npm test` 763 / 763；`npm run check` 47 个白名单文件。

容量合同与笔记提炼纠正后的自动门禁：`npm test` 771 / 771；`npm run check` 通过 47 个白名单文件。聚焦只读复核无阻断。M3 / M4 的新容量界面与三类视频的最终笔记体感等待用户在重新加载的真实 Chrome 中复验。

原提交：C1 `f7b062a`；C2 `ecd07d0`；C3 `12863a6`
