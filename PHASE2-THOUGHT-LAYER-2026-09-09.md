# 阶段 2 规格：想法层

> **2026-09-14 收尾说明：** §1–9 保留 2026-09-10 二次修订的原文，供追溯；其中“待执行”、职责分配和“当前行为”均为当时状态。当前验收须同时使用 [§10 收尾修订](#10-2026-09-14-收尾修订现行判据)，只在该节明确列出的范围内替代旧条款。证据与状态见 [收尾记录](PHASE2-CLOSEOUT-2026-09-14.md)。

> **2026-09-15 用户验收修订：** 保存/取消后的播放状态、同段重复捕获和验收分工，以本文 §11 为最新约定；已通过且未受改动影响的结果沿用，不要求用户重复执行整张清单。

> 文档状态：待执行
> 起草：2026-09-09；2026-09-10 修订（§4.5 读写边界、§5.2–5.4 删除「确认」、T7–T9 随改；2026-09-10 二次修订（§4.1 与现有筛选的关系、§4.5 候选上限、§5.3 thought/thoughtAt 不变式、§6 降级措辞、§3.2–3.3 收窄为「改写」、T8 随改））
> 基线：`main` @ `cea382e Release DigestDock 2.0.0`（阶段 0+1 已合并）
> 上位依据：`NOTES-LIBRARY-MVP-PLAN-2026-09-04.md` §2 核心链路、§3 三层模型

本文是**行为契约**，不是实施步骤。规定"改完之后什么必须成立"，怎么实现由执行方决定。

---

## 1. 职责划分（与阶段 1 相反）

- **Claude**：本规格中的全部设计判断，提交前复核 diff
- **Codex**：实现、补测试至验收矩阵全覆盖、真实 Chrome 验收、提交（不 push / merge / release）
- 规格未覆盖的情况：停下来问，不自行决定

阶段 1 是数据迁移（不可逆），所以 Claude 守边界。阶段 2 是交互设计（可逆），扩了也不致命，所以由 Claude 主导形状、Codex 接实现。

## 2. 要解决什么

现在库里 100% 是视频说了什么，0% 是用户想了什么。阶段 1 已建好 `thought` / `thoughtAt` / `triggerWindow` 三个字段，但**没有任何界面能输入**。

本阶段接上这条链路：

```
看视频 → 某一句触发想法 → 记下来
                          ↓
    日后检索到这条想法 → 回到触发点 → 想起当时为什么这么想
```

四块缺一块链路就断：**捕获 → 检索 → 回跳 → 「最近」**。

## 3. 捕获

### 3.1 两次 N，两条路

现状不变的部分：按 `N` 存一条笔记，视频右下角出现「📝 笔记已保存」toast。

新增的部分：

```
按 N          → 存笔记 + toast 出现（现状，视频不暂停）
再按一次 N     → toast 就地变成输入框，光标已就位，视频暂停
   ↓
语音输入 → Enter   → 存成「想法」，输入框关闭，视频保持暂停
Esc                → 关闭，这条就是「金句」
不再按 N，toast 消失 → 这条就是「金句」
```

**为什么第一次 N 不暂停**：大多数时候用户只是想留一句话（金句），不需要写东西，暂停是打断。只有第二次 N（表示"我有话说"）才需要暂停。

**为什么是"再按一次 N"而不是自动弹输入框**：让每次保存都弹出一个需要处理的输入框，是给高频路径（纯金句）加负担。第二次 N 是用户主动的信号，不是系统的假设。

### 3.2 硬约束

| 约束 | 说明 |
| --- | --- |
| 全程键盘可完成 | 输入框自动聚焦，不需要鼠标点击。用户的语音输入键在键盘上，整条链不应中途插入鼠标动作 |
| 不做二次确认 | 没有"确定保存吗"这类弹窗 |
| 变成输入框后不自动消失 | 直到 Enter 或 Esc |
| toast 停留 5 秒 → 10 秒 | 想法不会在按下的瞬间成型，给反应时间。当前值见 `content.js` 的 "Auto-dismiss after 5 seconds" |
| 存完不自动恢复播放 | 用户可能想再补一句或停下来想。继续播放按空格是他本来就熟的动作 |
| 想法不被 AI 改写 | 检索时读取是允许的，边界见 §3.3 与 §4.5 |

### 3.3 想法绝不被 AI 改写

用户使用语音输入法，**该输入法已经把口语整理成书面文字**。再叠一层 AI 优化就是三重转述：他说的 → 输入法整理的 → AI 优化的。

更实际的风险：语音识别的错别字是**明显的**，用户一眼能认出来；AI"优化"会把错字改成通顺但意思不对的词，那种错误认不出来，比留着更危险。

错别字的解法是用户自己改，时机在 §5.3。

**本条管的是改写，不是读取。** 零结果兜底要读想法才能找到想法，边界在 §4.5。

### 3.4 金句与想法不是两种存储

`thought` 为空 = 金句；有内容 = 想法。

**类型是输入行为的自然结果，不是一个要选的选项。** 不新增类型字段、不新增筛选按钮、不在捕获时询问类别。将来若需按类型翻找，那是检索侧的过滤，不是捕获侧的分类。

## 4. 检索

### 4.1 一个界面，两种状态

- 搜索框为空 → 显示「最近」（§5）
- 搜索框有输入 → 显示搜索结果
- 清空 → 回到「最近」

不做两个并列入口，不需要用户决定「该去哪个」。

**与现有「当前视频 / 全部笔记」的关系**（现状默认是「当前视频」，`sidepanel.html` 的 `notesFilterThis` 带 `active`）：

- `当前视频`（**默认不变**）→ 保持现状的当前视频笔记列表，不改
- `全部笔记` → 这里换成「最近」（按天分组倒序）。今天这个视图是一个平铺的全部笔记列表，改成按天分组是严格的改进
- 搜索框一有输入 → **忽略该筛选，永远全局**（§4.2）
- 清空搜索 → 回到用户搜索前所处的那一侧；搜索期间的全局化是临时的，不改变他的设定

为什么默认仍是「当前视频」：正在看视频时打开笔记，想看的是这个视频记了什么，这是高频需求，且是现状行为，不动。「最近」是偶发的回溯需求，点一下「全部笔记」即到，搜索时也自动全局。

### 4.2 搜索永远是全局的

不受笔记页现有的「当前视频 / 全部笔记」筛选限制。用户找的是"三个月前那个想法"，本来就不知道在哪个视频；若受筛选限制，他得先切到"全部"才搜得到——那是一个额外决策。

**一输入就自动放开范围。**

### 4.3 检索范围与排序

范围：想法、触发原句、视频标题、频道名。

排序：**有想法的在前，各组内按 `savedAt` 倒序。**

搜索是目标明确的行为——用户在找一个具体的东西，所以先给他自己的话，再给只有字幕的条目。

（初稿此处还有一条「未确认的不置顶」，随 §5.4 一并删除：系统里不再有「确认」这个状态。）

### 4.4 结果卡片

**想法在上，触发原句与触发窗口以次级样式附在下方。**

搜到的是自己的话，看到的是当时在听什么。

### 4.5 零结果时的 AI 兜底

精确匹配无结果时，出现一个**「让 AI 找找」按钮**。

五条边界：

1. 只在零结果时出现
2. 必须用户点击才调用（不自动消耗 API 额度）
3. **输入可以包含想法**：想法、触发原句、视频标题、频道名全部可交给 AI 检索
4. **输出只能是现有笔记的 ID**：不返回答案、不生成新内容、不改写任何已存字段。返回内容里对不上现有 ID 的部分一律丢弃，不展示；展示内容一律从本地原笔记取
5. **候选上限 5 条。** 输入装不下整个库时，按 `savedAt` 倒序取装得下的部分，并在界面上写明「只检索了最近 N 条」——**不静默截断、不自动多轮请求**

§3.3 禁止的是**改写**，不是**读取**。那一条命名的危害是「AI 把他的话改了」，而检索不改存储内容。若连读取也禁，这个按钮就找不到想法——而它存在的唯一目的正是找想法。

不做模糊匹配、不做向量检索、不把语义搜索变成默认路径。这是零结果的降级路径，不是检索的主路径。

### 4.6 错别字的检索兜底（已天然成立，无需新功能）

语音输入的同音错别字会让精确匹配失效。但检索范围同时包含**触发原句**（字幕，没有用户的识别错误），所以即便想法里写错字，仍可通过"当时那句话说了什么"搜到。两条路互为备份。

## 5. 「最近」

> 命名说明：这里指**现实日期**的浏览视图，不是笔记卡片上的视频内时间码（`26:19` 那种，已存在且不改）。规格中不使用"时间轴"一词，避免与后者混淆。

### 5.1 解决的问题

用户自述："忘了上上周看了什么视频""感觉当时记了一些，但想不到。"——记得大概什么时候，想不起是哪个视频。

搜索需要先记得关键词；「最近」不需要记得任何东西。

### 5.2 形态

- 按**天**分组，倒序；没有笔记的日子不显示
- **没有置顶区、没有「待确认」计数、不新增任何确认字段**（理由见 §5.4）
- `thought` 为空的笔记（含阶段 1 迁移来的历史笔记）就是金句，正常显示，不标「缺失」、不算待办

### 5.3 在「最近」里补想法与改错别字

这是 §3 那个 10 秒窗口的兜底：**toast 消失后想补想法，在这条笔记上直接补。**

- 任意一条笔记都可就地编辑 `thought`，不限时间、不限状态
- 这也是语音错别字的修正入口
- 编辑只写 `thought` / `thoughtAt`，不写任何状态标记
- **允许清空**：把想法删空 = 这条退回金句，不做二次确认（§3.2）

**不变式：`thought` 为空 ⟺ `thoughtAt` 为 `null`。** 两者永远同进同退。清空想法时 `thoughtAt` 一并置空，不留一个带时间戳的金句——否则 `thoughtAt` 就变成了一个隐式状态字段，而 §5.4 刚把状态字段删掉。

- 编辑不改 `createdAt`，也不改笔记在「最近」里的日期归属（改错别字不该让它跳到今天）

### 5.4 为什么没有「确认」

初稿（2026-09-09）写了「未确认置顶 + 显示还有几条没看」。撤回，理由三条：

1. **置顶判据不成立。** 唯一可用的信号是 `thought` 为空，但按 §3.4，`thought` 为空就是金句——一条**已完成**的笔记。把金句永久置顶，等于告诉用户「你有 40 条没写完」，而他有 0 条
2. **历史笔记会灌满置顶区。** 阶段 1 迁移来的笔记全部 `thought` 为空，会永久占住顶部
3. **排序已经做到了这件事。** 「最近」本来就按天倒序，刚记的就在最上面。置顶在日期排序之上不增加任何信息，只增加一个只涨不落的计数——对 ADHD 用户是负担，不是帮助

同一条原则（初稿 §5.3 已写过）推到底就是这个结论：确认做成 gate 会「忘了确认 = 丢数据」；而一个谁都不必处理的标记，是没有功能的愧疚计数器。两头都不要，所以整块删掉。

## 6. 回跳

每条笔记（搜索结果里的、「最近」里的）一键回到该视频该时间点，跨视频、跨平台均可用。

这是整条回路的终点，必须无条件可用。视频不可达时降级为展示冻结的 `triggerWindow`。原因**只在能确证时**说明（例如标签页打不开）；不能确证就只说「没能打开这个视频」，不推测「已删除」「无权限」之类。

## 7. 验收矩阵

（「最近」的分组按**浏览器本地时区**的自然日，不用 UTC。）

改完之后必须成立。实现前先把这张表写成自动化测试，真机验收再走一遍。

| # | 场景 | 预期 |
| --- | --- | --- |
| T1 | 按一次 N | 笔记已存，toast 出现，**视频继续播放**，`thought` 为空 |
| T2 | 按 N 后在 toast 可见期间再按 N | toast 就地变输入框，**光标已聚焦**，视频暂停，toast 不再自动消失 |
| T3 | T2 后输入文字 + Enter | `thought` 与 `thoughtAt` 落库，输入框关闭，**视频保持暂停不自动播放** |
| T4 | T2 后按 Esc | 输入框关闭，笔记仍在，`thought` 仍为空 |
| T5 | 全局搜索一个只出现在某条想法里的词 | 无论当前筛选是"当前视频"还是"全部笔记"，都能搜到；卡片上想法在上、触发原句在下 |
| T6 | 搜一个只出现在字幕里、不在想法里的词 | 能搜到（§4.6 的兜底成立） |
| T7 | 搜一个完全不存在的词 | 零结果态出现「让 AI 找找」按钮；**不点则不发起任何 provider 请求**。点击后：返回的候选全部对应到现有笔记 ID，且**库内容逐字节不变**（无任何字段被改写） |
| T8 | 切到「全部笔记」、搜索框为空 | 显示「最近」，按天分组倒序，没有笔记的日子不显示；**不存在置顶区、不存在待确认计数**；`thought` 为空的笔记正常显示，不标「缺失」。「当前视频」一侧与筛选默认值保持现状不变 |
| T9 | 在「最近」里给一条 `thought` 为空的笔记补写想法 | `thought` / `thoughtAt` 落库，**不写入任何确认字段**；该想法立即可被搜索到 |
| T10 | 回跳 | 搜索结果与「最近」里任意一条，一键回到对应视频对应秒 |

## 8. 明确不做

- **不做标签系统**（要求捕获时分类 + 长期维护分类法，与零决策冲突）
- **不做快捷键可配置**：Chrome 原生 `chrome.commands` 不支持裸字母键，保留裸 `N` 只能自建设置项；不在"想法进得来、找得回"这条链上。上架后真有用户反馈再做，成本相同
- **不对想法做任何 AI 改写**（§3.3）；检索时读取是允许的，边界见 §4.5
- **不新增确认 / 已读 / 待办类字段**（§5.4）
- **不做模糊匹配 / 向量检索 / 保存的搜索条件**
- 不改锚点、`triggerWindow -8/+12`、AI 输入窗口 `前4+TARGET+后2`
- 不改字幕获取链路、概览、导出的任何现有行为
- 迁移期间发现的无关缺陷只记录，不在本轮修

判据：一处改动若对应不到 §7 的某一行验收，它不属于本轮。

## 9. 交付要求

- 分阶段提交，红测在前
- `npm test` 与 `npm run check` 全过
- **不要 push、merge、release**
- 交付时给出：提交哈希、门禁数字、§7 十条的真机验收结果

## 10. 2026-09-14 收尾修订：现行判据

### 10.1 生效依据与记录方式

用户在本轮要求由 Codex 作最终判断，修整采纳的收尾意见，并提供人工验收清单。本节将已经落在 `DESIGN.md` 和功能基线 `8a07174` 中的后续调整纳入现行判据；不把后续要求追写成 9 月 10 日一开始就有的要求，也不凭现有实现反推每次历史调用的授权。

- 本文件 §1–9 是原阶段 2 行为基线；下表是它的明确修订。其他条款继续有效。
- `DESIGN.md` 的“笔记”部分承载当前呈现与交互要求，本节列出它与旧规格的对应关系。以后新增行为仍需明确需求，不能以“修 bug”为由自行扩大范围。
- 原职责分配和阶段提交顺序保留作历史记录，不再触发 Claude、其他模型或阶段编排。当前任务由 Codex 完成用户授权的本地收尾。
- 自动化通过、某次真实浏览器检查、用户验收、本地提交和发布分别记账。旧版 T1–T10 浏览器结果不能直接当成 `8a07174` 的完整用户验收。
- `savedAt` 指索引中的保存时间，来源为笔记 `createdAt`；编辑想法不会改变该时间或日期归属。

### 10.2 原条款与后续调整

| 原条款 / 验收项 | 现行判据 | 调整性质与证据 |
| --- | --- | --- |
| §3.1–3.2、T1–T4：第一次 N 不暂停，第二次 N 写想法 | **新金句**仍遵守原两次 N 交互与 10 秒窗口。同一视频、同一字幕锚点、同一原句再次捕获时复用原记录，第一次 N 就暂停并打开已有想法；Esc 保留旧内容，不额外新增空想法副本。相同原句有多条不同想法时提示到笔记页查看，不自动合并。 | 后续捕获去重与找回交互，`ab2745c`；`DESIGN.md`“笔记”与 `tests/note-capture-dedup.test.js`。原 T1 不能再被解释为所有重复捕获都不暂停。 |
| §4.1、§4.4、T5/T8：当前视频保持原列表 | 当前视频的默认入口与时间码排序保留；当前视频、全部笔记、搜索共用一致卡片。想法在上且易区分，金句使用当前语言模式；上下文默认折叠，点金句展开/收起，仅限制显示预览，不裁剪已存冻结窗口。 | 卡片一致性修复与呈现调整，`4d38366`、`b1d4899`；`tests/notes-view-consistency.test.js`、`tests/notes-feedback-20260914.test.js`。 |
| §4.1、§5.2、T8：全部笔记仅按天分组 | 全部笔记提供“按日期 / 按视频”，默认按日期并在本机记住选择。按日期为“日期 → 视频 → 笔记”，按视频为“视频 → 日期 → 笔记”；日期倒序，视频组按最近保存时间倒序，同一天按视频时间码升序。没有笔记的日期不显示。 | 9 月 14 日分组修订，`4d38366`、`b1d4899`。持久化的是排列偏好 `digestdock_notes_grouping`，不是搜索词或保存的搜索条件；搜索不受此偏好限制。 |
| §4.3、T5/T6：搜索想法、原句、标题、频道 | 同时覆盖已保存的完整金句与有效译文。保持全局精确检索、想法优先、组内保存时间倒序；清空搜索恢复原筛选和排列偏好。搜索与切换排列均不自动调用 AI。 | 搜索可见内容的一致性修复，`b1d4899`；`tests/notes-feedback-20260914.test.js`。不增加模糊/向量搜索。 |
| §3.2、§5.3、T9：清空想法不二次确认 | 编辑框中手动清空并保存仍不弹二次确认。另设“删除想法”按钮，点击后确认才清空，取消/Esc 保留原文。只清空 `thought` / `thoughtAt` 及同步既有派生索引；金句、来源、记录日期不变。 | 独立删除入口是后续交互增加，`b1d4899`。临时删除确认框不构成“已确认/待确认”持久状态。`tests/notes-feedback-20260914.test.js` 覆盖取消、失败及过期确认保护。 |
| §6、T10、§8：回跳且不改字幕获取链路 | 回跳正确视频、分 P 与秒数后，普通 YouTube 自动准备已有缓存或免费原生字幕，侧栏保持在用户所选笔记页；切视频时作废旧字幕，广告时不以广告时钟推进正片字幕。 | `a2e3d7d` 的字幕跟随修复、`8a07174` 的回跳后字幕恢复。仅纳入这两处关联调整；不新增字幕来源、不自动调用 AI/Supadata/付费路径，不改变冻结窗口、捕获锚点或 AI 清理输入窗口。 |
| §8：导出的任何现有行为不变 | “导出本视频笔记”的入口文案与按钮呈现可以更清楚；导出数据格式、范围和既有补全授权保持原规则。 | `b1d4899` 的入口澄清。不能据此声称 UI 完全未变，也不能把文案调整解释成导出协议修改。 |

### 10.3 保持不变的硬边界

1. 用户想法不经过 AI 改写。AI 候选检索允许读取想法，但只返回现有笔记 ID；展示用本地原文，最多 5 条，不写回笔记库。
2. 零结果检索必须用户点击才调用；容量截断可见、不自动多轮请求。不增加标签、快捷键设置、模糊/向量搜索或保存的搜索条件。
3. 不新增确认、已读、待办状态或计数；空想法仍是完整金句，`thought` 为空与 `thoughtAt=null` 同进同退。
4. 捕获锚点、冻结窗口与 AI 清理输入窗口不变；概览与导出内容契约不变。§10.2 的关联字幕修复不是全链路改造授权。

### 10.4 验收与归档

- §7 的 T1–T10 继续保留；按 §10.2 修订适用场景，并补测去重、卡片一致性、分组记忆、删除与字幕恢复，不能仅数带 T 编号的测试。
- 具体用户操作与结果入口见 [人工验收清单](PHASE2-MANUAL-ACCEPTANCE-2026-09-14.md)。未获得用户结果的项目保持“未验证”。
- 原委托中的“未跟踪规格不要提交”是当时的归档边界；本轮用户已授权整理采纳项，现将阶段 2 判据与相关历史证据纳入本地版本管理。保存版本不等于 push、merge 或 release。

## 11. 2026-09-15 用户验收后的修订

本节源于用户实际试用反馈，替代 §3.2/T3 的“保存后保持暂停”及 §10 中精确同句才重复的限制；其他未列出的边界不变。

1. **保存与取消后继续播放。** 用户明确选择“保存、取消都继续播放”。输入框仍在打开时暂停；成功保存或 Esc 取消后恢复进入前的播放状态。本来暂停不强行启动，失败不关闭草稿或恢复播放；旧播放器、旧视频、旧分 P 和广告状态不得被迟到回应误播。
2. **同一完整内容可以覆盖多行字幕。** 用户明确要求保留原来按一段内容采集的方式，时间码仍只显示一个点。保留原 AI 输入和冻结窗口；根据已存完整内容与对应冻结字幕证明同一出处中的连续内容范围，允许范围内不同字幕行找回原笔记。不能用固定秒数、跨视频相似文本或单独 AI 输出猜合并。原回跳点、原想法及其他存储字段保持不变；历史冲突不自动合并/删除。
3. **中文字幕优先。** 图示视频同时提供 `en-US` 与 `zh-Hans` 原生轨。存在中文轨时应先用中文，简体繁体均可，人工优先自动；已知中文目标时英文 Passive 或缓存不得提前结束任务。中文变体之间的有效同类型结果不反复重取。
4. **能由 Codex 测的由 Codex 完成。** 清单 4、6、7、9 移交 Codex；用户已报告 YouTube 3、5、8 通过。跟随/跳转已有用户通过结果，未改对应行为时沿用。只有受本次改动影响的部分需要补测；工具无法完成的必要用户动作须说明具体原因，不能再次把整份清单交回用户。

当前结果与后续停点见 [验收结果](PHASE2-MANUAL-ACCEPTANCE-2026-09-14.md) 和 [收尾记录](PHASE2-CLOSEOUT-2026-09-14.md)。
