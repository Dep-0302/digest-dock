DigestDock 阶段 2 执行委托：想法层

> **历史记录（2026-09-14 归档）：** 下文的状态、版本、门禁数字和执行约束属于文中所标的原阶段，不代表当前分支。保留原文用于追溯；当前阶段 2 的适用修订见 [规格 §10](PHASE2-THOUGHT-LAYER-2026-09-09.md#10-2026-09-14-收尾修订现行判据)，最终状态见 [收尾记录](PHASE2-CLOSEOUT-2026-09-14.md)。

> **2026-09-10 修订。** 本文初版写在「确认状态」与「AI 读写边界」两个问题补定之前；第 3 节第 3、5、7 条与第 5 节曾与现规格冲突，已就地改正。
> 行为事实来源永远是 `PHASE2-THOUGHT-LAYER-2026-09-09.md`（现 225 行）。本文与它冲突时，以它为准。

## 0. 你要做什么

实现「想法层」：让用户在看视频时把自己的想法记进笔记，并能日后检索到、跳回触发点。

**唯一事实来源**：`/Users/wangchao/Documents/061-DigestDock/PHASE2-THOUGHT-LAYER-2026-09-09.md`
开工前完整读一遍。本条消息不替代该文档，只是把边界和顺序再钉一次。

## 1. 基线与分支

- 基线：`main` @ `cea382e Release DigestDock 2.0.0`（已核对，阶段 0+1 已合并）
- 从 `main` 新建分支，建议 `codex/thought-layer-phase2`
- **不要 push、不要 merge、不要 release。** 全程停在本地分支。
- 仓库里 `worktrees/notes-trust-phase0/` 是阶段 0 的旧工作树：不要动、不要在里面改、不要删
- 仓库根目录那几个未跟踪的 `*.md` 规格文档不要提交进去

## 2. 职责划分（与阶段 1 相反）

- 设计判断已经定了，见规格 §3–§6，**不要重新设计**
- 你负责：实现、补测试到验收矩阵全覆盖、真实 Chrome 验收、分阶段提交
- Claude 负责：提交前复核 diff
- 规格未覆盖的情况：**停下来问，不要自行决定**

## 3. 设计判断（已定，不要改）

1. **两次 N**：第一次 N = 存笔记 + toast（视频继续播放），第二次 N = toast 就地变输入框 + 视频暂停。不要改成"按一次就弹输入框"——那是给高频路径（只想留一句金句）加负担
2. **`thought` 为空 = 金句，有内容 = 想法。** 不新增类型字段、不新增筛选按钮、不在捕获时问类别。类型是输入行为的自然结果，不是一个要选的选项
3. **想法绝不被 AI 改写，但检索可以读。** 用户用的语音输入法已经把口语整理成书面文字了；再叠一层 AI 优化会把「看得见的错别字」变成「看不见的意思错误」。错别字由用户自己在「最近」里改。零结果兜底必须能读想法（否则它找不到想法），边界落在输出侧：只取现有笔记 ID，其余一律丢弃，库内容不变——见规格 §4.5
4. **「最近」指现实日期的浏览视图**，不是笔记卡片上 `26:19` 那种视频内时间码（已存在，不改）。规格里不用"时间轴"这个词，你的代码、注释、UI 文案也不要用
5. **没有「确认」这个状态。** 不新增 `confirmedAt` / 已读 / 未读 / 待办任何字段；「最近」里没有置顶区、没有待确认计数。`thought` 为空就是金句、是一条已完成的笔记，不是待办——理由见规格 §5.4。任意笔记随时可就地编辑 `thought`，只写 `thought` / `thoughtAt`
6. **搜索永远是全局的。** 一有输入就自动放开笔记页现有的「当前视频 / 全部笔记」筛选——用户找"三个月前那个想法"时本来就不知道在哪个视频
7. **两处排序规则不同，是故意的**：搜索结果按「有想法的在前、各组内 `savedAt` 倒序」；「最近」按天分组倒序。两边都没有置顶区。不要统一这两处

## 4. 执行顺序（硬要求）

1. 先把规格 §7 的 T1–T10 写成自动化测试，**先红**
2. 再实现，让它们变绿
3. `npm test` 与 `npm run check` 全过
4. 真实 Chrome 里把 T1–T10 再走一遍

T7 要真的断言"不点「让 AI 找找」就不发起任何 provider 请求"——要能捕获到 provider 调用层，不是只看 UI 没变化。

## 5. 范围围栏

判据：**一处改动若对应不到 §7 某一行验收，它不属于本轮。**

明确不在本轮（即使你看到它是错的）：

- 不做标签系统
- 不做快捷键可配置。`content.js:729` 的裸 `N` 保留原样。Chrome 原生 `chrome.commands` 不支持裸字母键，不要为此加设置项、不要往 manifest 加 `commands` 段
- 不对想法做任何 AI **改写**（检索时读取允许，边界见规格 §4.5）
- 不新增确认 / 已读 / 待办类字段
- 不做模糊匹配 / 向量检索 / 保存的搜索条件
- 不改锚点（`content.js` 里的 `- 3` 秒）、不改 `triggerWindow -8/+12`、不改 AI 输入窗口「前4+TARGET+后2」
- 不改字幕获取链路、概览、导出的任何现有行为
- 两个已知遗留问题**本轮不修**，只要别让它们变严重：`background.js` ~5143 的恒假分支；`ytd_notes_<mediaKey>` 与 `ytd_notes_schema` 的键名碰撞
- 发现的其他无关缺陷只记录在交付说明里，不在本轮修

**如果某个门禁（测试 / lint）逼着你改规格之外的文件：拆提交，并在交付时说明，不要把本轮范围扩大。**
阶段 0 那次是 305 行的改动范围，交付成了 6672 行、22 个文件，结果我为那 7 项改动写的真机验收文档完全覆盖不到实际 diff。这次不要重演。

## 6. 已知落点（参考，不是清单）

- `content.js:729` 按键处理；`content.js:941` 的「Auto-dismiss after 5 seconds」→ 10 秒
- `background.js:5367` 附近已有 `thought` / `thoughtAt` / `triggerWindow` 字段（阶段 1 已建好）：**直接用，不要新增字段**
- 笔记页的搜索与列表在 `sidepanel.js`

## 7. 交付时给出

- 各阶段提交哈希
- `npm test` / `npm run check` 的门禁数字
- §7 十条在真实 Chrome 里的逐条结果
- 本轮发现但没修的问题清单
