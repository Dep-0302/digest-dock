# DigestDock 设计规范

<!-- impeccable:design-contract 1 -->

状态：2026-08-23 本轮 UI、笔记分组、可恢复导出与多 Provider 方案已落到本地工作树；自动门通过后仍需真实浏览器与用户验收。
适用范围：Chrome 扩展图标、YouTube/B 站页面入口、侧栏字幕／概览／笔记、设置页。
实现状态：本文件是当前设计与产品行为真源。“已实现”“测试通过”“真实浏览器已验证”“用户验收”“已发布”必须分别记录。

## 设计主张

DigestDock 是一个以时间为索引的阅读与笔记工具。视觉层级必须优先回答三个问题：正在看哪一段、这一段发生在什么时间、下一步可以做什么。

界面使用冷灰白工作台、深石墨文字与克制的蓝青状态色。渐变只用于品牌图标、短状态线和紧凑主操作，不铺满内容表面；深度来自分组、位移和少量投影。

## 平台与语言矩阵

| 平台 | 字幕 | 概览 | 笔记 |
| --- | --- | --- | --- |
| YouTube 非中文字幕 | `原文 / 中文 / 双语` | `原文 / 中文 / 双语` | `原文 / 中文 / 双语` |
| YouTube 中文字幕 | 保留原文；不发重复翻译请求 | 三种显示模式复用中文底稿 | 三种显示模式复用已保存中文内容 |
| B 站 | 不显示语言分段控件，直接呈现站内字幕 | 不显示语言分段控件，直接生成中文概览 | 保留 `原文 / 中文 / 双语`，以支持跨平台“全部笔记” |

平台差异必须由媒体身份驱动，切换视频或平台时重新计算，不能让 B 站隐藏状态污染 YouTube。字幕正文保持可选中，跳转只由时间码或明确动作承担。

## 已确认视觉稿

- [完整设计总览](docs/design/final-design-board.jpg)
- [图标与页面入口](docs/design/final-icons-and-entry.jpg)
- [侧栏字幕／概览／笔记](docs/design/final-sidepanel-states.jpg)
- [设置页](docs/design/final-settings.jpg)

视觉稿是本规范的构图证据；本文件是实现时的规则真源。若二者冲突，应先停止并向用户确认，不得自行折中。

## 颜色

| 角色 | 色值 | 用途 |
| --- | --- | --- |
| Canvas | `#EDF1F3` | 设计画布与设置页外层背景 |
| Panel background | `#F7F9FA` | 侧栏内容区域 |
| Surface | `#FFFFFF` | 普通卡片、顶部栏、输入框 |
| Surface soft | `#F6F8F9` | 次级控件、非选中状态 |
| Ink | `#17212A` | 标题与主要正文 |
| Ink secondary | `#53616D` | 正文、说明 |
| Ink muted | `#89949D` | 时间、来源、元数据 |
| Line | `#DFE5E8` | 卡片与输入框细边 |
| UI blue | `#076DD1` | 当前播放文字、焦点、细线状态 |
| Action gradient | 品牌渐变＋由 `16% → 45%` 加深的透明覆层 | 反白文字的紧凑主操作，在保留亮蓝青识别度的同时满足对比度 |
| Active surface | `#EAF6FC` | 当前字幕与选中章节底色 |
| Brand gradient | `#0A5FE9 → #04B7D2` | 插件图标与页面入口底色 |
| Brand marker | `#D8F7FF` | 插件图标时间点 |
| Danger | `#C23B49` | 删除、翻译失败和错误提示 |
| Success | `#2C8A65` | 已保存、已配置 |

蓝青色表达当前状态或主要提交动作；错误和删除保持独立红色语义。字幕文本选择不得变成高饱和整行蓝色卡片。

## 字体与数字

### 常规界面

使用系统字体：

```css
font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
```

侧栏正文建议 `12.5–14px`，行高 `1.55–1.65`。设置页正文以 `15px` 为基准，导航 `14px`，服务名称 `16px`，页面标题约 `27px`。

### 时间码

时间码单独使用本地内置的 `Barlow Condensed`：Regular 400，当前状态 Medium 500。不得运行时请求第三方字体。

```css
font-family: "Barlow Condensed", "Arial Narrow", sans-serif;
font-size: 11.5px;
font-weight: 400;
font-variant-numeric: tabular-nums;
font-feature-settings: "tnum" 1;
letter-spacing: 0.025em;
```

格式规则：

- 小于 1 小时：`MM:SS`，例如 `59:18`。
- 大于或等于 1 小时：`H:MM:SS`，例如 `1:00:27`、`3:02:58`。
- 时间栏固定为约 `66px`，最长三小时样例不得挤压正文。
- 时间是辅助信息；除当前字幕／选中章节外，颜色与字重必须弱于正文。

字体实现时应内置所需 WOFF2 和 OFL 许可证。上游来源：[Google Fonts / Barlow Condensed](https://github.com/google/fonts/tree/main/ofl/barlowcondensed)。

## 图标系统

### 插件图标

正式源文件：[icons/digestdock-icon-solid.svg](icons/digestdock-icon-solid.svg)。

- 深蓝到青蓝的自然渐变圆角方形底，边缘略深、右下柔和提亮。
- 三条白色章节线。
- 一枚冰蓝白时间点。
- 已准备 `16px`、`48px`、`128px` 候选 PNG；正式接入时替换 manifest 使用的同名图标。

### 页面入口

- 打开 DigestDock：品牌图标＋`DDK` 短文字；视觉图标约 `24px`，按钮高度至少 `34px`，完整名称保留在 tooltip 与 `aria-label`。
- 保存当前时刻：使用线性 `bookmark-plus` 图标＋`金句速记 (N)` 文字；默认蓝青渐变底为 `20%`、图标和文字为 `50%`，悬浮时三者均提升到 `100%`，并轻微模糊背后视频；保留 tooltip 与 `aria-label`。
- 页面按钮不得使用 emoji、字符三角或重复显示完整 `DigestDock` 文案；页面入口统一显示 `DDK`。

### 品牌字样

- 只在品牌图标旁显示完整 `DigestDock` 时，将首字母 `D`、词中 `D` 与末尾 `K` 设为 `750`，形成克制的 `DDK` 视觉骨架；其余字母为 `600`，无图标的眉题、版本和正文保持普通字重。
- Chrome 原生侧边栏标题无法分别设置字母字重，保持标准 `DigestDock` 名称，不使用 Unicode 假粗体。

Chrome 原生侧栏标题栏的固定、关闭和容器布局不属于可设计范围；仅替换扩展名称与图标。

## 侧栏结构

### 顶部

- Chrome 原生标题栏下显示视频标题、频道／来源与时长。
- 语言模式使用紧凑分段控件：`原文 / 中文 / 双语`。
- 设置入口为图标按钮。
- 主导航保留文字：`字幕 / 概览 / 笔记`；活动项使用短蓝青渐变下划线，不使用整块胶囊。

### 字幕

- 每个字幕段落是一张独立白色卡片，卡片间距 `8px`，圆角约 `11px`。
- 正文保持原生可选择文本；正文区域不承担跳转点击，时间码负责跳转。
- 不显示整行蓝色选择状态。
- 当前播放卡向右缩进 `6px`，宽度同步缩减，避免横向溢出。
- 当前卡使用 `#EAF6FC` 底色、左侧 `3px` 蓝青渐变短竖线与中性投影 `0 8px 18px rgba(23,33,42,.10)`。
- 普通卡无投影，只使用细边。
- 左侧时间栏与正文之间使用 `1px` 竖向分隔线；不使用圆点。
- 自动跟随暂停后显示“跟随视频”按钮；按钮使用可保证白字对比度的深蓝青渐变。

### 概览

- 章节与字幕使用同一套独立卡片语法。
- 选中章节向右缩进 `6px`，使用相同冷色底、左侧蓝青竖线和投影。
- 时间码支持长视频格式。
- 章节标题优先于摘要；摘要最多使用次级正文色。
- 关键语句使用普通细边卡片，不使用粗彩色侧边。

### 笔记

- 顶部保留“当前视频 / 全部笔记”筛选，以及 `原文 / 中文 / 双语`。
- “全部笔记”按稳定 `mediaKey` 分组，同一个视频的所有笔记放在一个完整来源容器内；不能按标题分组。
- 来源容器显示语言模式一致的视频标题、频道、平台和笔记数量。视频标题的中文版本按媒体去重生成并持久化。
- 容器之间按当前可见标题排序；容器内部只按时间码升序，保存时间不得参与排序。
- 时间码、视频名、正文依次降级；长视频使用 `H:MM:SS`。
- 播放为蓝青渐变主操作；复制文字与复制链接为中性图标按钮；删除进入更多菜单并保持红色危险语义。

### 笔记与字幕导出

- 笔记页提供当前视频、所选视频、全部视频和单个来源容器导出；格式统一为 UTF-8 TXT。“全部笔记”的选择器必须在确认前显示每个来源的 `可导出 / 需补资料 / 需补译 N 项`，范围在点击导出时冻结。
- 每个视频段必须包含标题、频道、网址、视频简介和按时间码排序的已保存笔记；不得附带整部视频字幕。
- 字幕下载为 UTF-8 TXT；原文、中文、双语由当前字幕模式决定，文件名带语言后缀。
- 原文导出只读取本地资料，不调用任何第三方服务。
- 中文或双语笔记导出只预检元数据、标题、简介和已保存笔记；字幕 TXT 才预检完整字幕段。笔记导出只有两个业务出口：`完整导出` 与 `直接导出`。语言只由顶部 `原文 / 中文 / 双语` 决定；取消使用关闭或 Esc，不再作为第四个业务动作。
- `完整导出` 点击前必须披露所选范围、需打开的视频数量、当前 AI Provider、待补译数量和本轮最多 20 批。该点击只授权当前冻结范围的当前一轮；内存授权同时绑定 Provider／模型路由、笔记版本及点击时已经完整的来源版本，只允许当时缺资料的视频在逐页补齐后冻结新的来源版本。页面资料补齐后可自动复检、补译并下载；达到批次上限、侧栏重建、Provider／范围／语言／笔记或已冻结资料变化时必须再次点击继续，且重新确认前不得调用 Provider。
- `直接导出` 不补页面资料、不调用 AI 或 Supadata；缺失翻译、未知简介和裁剪简介必须在 TXT 中明确标记，禁止用原文冒充中文。
- 历史资料缺口在同一导出工作区内逐个处理：首次完整导出可自动打开第一个缺资料视频，成功后自动复检；多个视频只显示一个 `打开下一个` 主操作，不批量创建标签页。页面尚未就绪或标签页打开失败时，该操作必须恢复为可点击的 `重试当前视频`，不能停留在永久禁用状态。
- YouTube 页面资料优先读取当前播放器的完整 `videoDetails`，仅在播放器资料不完整时回退内容脚本；两条路径都必须与当前网址中的 `videoId` 精确一致。新标签的 `pendingUrl` 可作为加载中的目标身份，但任何旧视频、无身份的旧脚本响应都不得写入，分别进入稍后重试或刷新提示。
- YouTube 字幕取得顺序固定为正缓存、零请求 Passive、单次固定 IOS/json3 Active、CC 明确重试、逐次同意的 Supadata。Active 最多 `1 player + 1 timedtext`，不操作播放器或文字稿面板；有中文轨时中文变体同权、人工优先且同类保持源顺序，无中文时读取当前轨，否则默认轨。
- 笔记成功保存后，background 必须在精确媒体身份校验通过时 best-effort 固化标题、频道、网址、简介、语言和已有字幕资料；来源写入失败不得反转已成功保存的笔记。侧栏只对同一个 mediaKey 做幂等补强。
- `descriptionStatus === unknown` 或 `descriptionTruncated === true` 继续阻断完整导出；不得为了减少跳转而放宽完整简介要求。确认完整的播放器资料可以覆盖历史裁剪片段。
- 长视频不会因总字幕段数超过旧上限而永久禁用。每次明确点击最多执行 20 个任务批次、最多 100 次保守模型请求；一轮结束后停在“继续补齐”，不得自动开始下一轮。
- 每个有效批次完成后立即写入本地视频级翻译资料；刷新、关闭侧栏或稍后回来时按当前导出类型只规划仍缺少的内容，不重复翻译已验证内容。
- 用户可以取消后续批次；已经发出的当前批次若仍匹配冻结的媒体与原文版本，可以写入本地复用缓存，但不得更新已离开的页面、继续启动新批次或自动导出。
- 补译不会调用 Supadata，也不会静默切换 Provider。保存 API Key 只用于用户本轮明确点击后的当前 Provider 请求，不构成自动续跑授权。

#### 可恢复状态与本地资料

- 视频资料与已验证译文保存在 `ytd_note_sources_v2`；旧 `ytd_note_sources` 只在实际读取时惰性迁移，不能用旧记录覆盖较新的原文版本。
- 字幕段以媒体、稳定段落身份、精确毫秒时间与原文指纹共同校验；原文变化只使对应译文失效。
- `ytd_note_export_jobs_v1` 只保存导出范围、语言、进度、稳定单元键、轮次和 Provider 快照，不保存 API Key、字幕正文、笔记正文或译文。
- 原文导出不创建 AI 请求；中文／双语导出只在缺口预检后由用户主动开始或继续。
- 删除全部笔记或重置扩展数据时，同时清理对应的视频来源资料与导出任务，避免留下无法从界面管理的本地记录。

## 设置页

- 独立标签页采用左侧分区导航与右侧内容区。
- 品牌图标、焦点、链接、当前导航与主要保存动作统一使用蓝青体系，不保留旧珊瑚色。
- 最终导航只有：扩展服务、阅读显示、笔记与备份、本地数据、隐私说明。不得出现开发术语“字幕回退”或“本地改造”。
- 设置页锁定为 `100vh` 工作区；右侧 `.settings-scroll` 独立滚动，使左侧导航持续可见。窄屏时导航转为顶部横向区域导航。
- AI Provider 与 Supadata 在宽屏双列展示；较窄窗口改为单列。Supadata 仍是逐视频确认的 YouTube 字幕服务，不进入一级导航。
- 标题、导航、输入、说明文字均比旧版放大，不以小字制造“简约感”。
- 保存状态与保存按钮放在“扩展服务”的 AI 服务卡底部，与服务商和密钥形成同一操作闭环；阅读显示继续即时应用，不共用保存动作。
- 删除笔记、重置数据等操作只出现在“本地数据”的危险区域。
- 点击导航、滚动分区和 URL hash 变化必须同步唯一 `.is-active` 与 `aria-current="location"`。

### AI Provider 选择器

- 用户只选择服务商并填写对应 Key；Endpoint、模型 ID、请求格式和能力限制均为内置预设，不提供自由文本 Endpoint/模型字段。
- 自定义控件使用 ARIA combobox/listbox，支持方向键、Home、End、Enter、Space、Escape、焦点返回和活动项说明。
- 下拉与服务卡加载本地品牌图标，不运行时热链。来源登记在 `icons/providers/PROVENANCE.md`。
- 服务卡大图标使用 `40 × 40px` 容器：极浅青蓝渐变底、低对比度 `1px` 蓝灰描边、轻微向下柔影和白色内高光；品牌图形在约 `30 × 30px` 内光学居中。禁止黑色底与彩色光晕。AI Provider 与 Supadata 使用同一材质规则。
- 下拉按钮和列表项图标使用 `24 × 24px` 同色极浅渐变与细描边，品牌图形约 `18 × 18px`；不加投影，以免密集列表产生噪音。
- 当前已核实并可选择：DeepSeek V4 Flash、智谱 GLM-4.7-Flash、阿里云百炼 Qwen Flash（Qwen3）、SiliconFlow Qwen3-8B、Fireworks DeepSeek V4 Flash。
- 腾讯混元翻译保留第六个官方图标和认知入口，但必须显示“暂不可用”并设置 `aria-disabled="true"`。官方已确认 `hunyuan-translation-lite` 存在，但尚未确认它可使用当前单 Key 的 OpenAI-compatible Endpoint；不得猜测配置或误收用户 Key。
- 每家 Key 独立保存，切换后返回不丢失草稿；选择、切换和保存本身不发网络请求。失败时不得自动换用另一家。

## 圆角、间距与投影

- 基础间距单位：`4px`。
- 卡片间距：`8px`。
- 控件圆角：`8–10px`。
- 内容卡圆角：`11–14px`。
- 侧栏／设置外框圆角：约 `22px`（仅设计预览容器；真实 Chrome 侧栏遵循浏览器容器）。
- 普通卡只使用细边，不叠加投影。
- 状态卡使用位移和中性投影；不得使用彩色光晕。

## 动效与状态

- 状态切换基础时间：`160ms`。
- 当前卡进入时只改变 `transform`、`background` 和 `box-shadow`。
- 按钮按下立即反馈，不等待异步操作完成后才变化。
- 保存成功使用短暂对勾或状态提示，不通过持续动画吸引注意。
- `prefers-reduced-motion: reduce` 时取消位移，改为背景／边线变化。
- 加载、空状态、错误、禁用、翻译中状态必须沿用同一层级和颜色语义。

## 响应式与无障碍

- 侧栏目标宽度：约 `320–480px`。
- 长时间码在最窄侧栏也必须完整显示；不得用省略号截断。
- 设置页宽屏双列，窄屏单列；左侧导航在窄屏转换为顶部区域导航。
- 图标按钮必须提供 `aria-label`、键盘焦点与 tooltip。
- 可见图标可以小于点击区域；点击区域不得因“简约”而缩小到难以操作。
- 保持浏览器缩放、文本选择、键盘顺序和高对比度模式可用。

## 界面主题（皮肤）

- 主题为纯表现层：`ui-themes.css` 只在 `html[data-theme="..."]` 作用域下覆盖本规范的颜色与投影令牌，不改布局、字体、间距、圆角与时间栏；`ui-themes.js` 负责启动、持久化与跨页同步。
- 三套取值：`classic`（经典工作台，即本规范默认实现，不挂 `data-theme` 属性，像素保持不变）、`ink-night`（墨夜，深色石墨＋亮蓝青）、`warm-paper`（暖纸，米色纸面＋铜棕）。
- 选择保存在 `chrome.storage.local` 的 `ytd_ui_theme`，独立于 `ytd_settings`；`localStorage` 镜像只用于首帧前同步应用，防闪烁。
- 切换入口只在侧栏头部（设置按钮旁的主题按钮＋菜单）；设置页跟随所选主题，不提供第二个切换器，左侧导航维持四个分区不变。
- 品牌图标、扩展图标与 YouTube／B 站页面入口按钮在所有主题下保持品牌渐变，不随主题变化。
- 新主题必须保持：状态卡语法（冷／暖色底＋短竖线＋中性投影）、反白文字主操作的对比度、危险红色语义独立、减少动态路径可用。

## 不可变边界

- 不改变 `原文 / 中文 / 双语` 的产品语义。
- 不绕过字幕来源授权、Provider 能力限制、Key 隔离、笔记备份兼容或本地存储边界。
- 不修改 Chrome 原生侧栏标题栏的结构。
- 不在页面按钮中恢复长品牌文字。
- 不把讨论稿中的 3D 图标、圆点时间轴或高饱和整行蓝色选择状态带回实现。
- 未经用户另行授权，不发布、不推送、不创建 Release。

## 完成标准

设计实现只有在以下条件同时满足时才能写“UI 已验证”：

1. 图标在 16/48/128px 和白／深色背景上清晰。
2. YouTube 与 B 站页面入口均为短图标按钮且可访问。
3. 字幕、章节、笔记三种状态与本规范一致。
4. `59:18`、`1:00:27`、`3:02:58` 均不挤压正文。
5. 设置页在宽屏和窄屏均无溢出或大面积无意义空白，导航活动态与固定保存栏行为正确。
6. 六个 Provider 图标离线可见；五个可用项可选择，腾讯禁用项可理解且不可误选。
7. 笔记按来源分组、标题语言切换、所选来源 TXT 导出和字幕 TXT 导出符合当前语言模式；选择器准备状态、完整／直接两个出口、自动复检和零请求边界正确。
8. 长视频首轮、暂停／继续、关闭后恢复和取消均不重复翻译已验证单元，也不自动开始下一轮或错误下载。
9. 加载、错误、空状态、禁用、焦点、取消补译和减少动态路径可用。
10. 自动测试、发布检查、真实视频检查和用户最终验收均有证据。
