# DigestDock 设计规范

<!-- impeccable:design-contract 1 -->

状态：2026-08-21 用户确认蓝青识别优化。
适用范围：Chrome 扩展图标、YouTube/B 站页面入口、侧栏字幕／概览／笔记、设置页。
实现状态：本文件记录已确认设计，不表示 UI 代码已经替换或验收通过。

## 设计主张

DigestDock 是一个以时间为索引的阅读与笔记工具。视觉层级必须优先回答三个问题：正在看哪一段、这一段发生在什么时间、下一步可以做什么。

界面使用冷灰白工作台、深石墨文字与克制的蓝青状态色。渐变只用于品牌图标、短状态线和紧凑主操作，不铺满内容表面；深度来自分组、位移和少量投影。

## 已确认视觉稿

- [完整设计总览](docs/design/final-design-board.jpg)
- [图标与页面入口](docs/design/final-icons-and-entry.jpg)
- [侧栏字幕／概览／笔记](docs/design/final-sidepanel-states.jpg)
- [设置页](docs/design/final-settings.jpg)

视觉稿是本规范的构图证据；本文件是实现时的规则真源。若二者冲突，应先停止并向用户确认，不得自行折中。

## 颜色

| 角色 | 色值 | 用途 |
| --- | --- | --- |
| Canvas | `#EDF1F3` | 设计画布与设置页外层背景 |
| Panel background | `#F7F9FA` | 侧栏内容区域 |
| Surface | `#FFFFFF` | 普通卡片、顶部栏、输入框 |
| Surface soft | `#F6F8F9` | 次级控件、非选中状态 |
| Ink | `#17212A` | 标题与主要正文 |
| Ink secondary | `#53616D` | 正文、说明 |
| Ink muted | `#89949D` | 时间、来源、元数据 |
| Line | `#DFE5E8` | 卡片与输入框细边 |
| UI blue | `#076DD1` | 当前播放文字、焦点、细线状态 |
| Action gradient | 品牌渐变＋由 `16% → 45%` 加深的透明覆层 | 反白文字的紧凑主操作，在保留亮蓝青识别度的同时满足对比度 |
| Active surface | `#EAF6FC` | 当前字幕与选中章节底色 |
| Brand gradient | `#0A5FE9 → #04B7D2` | 插件图标与页面入口底色 |
| Brand marker | `#D8F7FF` | 插件图标时间点 |
| Danger | `#C23B49` | 删除、翻译失败和错误提示 |
| Success | `#2C8A65` | 已保存、已配置 |

蓝青色表达当前状态或主要提交动作；错误和删除保持独立红色语义。字幕文本选择不得变成高饱和整行蓝色卡片。

## 字体与数字

### 常规界面

使用系统字体：

```css
font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
```

侧栏正文建议 `12.5–14px`，行高 `1.55–1.65`。设置页正文以 `15px` 为基准，导航 `14px`，服务名称 `16px`，页面标题约 `27px`。

### 时间码

时间码单独使用本地内置的 `Barlow Condensed`：Regular 400，当前状态 Medium 500。不得运行时请求第三方字体。

```css
font-family: "Barlow Condensed", "Arial Narrow", sans-serif;
font-size: 11.5px;
font-weight: 400;
font-variant-numeric: tabular-nums;
font-feature-settings: "tnum" 1;
letter-spacing: 0.025em;
```

格式规则：

- 小于 1 小时：`MM:SS`，例如 `59:18`。
- 大于或等于 1 小时：`H:MM:SS`，例如 `1:00:27`、`3:02:58`。
- 时间栏固定为约 `66px`，最长三小时样例不得挤压正文。
- 时间是辅助信息；除当前字幕／选中章节外，颜色与字重必须弱于正文。

字体实现时应内置所需 WOFF2 和 OFL 许可证。上游来源：[Google Fonts / Barlow Condensed](https://github.com/google/fonts/tree/main/ofl/barlowcondensed)。

## 图标系统

### 插件图标

正式源文件：[icons/digestdock-icon-solid.svg](icons/digestdock-icon-solid.svg)。

- 深蓝到青蓝的自然渐变圆角方形底，边缘略深、右下柔和提亮。
- 三条白色章节线。
- 一枚冰蓝白时间点。
- 已准备 `16px`、`48px`、`128px` 候选 PNG；正式接入时替换 manifest 使用的同名图标。

### 页面入口

- 打开 DigestDock：品牌图标＋`DDK` 短文字；视觉图标约 `24px`，按钮高度至少 `34px`，完整名称保留在 tooltip 与 `aria-label`。
- 保存当前时刻：使用线性 `bookmark-plus` 图标＋`金句速记 (N)` 文字；默认蓝青渐变底为 `20%`、图标和文字为 `50%`，悬浮时三者均提升到 `100%`，并轻微模糊背后视频；保留 tooltip 与 `aria-label`。
- 页面按钮不得使用 emoji、字符三角或重复显示完整 `DigestDock` 文案；页面入口统一显示 `DDK`。

### 品牌字样

- 只在品牌图标旁显示完整 `DigestDock` 时，将首字母 `D`、词中 `D` 与末尾 `K` 设为 `750`，形成克制的 `DDK` 视觉骨架；其余字母为 `600`，无图标的眉题、版本和正文保持普通字重。
- Chrome 原生侧边栏标题无法分别设置字母字重，保持标准 `DigestDock` 名称，不使用 Unicode 假粗体。

Chrome 原生侧栏标题栏的固定、关闭和容器布局不属于可设计范围；仅替换扩展名称与图标。

## 侧栏结构

### 顶部

- Chrome 原生标题栏下显示视频标题、频道／来源与时长。
- 语言模式使用紧凑分段控件：`原文 / 中文 / 双语`。
- 设置入口为图标按钮。
- 主导航保留文字：`字幕 / 概览 / 笔记`；活动项使用短蓝青渐变下划线，不使用整块胶囊。

### 字幕

- 每个字幕段落是一张独立白色卡片，卡片间距 `8px`，圆角约 `11px`。
- 正文保持原生可选择文本；正文区域不承担跳转点击，时间码负责跳转。
- 不显示整行蓝色选择状态。
- 当前播放卡向右缩进 `6px`，宽度同步缩减，避免横向溢出。
- 当前卡使用 `#EAF6FC` 底色、左侧 `3px` 蓝青渐变短竖线与中性投影 `0 8px 18px rgba(23,33,42,.10)`。
- 普通卡无投影，只使用细边。
- 左侧时间栏与正文之间使用 `1px` 竖向分隔线；不使用圆点。
- 自动跟随暂停后显示“跟随视频”按钮；按钮使用可保证白字对比度的深蓝青渐变。

### 概览

- 章节与字幕使用同一套独立卡片语法。
- 选中章节向右缩进 `6px`，使用相同冷色底、左侧蓝青竖线和投影。
- 时间码支持长视频格式。
- 章节标题优先于摘要；摘要最多使用次级正文色。
- 关键语句使用普通细边卡片，不使用粗彩色侧边。

### 笔记

- 笔记保持连续列表，而非每条都悬浮成独立厚卡。
- 顶部保留“当前视频 / 全部”筛选。
- 时间码、视频名、正文依次降级。
- 播放为蓝青渐变主操作；复制文字与复制链接为中性图标按钮；删除进入更多菜单并保持红色危险语义。

## 设置页

- 独立标签页采用左侧分区导航与右侧内容区。
- 品牌图标、焦点、链接、当前导航与主要保存动作统一使用蓝青体系，不保留旧珊瑚色。
- 导航分为：服务连接、字幕回退、笔记与备份、本地数据、本地改造、隐私说明。
- DeepSeek 与 Supadata 在宽屏双列展示；较窄窗口改为单列。
- 标题、导航、输入、说明文字均比旧版放大，不以小字制造“简约感”。
- 保存状态与保存按钮固定在内容区底部。
- 删除笔记、重置数据等操作只出现在“本地数据”的危险区域。
- “本地改造”保持高级功能后置，不与服务连接争夺首屏。

## 圆角、间距与投影

- 基础间距单位：`4px`。
- 卡片间距：`8px`。
- 控件圆角：`8–10px`。
- 内容卡圆角：`11–14px`。
- 侧栏／设置外框圆角：约 `22px`（仅设计预览容器；真实 Chrome 侧栏遵循浏览器容器）。
- 普通卡只使用细边，不叠加投影。
- 状态卡使用位移和中性投影；不得使用彩色光晕。

## 动效与状态

- 状态切换基础时间：`160ms`。
- 当前卡进入时只改变 `transform`、`background` 和 `box-shadow`。
- 按钮按下立即反馈，不等待异步操作完成后才变化。
- 保存成功使用短暂对勾或状态提示，不通过持续动画吸引注意。
- `prefers-reduced-motion: reduce` 时取消位移，改为背景／边线变化。
- 加载、空状态、错误、禁用、翻译中状态必须沿用同一层级和颜色语义。

## 响应式与无障碍

- 侧栏目标宽度：约 `320–480px`。
- 长时间码在最窄侧栏也必须完整显示；不得用省略号截断。
- 设置页宽屏双列，窄屏单列；左侧导航在窄屏转换为顶部区域导航。
- 图标按钮必须提供 `aria-label`、键盘焦点与 tooltip。
- 可见图标可以小于点击区域；点击区域不得因“简约”而缩小到难以操作。
- 保持浏览器缩放、文本选择、键盘顺序和高对比度模式可用。

## 不可变边界

- 不改变 `原文 / 中文 / 双语` 的产品语义。
- 不改变字幕、翻译、概览、笔记、备份、服务商或存储逻辑。
- 不修改 Chrome 原生侧栏标题栏的结构。
- 不在页面按钮中恢复长品牌文字。
- 不把讨论稿中的 3D 图标、圆点时间轴或高饱和整行蓝色选择状态带回实现。
- 未经用户另行授权，不发布、不推送、不创建 Release。

## 完成标准

设计实现只有在以下条件同时满足时才能写“UI 已验证”：

1. 图标在 16/48/128px 和白／深色背景上清晰。
2. YouTube 与 B 站页面入口均为短图标按钮且可访问。
3. 字幕、章节、笔记三种状态与本规范一致。
4. `59:18`、`1:00:27`、`3:02:58` 均不挤压正文。
5. 设置页在宽屏和窄屏均无溢出或大面积无意义空白。
6. 加载、错误、空状态、禁用、焦点、减少动态路径可用。
7. 自动测试、发布检查、真实视频检查和用户最终验收均有证据。
