# B 站接入基线与整合验收合同

记录日期：2026-08-19

冻结提交：`6d30f58 Add Bilibili transcripts, summaries, and notes`

冻结分支：`codex/bilibili-subtitle-poc`

工作树：`/Users/wangchao/Documents/youtube-digest-bilibili-subtitle-poc`

## 1. 当前有效目的

在不破坏 YouTube 现有能力的前提下，让本地 Chrome 扩展在标准 B 站 BV
视频页上复用已有字幕轨，提供可阅读字幕、中文结构化概览和时间戳笔记。

这是个人本地学习工具，不是批量采集器，也不以发布到 Chrome Web Store
为目标。接入应保持用户自带 DeepSeek Key、本地存储、无分析统计和最小权限
边界。

## 2. 冻结范围

### 包含

- 标准 `https://www.bilibili.com/video/BV...` 视频页。
- 当前分P；媒体身份按 BVID、CID 和分P区分。
- 当前浏览器会话可访问的人工或 AI 独立字幕轨。
- 带时间戳的字幕阅读、播放定位、中文概览和时间戳笔记。
- 中文 B 站字幕直接生成中文结果，不经过中文到英文再回中文的中间链路。
- YouTube 现有功能保持可用。

### 不包含

- B 站番剧页、直播、会员或付费内容绕过。
- 无独立字幕轨视频的音频转录、OCR 或画面硬字幕提取。
- 合集批量抓取、整站采集或后台自动运行。
- 读取、导出、保存或要求用户粘贴 B 站 Cookie。
- 把 B 站 API 或页面内部 JSON 描述为官方稳定开放接口。

## 3. 已实现并验收的功能结果

### 3.1 媒体与分P身份

- 标准 BV URL 能解析 BVID 与 `p`。
- 当前分P通过 BVID 请求视频信息并取得 AID、CID、标题、UP 主、简介和时长。
- 稳定缓存与笔记键为 `bilibili:BVID:CID`；路由身份为 `BVID + p`。
- 时间戳链接保留当前分P并使用 B 站 `t` 参数。

### 3.2 字幕读取

- 字幕链路为：视频信息接口 → 当前 CID → 播放器字幕列表 → 字幕 JSON
  `body[from,to,content]`。
- 轨道优先级为人工中文、AI 中文、其他可用轨道。
- 字幕被规范化为现有 `{ text, start, duration, language }` 合同。
- B 站字幕读取不调用 Supadata，也不消耗 Supadata 额度。
- 登录态只随固定 B 站 API 请求发送；字幕 CDN 请求使用
  `credentials: "omit"`。
- 临时签名字幕 URL 不进入返回结果、缓存、日志或笔记。

### 3.3 页面与侧栏

- B 站工具栏中的“生成摘要”按钮位于原生“笔记”按钮之前，并保持单实例。
- 播放器悬浮笔记按钮和 `N` 快捷键可保存当前时间点。
- 侧栏能显示当前视频标题、UP 主和带时间戳字幕。
- 字幕、概览、笔记三个标签在 B 站页面均可正常使用。
- 页面动态更新后会修复缺失按钮；分P或视频切换不会复用错误媒体身份。

### 3.4 中文概览

- 中文字幕直接生成中文章节和关键语句。
- 已验证一次概览只发生一次 AI 生成，不进行英文中转或第二次中文翻译。
- 章节时间戳可用于定位视频。
- 合并到当前主线后，输出结构必须采用主线 Chinese-first schema v3；这里冻结的
  是功能行为，不要求保留旧 schema v2 实现。

### 3.5 时间戳笔记

- B 站笔记保存 BVID、CID、分P、稳定媒体键和安全时间戳 URL。
- 中文字幕笔记可直接进行一次中文润色，并标记为中文文本；不再发起第二次
  中文翻译。
- 笔记页支持当前视频筛选、复制文字、复制时间戳和播放定位。
- 新中文笔记显示润色后的 `text`；旧版中文笔记仍保持兼容行为。

### 3.6 竞态与隔离

- 视频或分P切换后，旧概览响应不能覆盖或写入新媒体缓存。
- 旧笔记加载响应不能覆盖新媒体笔记列表。
- 页面命令使用明确的目标 tab，不把播放或高亮指令发送到另一视频标签页。

## 4. 权限与数据边界

- Manifest 没有 `cookies` 权限。
- 内容脚本只匹配标准 B 站 BV 视频页。
- B 站 API 请求固定在受信任的 B 站 API 域名。
- 字幕 URL 只接受 HTTPS 的 B 站、hdslb 或 bilivideo 域名。
- B 站响应具有超时和最大正文限制。
- 页面动态文本通过安全 DOM 文本节点呈现；时间戳 URL 在使用前重新校验。
- DeepSeek 仅在用户触发概览、讲解或笔记润色时接收所需字幕上下文。
- 自动检查覆盖了上述边界，但没有完成独立的完整安全审计；不得把本记录描述为
  第三方安全认证。

## 5. 已取得的证据

### 自动检查

- 冻结提交前 `npm run check`：91/91 通过。
- 发布白名单检查：24 个文件通过。
- `node --check`：正式 B 站适配器和内容脚本通过。
- 正式包包含 `bilibili.js` 与 `content-bilibili.js`，不包含 `poc/` 或
  `tests/`。
- 冻结包：`dist/youtube-digest-v1.1.5.zip`。
- 冻结包 SHA-256：
  `0cc5e55725678e3b13da8757394205b854daa900f265646b05027951b99cac7b`。

### Chrome 实机验证

验证视频：`BV1zfg36ZEXi`，P1。

- 解析媒体键：`bilibili:BV1zfg36ZEXi:40830435549`。
- 读取到 1 条中文 AI 字幕轨和 167 个有效片段。
- 正式侧栏显示完整时间戳字幕。
- 正式侧栏生成中文章节概览，覆盖视频开头至后段。
- 3:02 与 3:06 两条测试笔记出现在“当前视频”笔记列表。
- 3:06 笔记完成中文润色，并显示复制文字、复制时间戳和播放入口。
- 页面中摘要按钮与播放器笔记按钮均为单实例，未观察到扩展错误。

### v1.3.0 组合版本 Chrome 回归

- B 站单P `BV1dzbv6eEmj`：读取 `zh-CN` 时间戳字幕并生成主线
  Chinese-first v3 中文章节概览。
- B 站页面自动连播切到 `BV1uQ8E6LE6G` 后，媒体标题、URL、字幕和新笔记
  同步切换，没有把旧视频身份写入新笔记。
- B 站多P `BV1aETD6mEEZ` 共 21P：P1 CID `39690438616`，P2 CID
  `39690440407`；P1 与 P2 均成功读取字幕并保存内容不同的中文笔记，没有串P。
- YouTube `0KHvXrq0gT8`：Supadata 英文字幕与时间戳正常，0:04 笔记完成
  英文清理并保存。
- 页面在扩展重新加载后按主线合同要求刷新；没有恢复动态脚本重注入。
- 笔记备份导出 5 条到 `youtube-digest-notes-2026-08-20.json`；删除全部
  笔记后导入该 schema v2 文件，页面显示“已恢复 5 条新笔记，当前共保存 5
  条笔记”。
- 恢复结果同时覆盖 YouTube、B 站单P和 B 站多P笔记；时间戳 URL 由经过
  校验的媒体身份重新构建。

完整浏览器截图未写入仓库，避免把浏览器标签、账号头像等无关信息作为项目
资产保存；本节只记录已观察的验收事实。

### 当前整合证据

- 2026-08-19：7 个文本冲突文件和全部自动合并文件完成 main-first 收口，
  Git 未合并条目为 0。
- `npm test` 与 `npm run check`：175/175 通过。
- 发布白名单检查：25 个文件通过。
- 所有发布 JavaScript 的 `node --check` 通过。
- 组合包：`dist/youtube-digest-v1.3.0.zip`。
- 组合包 SHA-256：
  `8c28a4ba48bbfdc75e389f2aa8ffa63095844eb25331cdfbb7568d1a8184f09f`。
- ZIP 完整性检查通过；25/25 个 ZIP 文件与当前源码逐字节一致。
- ZIP 包含 `notes-backup.js`、`bilibili.js` 和 `content-bilibili.js`，
  不包含 PoC、测试、验收文档或凭据。
- 组合逻辑复核未发现剩余代码级合并阻断项；Chrome 组合版本回归随后完成，
  详见本文件的实机证据与验收清单。
- 本地 `main` 快进到 `0c9c0a4` 后重新执行 `npm run check`：175/175
  通过，发布检查仍为 25 个文件。
- 主工作树重新生成 `dist/youtube-digest-v1.3.0.zip`，SHA-256 为
  `30af729af95eca9b4146918835cfd8b88573ae88a7e7a3baf39ccc8dc2fdf11d`；
  ZIP 完整性和 25/25 源文件比对通过。

## 6. 吸收当前 main 时必须保留的主线能力

当前待吸收主线基线：`main@5e87ca3`，版本 `1.2.1`，主线
`npm run check` 为 131/131。

整合必须以主线实现为骨架，并保留：

- Chinese-first overview schema v3 和按需原文翻译。
- YouTube 精确字幕语言请求及错误语言回退拒绝。
- 页面接收器缺失时先重试、最终提示刷新；不得恢复动态重复注入脚本。
- A-B-A 视频切换、概览、字幕和笔记的 generation/陈旧响应保护。
- UUID 笔记 ID、100 条容量限制、写队列，以及保存与清空/重置竞态保护。
- 笔记翻译的限次、超时、冷却和验证字段。
- 笔记 JSON 备份与恢复的原子性、字段白名单和旧备份兼容。

## 7. 整合新增的强制兼容项

主线的笔记备份 schema v1 只接受 YouTube ID，并固定重建 YouTube URL。
组合版本必须：

- 升级为明确的新备份 schema，并继续导入旧 v1 YouTube 备份。
- 为 B 站笔记验证并保存 `platform`、`mediaKey`、`bvid`、`cid`、`page` 和
  `textLanguage`。
- 根据已验证的媒体字段重建 B 站时间戳 URL，不信任备份文件提供的 URL。
- 保持重复 ID、容量超限、损坏 JSON、过大文件和身份冲突的失败关闭行为。

## 8. 整合后验收清单

只有以下全部满足，组合版本才可进入主线：

- [x] `background.js` 与 `sidepanel.js` 使用同一新运行时协议，且不回退到 v4。
- [x] `manifest.json` 与 `package.json` 的组合版本一致为 `1.3.0`。
- [x] 主线 Chinese-first schema v3 测试全部保留并通过。
- [x] 主线 131 项测试没有被删除来换取通过。
- [x] B 站适配器、页面脚本、PoC 和跨平台集成测试通过。
- [x] YouTube 精确字幕语言、缓存失效和页面刷新恢复自动回归通过。
- [x] B 站 P1/P2 使用不同 CID 与媒体键，旧响应不能串页。
- [x] B 站简体中文概览一次 AI 请求，无英文中转。
- [x] B 站简体中文笔记一次中文润色、零二次翻译。
- [x] 自动测试覆盖 YouTube/B 站备份往返、v1 导入兼容和安全时间戳重建。
- [x] `npm test`、`npm run check`、`npm run package` 全部通过。
- [x] ZIP 同时包含 `notes-backup.js`、`bilibili.js`、
  `content-bilibili.js`，且不包含 PoC、测试或凭据。
- [x] Chrome 实测一个 YouTube 视频、一个 B 站单P视频和一个 B 站多P视频，
  并验证两平台笔记备份、清空后恢复。
- [x] 主线合并前获得用户确认；没有自动推送或发布。

## 9. 当前阶段与准确下一步

当前阶段：本文件已保存 B 站独立实现的目的、功能结果和实机证据。从冻结点
建立的 `codex/bilibili-main-integration` 已吸收 `main@5e87ca3`，并形成整合
提交 `0c9c0a4`。冲突、跨平台备份兼容、完整自动测试、v1.3.0 打包和 Chrome
组合版本回归均已完成；经用户确认，本地 `main` 已使用 `--ff-only` 快进到
`0c9c0a4`。远程未推送，版本未公开发布。

准确下一步：当前本地整合任务已完成。若未来需要同步 GitHub 或公开发布，必须
另行确认远程范围、发布说明和凭据边界；本任务不自动推送或发布。
